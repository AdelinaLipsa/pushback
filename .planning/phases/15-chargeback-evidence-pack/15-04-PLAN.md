---
phase: 15-chargeback-evidence-pack
plan: 04
type: execute
wave: 3
depends_on: [15-01, 15-02, 15-03]
files_modified:
  - app/api/projects/[id]/dispute-pack/route.ts
  - tests/api/dispute-pack.test.ts
autonomous: true
requirements:
  - PHASE-15 (ROADMAP success criteria 1, 2, 3 — one-click pack generation, full contents in D-05 order, ranking applied)
must_haves:
  truths:
    - "POST /api/projects/[id]/dispute-pack exists and returns 401 with { error: 'Unauthorized' } to unauthenticated users"
    - "Route returns 403 with { error: 'PRO_REQUIRED' } when profile.plan !== 'pro' (D-16)"
    - "Route returns 404 with { error: 'Not found' } when project does not belong to the caller (IDOR guard via .eq('user_id', user.id))"
    - "Route returns 400 when dispute_type is missing or not one of the 4 D-06 values, or when case_reference > 80 chars"
    - "Route gates against the shared defense_responses quota via check_and_increment_defense_responses RPC (D-18) and returns 403 UPGRADE_REQUIRED when the gate denies"
    - "On success returns 200 with Content-Type: application/pdf and Content-Disposition: attachment; filename=\"dispute-pack-{type}-{slug}-{YYYY-MM-DD}.pdf\""
    - "Every post-RPC failure path (validation, project 404, renderPack throw, empty buffer, outer catch) calls decrement_defense_responses so the user's credit is not consumed"
    - "Route is Node runtime (NOT edge) — @react-pdf/renderer requires Node APIs (Buffer, streams)"
    - "End-to-end p50 generation time stays under 10s (ROADMAP SC 1; D-21 budgets ≤2s render + ≤0.5s ranking/extract + DB)"
  artifacts:
    - path: "app/api/projects/[id]/dispute-pack/route.ts"
      provides: "POST handler — auth → Pro gate → rate limit → RPC quota gate → Zod validate → IDOR-safe fetch → assemblePackData → renderPack → binary Response with attachment Content-Disposition"
      exports: ["POST", "maxDuration"]
      contains: "renderPack"
    - path: "tests/api/dispute-pack.test.ts"
      provides: "Vitest suite covering 401 / 403-PRO / 403-RPC / 404 / 400-type / 400-len / 200-happy / 500-decrement / all-four-types-accepted"
      contains: "describe('POST /api/projects/[id]/dispute-pack'"
  key_links:
    - from: "route.ts"
      to: "lib/dispute/assemble.ts"
      via: "assemblePackData(input)"
      pattern: "assemblePackData\\("
    - from: "route.ts"
      to: "lib/dispute/renderPack.tsx"
      via: "renderPack(data)"
      pattern: "renderPack\\("
    - from: "route.ts"
      to: "user_profiles + projects + contracts + defense_responses"
      via: "supabase joined SELECT"
      pattern: "from\\('projects'\\)"
    - from: "route.ts"
      to: "check_and_increment_defense_responses + decrement_defense_responses"
      via: "supabase.rpc"
      pattern: "check_and_increment_defense_responses"
---

<objective>
Ship the API route that turns the Plans 01-03 pipeline into a downloadable PDF. Same security stack as the existing document route (auth → Pro gate → rate limit → RPC quota gate → IDOR-safe project fetch → compensating decrement on every failure path), with `renderPack` + a binary `Response` replacing Anthropic + JSON.

Purpose: closes the back-end loop. Once this lands, a Pro user can POST and receive a real downloadable PDF. Plan 05's UI consumes this endpoint. Decoupled from the UI so the route is testable in isolation (Vitest mocks Supabase + `renderPack`).

Output: 1 new route file at `app/api/projects/[id]/dispute-pack/route.ts`, 1 test suite at `tests/api/dispute-pack.test.ts`. No DB schema changes, no new RPC functions, no new dependencies (`@react-pdf/renderer@^4.5.1` is already installed from Plan 02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-chargeback-evidence-pack/15-CONTEXT.md
@.planning/phases/15-chargeback-evidence-pack/15-01-SUMMARY.md
@.planning/phases/15-chargeback-evidence-pack/15-02-SUMMARY.md
@.planning/phases/15-chargeback-evidence-pack/15-03-SUMMARY.md

@CLAUDE.md
@AGENTS.md

@app/api/projects/[id]/document/route.ts
@lib/supabase/server.ts
@lib/rate-limit.ts
@lib/dispute/assemble.ts
@lib/dispute/renderPack.tsx
@tests/api/defend.test.ts
@tests/helpers/supabase.ts

<interfaces>
**Canonical pattern to mirror — `app/api/projects/[id]/document/route.ts` (read this file end-to-end).** The shipped document route lays out the exact gate sequence this route MUST follow: (1) `await params` for `id`, (2) `createServerSupabaseClient` + `supabase.auth.getUser()` → 401 on missing user, (3) fetch `user_profiles` → 403 `PRO_REQUIRED` when `profile.plan !== 'pro'`, (4) `checkRateLimit(defendRateLimit, user.id)` early-return, (5) `supabase.rpc('check_and_increment_defense_responses', { uid })` → 403 with `gate.reason ?? 'UPGRADE_REQUIRED'` on denial, (6) wrap the rest in try/catch so the outer catch always calls `decrement_defense_responses` before returning 500, (7) Zod-parse the body — on parse failure decrement then return 400 with `${field}: ${message}`, (8) project fetch with `.eq('id', id).eq('user_id', user.id).single()` — on miss decrement then 404, (9) normalize the `contracts` join via `Array.isArray` (Supabase returns object OR array depending on relationship cardinality).

**From `@/lib/supabase/server.ts`:**
- `createServerSupabaseClient(): Promise<SupabaseClient<Database>>` — async (always `await`).
- (`createServiceSupabaseClient` exists too but is NOT needed here — RLS + the IDOR `.eq('user_id', user.id)` guard are sufficient.)

**From `@/lib/rate-limit.ts`:**
- `defendRateLimit: Ratelimit | null`
- `checkRateLimit(limit, userId): Promise<Response | null>` — returns a 429 Response when limited, `null` when allowed.
- `acquireAnthropicSlot` / `releaseAnthropicSlot` are NOT needed here (no LLM call).

**From `@/lib/dispute/`:**
- `assemblePackData(input: AssembleInput): PackData` (Plan 01) — pure function. `AssembleInput` shape is exported from `lib/dispute/assemble.ts` and includes `disputeType`, `caseReference`, `user: { id, email, full_name }`, `project` (subset of row), `contract: { contract_text, clausesPresent } | null`, `responses[]`. The route builds this from the Supabase join rows.
- `renderPack(data: PackData): Promise<Buffer>` (Plan 03) — server-side renderer. Returns a Node `Buffer`. Verified at `node_modules/@react-pdf/renderer/lib/react-pdf.d.cts:790`.

**Supabase SELECT for this route (NEW shape — pulls more fields than `document/route.ts`):**
```
.from('projects').select(`
  id, title, client_name, project_value, currency,
  payment_due_date, payment_received_at, payment_amount,
  contracts(id, contract_text, analysis),
  defense_responses(id, tool_type, response, situation, created_at, was_sent)
`).eq('id', id).eq('user_id', user.id).single()
```
The `analysis` column is JSON; the only field this route reads is `analysis.clauses_present` (D-11 placeholder gate). Treat it as `string[] | null | undefined`.

**Runtime — Node, not edge.** `@react-pdf/renderer` needs Node `Buffer` + streams. The shipped document route already runs on the Node runtime (no `export const runtime = 'edge'`), and that is the correct default for this route too. Set `export const maxDuration = 30` at the top of the file — matches the document route and is comfortably above the D-21 ≤10s budget.

**Existing Vitest patterns — `tests/api/defend.test.ts` and `tests/helpers/supabase.ts`.** The test file in this plan must reuse `makeSupabaseMock` from `tests/helpers/supabase.ts` rather than redefining its own mock shape. If the helper does not currently accept the `user` / `rpcMap` shape the test needs (read its current signature before writing the test), extend the helper minimally — do NOT inline a separate mock.

**Pro-only enforcement is server-side authoritative.** The UI in Plan 05 hides the button from free users, but the server is the gate. Per CONTEXT D-16 the route MUST return 403 `PRO_REQUIRED` to any free user that hits the endpoint directly.

**No new DB tables, no audit log (D-17).** The route only writes via the RPC quota counter (and the compensating decrement). It does NOT insert into `defense_responses` or any other table.

**No LLM at runtime (D-23).** The route MUST NOT import from `@/lib/anthropic`. Pack assembly + rendering are fully deterministic.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build POST /api/projects/[id]/dispute-pack route with the full security stack and PDF binary response</name>
  <files>app/api/projects/[id]/dispute-pack/route.ts</files>
  <read_first>
    - app/api/projects/[id]/document/route.ts (full file end-to-end — the canonical gate sequence this route mirrors)
    - lib/dispute/assemble.ts (the `AssembleInput` shape — the route maps Supabase rows into this exact object)
    - lib/dispute/renderPack.tsx (confirm `renderPack` signature returns `Promise<Buffer>`)
    - lib/rate-limit.ts (defendRateLimit + checkRateLimit signatures, lines around 13-15 and 52-80)
    - .planning/phases/15-chargeback-evidence-pack/15-CONTEXT.md (D-03 endpoint + response shape; D-06 4 dispute types; D-16 Pro-only; D-17 no DB writes beyond quota; D-18 shared defense_responses quota counts as 1 unit; D-22 standard Vercel timeout; D-23 no LLM)
  </read_first>
  <behavior>
    - Unauthenticated POST returns 401 with body `{ error: 'Unauthorized' }`
    - Free-plan user POST returns 403 with body `{ error: 'PRO_REQUIRED' }` BEFORE any quota increment
    - Pro user with quota denied by RPC returns 403 with body `{ error: gate.reason ?? 'UPGRADE_REQUIRED' }`
    - Invalid body (missing `dispute_type`, unknown `dispute_type`, `case_reference` length > 80) returns 400 with body `{ error: '${field}: ${message}' }` AND calls `decrement_defense_responses`
    - Project not owned by caller returns 404 with body `{ error: 'Not found' }` AND calls `decrement_defense_responses`
    - `renderPack` throw → 500 with body `{ error: 'Pack generation failed — please try again' }` AND calls `decrement_defense_responses`
    - Empty/zero-byte buffer → same 500 path with decrement
    - Outer try/catch → same 500 path with decrement
    - Happy path returns status 200 with headers `Content-Type: application/pdf`, `Content-Disposition: attachment; filename="dispute-pack-{type}-{slug}-{YYYY-MM-DD}.pdf"`, `Content-Length: ${buffer.byteLength}`, `Cache-Control: private, no-store`, and body = the rendered PDF buffer
    - All 4 D-06 dispute types are accepted (`not_as_described`, `not_received`, `cancelled`, `unauthorized`); any other value yields 400
  </behavior>
  <action>
Create the file `app/api/projects/[id]/dispute-pack/route.ts`. Start with `export const maxDuration = 30` on line 1 (same as the document route). Do NOT set `export const runtime = 'edge'` — the file runs on the Node runtime so `@react-pdf/renderer`'s `Buffer`/stream APIs work.

Imports: `createServerSupabaseClient` from `@/lib/supabase/server`, `checkRateLimit` and `defendRateLimit` from `@/lib/rate-limit`, `assemblePackData` from `@/lib/dispute/assemble`, `renderPack` from `@/lib/dispute/renderPack`, `z` from `zod`. Do NOT import `anthropic`, `acquireAnthropicSlot`, or `releaseAnthropicSlot` — this route has no LLM call (per D-23).

Declare the dispute-type tuple as `const DISPUTE_TYPE_VALUES = ['not_as_described', 'not_received', 'cancelled', 'unauthorized'] as const` so Zod gets the exact D-06 set. Build the Zod schema `disputePackSchema = z.object({ dispute_type: z.enum(DISPUTE_TYPE_VALUES), case_reference: z.string().max(80).optional() })`. The 80-char cap matches the `CASE_REF_MAX` in `lib/dispute/assemble.ts`'s `sanitizeCaseRef` — Plan 01 already silently truncates server-side, but rejecting >80 at the route boundary surfaces the error to the caller cleanly.

Define a helper `safeFilename(disputeType: string, projectTitle: string): string` that strips `projectTitle` to `[A-Za-z0-9- ]`, collapses runs of whitespace to dashes, slices to 40 chars, lowercases, falls back to `'project'` if empty, then returns `` `dispute-pack-${disputeType}-${cleanTitle}-${stamp}.pdf` `` where `stamp = new Date().toISOString().slice(0, 10)`. The whitelist guarantees the Content-Disposition `"..."` quoting cannot be broken out of (mitigates T-15-22).

Implement the handler `export async function POST(request: Request, { params }: { params: Promise<{ id: string }> })`. The Next.js 16 convention is to `await params` — the param object is a Promise (AGENTS.md: "This is NOT the Next.js you know"). Inside the handler, follow this gate order (mirrors the document route):

1. `const { id } = await params`.
2. `const supabase = await createServerSupabaseClient()`.
3. `const { data: { user } } = await supabase.auth.getUser()`; if no `user`, return `Response.json({ error: 'Unauthorized' }, { status: 401 })`.
4. Fetch profile: `supabase.from('user_profiles').select('plan, email, full_name').eq('id', user.id).single()`. If missing or `profile.plan !== 'pro'`, return `Response.json({ error: 'PRO_REQUIRED' }, { status: 403 })`. The Pro gate runs BEFORE rate limit + RPC so free users do not consume rate-limit budget.
5. `const rateLimitResponse = await checkRateLimit(defendRateLimit, user.id)`; if non-null, return it as-is.
6. `supabase.rpc('check_and_increment_defense_responses', { uid: user.id })`. Treat the result as `{ allowed: boolean; reason?: string } | null`. If error OR `!gate.allowed`, return `Response.json({ error: gate?.reason ?? 'UPGRADE_REQUIRED' }, { status: 403 })`.

After the RPC succeeds, every failure path inside the try block MUST `await supabase.rpc('decrement_defense_responses', { uid: user.id })` before returning. Wrap steps 7-12 in `try { ... } catch (err) { console.error('Dispute-pack route error:', err); await supabase.rpc('decrement_defense_responses', { uid: user.id }); return Response.json({ error: 'Pack generation failed — please try again' }, { status: 500 }) }`.

7. Parse body: `const body = await request.json()`; `const parsed = disputePackSchema.safeParse(body)`. On failure, take the first issue, decrement, and return `Response.json({ error: \`${String(issue.path[0])}: ${issue.message}\` }, { status: 400 })`. Destructure `{ dispute_type, case_reference } = parsed.data`.
8. Project fetch: use the SELECT shape from the `<interfaces>` block (`id, title, client_name, project_value, currency, payment_due_date, payment_received_at, payment_amount, contracts(id, contract_text, analysis), defense_responses(id, tool_type, response, situation, created_at, was_sent)`), chain `.eq('id', id).eq('user_id', user.id).single()`. The `.eq('user_id', user.id)` IDOR guard is critical — without it a Pro user could enumerate other users' projects. If `!project`, decrement and return 404.
9. Normalize `contracts`: declare a local type `type ContractRow = { id: string; contract_text: string | null; analysis: { clauses_present?: string[] | null } | null }`. Then `const contractRow: ContractRow | null = Array.isArray(project.contracts) ? (project.contracts[0] ?? null) : (project.contracts ?? null)`. This mirrors the document route's handling.
10. Build the `AssembleInput`: pass `disputeType: dispute_type`, `caseReference: case_reference ?? null`, `user: { id: user.id, email: profile.email ?? user.email ?? '', full_name: profile.full_name ?? null }`, `project: { id, title, client_name, project_value, currency, payment_due_date, payment_received_at, payment_amount }` (just copy the relevant fields from the joined row), `contract: contractRow ? { contract_text: contractRow.contract_text, clausesPresent: contractRow.analysis?.clauses_present ?? null } : null`, `responses: project.defense_responses ?? []` (cast to the shape `AssembleInput['responses']`). Call `const packData = assemblePackData(input)`.
11. Render: wrap `await renderPack(packData)` in its own try/catch. On throw OR empty buffer (`!pdfBuffer || pdfBuffer.byteLength === 0`), `console.error` the error, decrement, return 500 with the standard error body.
12. Success response: construct `const filename = safeFilename(dispute_type, project.title)` and return `new Response(pdfBuffer as unknown as BodyInit, { status: 200, headers: { 'Content-Type': 'application/pdf', 'Content-Disposition': \`attachment; filename="${filename}"\`, 'Content-Length': String(pdfBuffer.byteLength), 'Cache-Control': 'private, no-store' } })`. The `private, no-store` cache directive prevents intermediaries from caching client PII (mitigates T-15-21).

Forbidden patterns:
- Do NOT `import { anthropic } from '@/lib/anthropic'` — D-23 prohibits LLM at runtime.
- Do NOT insert into `defense_responses` or any other table — D-17 forbids DB writes beyond the quota counter.
- Do NOT use `params.id` directly without `await` — Next.js 16 requires `await params` (AGENTS.md heeds this breaking change).
- Do NOT serialize the PDF as JSON or base64 — return the binary `Buffer` with `Content-Type: application/pdf`.
- Do NOT mutate the `'PRO_REQUIRED'` vs `'UPGRADE_REQUIRED'` error strings — Plan 05's UI discriminates on them.
- Do NOT skip `decrement_defense_responses` on any failure path after the RPC gate succeeded — the test in Task 2 explicitly verifies the decrement on `renderPack` throw.
  </action>
  <verify>
    <automated>test -f 'app/api/projects/[id]/dispute-pack/route.ts' && grep -q "renderPack" 'app/api/projects/[id]/dispute-pack/route.ts' && grep -q "assemblePackData" 'app/api/projects/[id]/dispute-pack/route.ts' && grep -q "PRO_REQUIRED" 'app/api/projects/[id]/dispute-pack/route.ts' && grep -q "UPGRADE_REQUIRED" 'app/api/projects/[id]/dispute-pack/route.ts' && grep -q "application/pdf" 'app/api/projects/[id]/dispute-pack/route.ts' && grep -q "Content-Disposition" 'app/api/projects/[id]/dispute-pack/route.ts' && grep -q "check_and_increment_defense_responses" 'app/api/projects/[id]/dispute-pack/route.ts' && [ "$(grep -c "decrement_defense_responses" 'app/api/projects/[id]/dispute-pack/route.ts')" -ge "4" ] && [ "$(grep -c "eq('user_id', user.id)" 'app/api/projects/[id]/dispute-pack/route.ts')" = "1" ] && [ "$(grep -cE "anthropic|Anthropic" 'app/api/projects/[id]/dispute-pack/route.ts')" = "0" ] && npx tsc --noEmit</automated>
  </verify>
  <done>
    Route exists at `app/api/projects/[id]/dispute-pack/route.ts`. All 6 gates present in order (auth → Pro → rate limit → RPC quota → validate → IDOR fetch). `assemblePackData` + `renderPack` invoked. 200 returns `application/pdf` with attachment `Content-Disposition`. Every post-RPC failure path calls `decrement_defense_responses` (grep finds ≥4 calls). No anthropic import. No DB writes other than the quota counter. Type-check passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Vitest suite covering 401 / 403 / 404 / 400 / 200 paths with mocked Supabase + renderPack</name>
  <files>tests/api/dispute-pack.test.ts</files>
  <read_first>
    - tests/api/defend.test.ts (full file — Vitest mock pattern, `makeRequest` + `makeParams` helpers, `beforeEach` reset, mock-module ordering)
    - tests/helpers/supabase.ts (full file — confirm the current signature of `makeSupabaseMock` before extending it)
    - app/api/projects/[id]/dispute-pack/route.ts (Task 1 — POST signature, exact error strings, gate sequence)
  </read_first>
  <behavior>
    - `it('returns 401 when unauthenticated')` → expect status 401, body `{ error: 'Unauthorized' }`
    - `it('returns 403 PRO_REQUIRED when plan is free')` → expect 403, `{ error: 'PRO_REQUIRED' }`
    - `it('returns 403 UPGRADE_REQUIRED when quota RPC denies')` → expect 403, `{ error: 'UPGRADE_REQUIRED' }`
    - `it('returns 404 when project does not belong to user')` → expect 404, IDOR-safe
    - `it('returns 400 for invalid dispute_type')` → expect 400
    - `it('returns 400 when case_reference exceeds 80 chars')` → expect 400
    - `it('returns 200 application/pdf with attachment Content-Disposition on success')` → expect 200, `content-type` is `application/pdf`, `content-disposition` matches `/^attachment; filename=".*\.pdf"$/`, `renderPack` was called exactly once
    - `it('decrements credit when renderPack throws')` → mock `renderPack` to reject, expect 500, expect `mock.rpc` was called with `('decrement_defense_responses', { uid: 'user-1' })`
    - `it('accepts all four D-06 dispute types')` → loop the 4 values, each returns 200
  </behavior>
  <action>
Create `tests/api/dispute-pack.test.ts`. Mirror `tests/api/defend.test.ts` exactly for the Vitest setup pattern (`vi.mock` declarations BEFORE imports, `beforeEach(() => vi.clearAllMocks())`, helper functions `makeRequest` and `makeParams`, mock of `createServerSupabaseClient` via `vi.mocked(...)`).

Three top-level `vi.mock` calls before any non-type import:
1. `vi.mock('@/lib/supabase/server', () => ({ createServerSupabaseClient: vi.fn() }))`
2. `vi.mock('@/lib/rate-limit', () => ({ defendRateLimit: null, checkRateLimit: vi.fn().mockResolvedValue(null) }))`
3. `vi.mock('@/lib/dispute/renderPack', () => ({ renderPack: vi.fn().mockResolvedValue(Buffer.from('%PDF-1.4 mock', 'utf-8')) }))` — this is critical: the test must NEVER actually render a real PDF. The mocked buffer just needs `byteLength > 0` so the empty-buffer guard does not fire.

Imports after the mocks: `describe`, `it`, `expect`, `vi`, `beforeEach` from `vitest`; `makeSupabaseMock` from `../helpers/supabase`; the mocked `createServerSupabaseClient` and `renderPack` (so the tests can re-mock per-case); the route's `POST` from `@/app/api/projects/[id]/dispute-pack/route`.

Define `mockProject` at module scope with the same shape the route's SELECT returns: `id`, `title` (`'Brand Identity Refresh'`), `client_name` (`'ACME Inc.'`), `project_value`, `currency`, `payment_due_date`, `payment_received_at`, `payment_amount`, plus a `contracts` join (object shape — single related row, with `contract_text` and `analysis: { clauses_present: ['scope'] }`) and `defense_responses` (1-element array with the full row shape).

Define `makeRequest(body, projectId = 'project-1')` returning `new Request(\`http://localhost/api/projects/${projectId}/dispute-pack\`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(body) })`. Define `makeParams(id = 'project-1')` returning `{ params: Promise.resolve({ id }) }` — the route awaits the params Promise per Next.js 16.

Define `setup(overrides?: { authed?: boolean; plan?: 'free' | 'pro'; project?: unknown; rpcAllowed?: boolean })` that builds the mock via `makeSupabaseMock({ user: authed ? { id: 'user-1', email: 'u@example.com' } : null, fromMap: { user_profiles: { data: { plan, email: 'u@example.com', full_name: 'Test User' }, error: null }, projects: { data: project, error: null } }, rpcMap: { check_and_increment_defense_responses: { data: { allowed: rpcAllowed, reason: rpcAllowed ? undefined : 'UPGRADE_REQUIRED' }, error: null }, decrement_defense_responses: { data: null, error: null } } })`, then `vi.mocked(createServerSupabaseClient).mockResolvedValue(mock as any)`, then `return mock`.

If the current `makeSupabaseMock` helper in `tests/helpers/supabase.ts` does NOT support `user` / `rpcMap` parameters as shown, extend the helper minimally (single helper-side edit). Do NOT inline a parallel mock — keeping one shared helper preserves consistency with the 4 existing test files.

Write the 9 `it()` cases listed in `<behavior>`. The all-four-dispute-types loop should iterate the literal array `['not_as_described', 'not_received', 'cancelled', 'unauthorized']` and assert `expect(res.status, \`dispute_type=${dt}\`).toBe(200)` so a failure surfaces the offending type.

For the `case_reference > 80 chars` case, use `'x'.repeat(81)` and expect 400. (The `disputePackSchema.max(80)` enforces this server-side.)

For the `decrements credit when renderPack throws` case, call `vi.mocked(renderPack).mockRejectedValueOnce(new Error('boom'))` AFTER `setup()`, then assert both `res.status === 500` and `expect(mock.rpc).toHaveBeenCalledWith('decrement_defense_responses', { uid: 'user-1' })`. If the helper exposes the `rpc` mock differently (e.g. `mock.rpcMock` or `mock.calls`), the executor adapts the assertion — the intent is "decrement was called for this user", not the exact property name.
  </action>
  <verify>
    <automated>test -f tests/api/dispute-pack.test.ts && npx vitest run tests/api/dispute-pack.test.ts</automated>
  </verify>
  <done>
    `tests/api/dispute-pack.test.ts` ships 9 cases mirroring the document route's test conventions. Supabase + `renderPack` mocked end-to-end — no real PDF generated. All cases pass. The mock buffer's bytes do NOT need to be a real PDF; just `byteLength > 0`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /api/projects/[id]/dispute-pack | Untrusted body (`dispute_type`, `case_reference`), URL param `id`, session cookie |
| API → Supabase | Authenticated query with RLS + IDOR guard `.eq('user_id', user.id)` |
| API → Buffer → Browser | Generated PDF served as binary attachment with `private, no-store` cache directive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-14 | Spoofing | POST handler | mitigate | `supabase.auth.getUser()` returns 401 on missing session. Mirrors the document/defend routes; ASVS V2.1. |
| T-15-15 | Elevation of Privilege (Pro bypass) | POST handler | mitigate | `profile.plan !== 'pro'` returns 403 PRO_REQUIRED BEFORE rate limit + RPC, so free users cannot consume quota budget. ASVS V4. |
| T-15-16 | Elevation of Privilege (quota bypass) | POST handler | mitigate | `check_and_increment_defense_responses` RPC enforces per-period quota atomically (existing infrastructure from Phase 1). |
| T-15-17 | Tampering / IDOR | project fetch | mitigate | `.eq('user_id', user.id)` plus 404 (not 403) when mismatched prevents project-id enumeration. ASVS V4. Verified by the 404 test case. |
| T-15-18 | Tampering / Input | Zod validation | mitigate | `z.enum(['not_as_described', ...])` enforces the 4 locked D-06 dispute types; case_reference capped at 80 chars; both surface 400 with the failing field. |
| T-15-19 | DoS | renderPack on large fixtures | mitigate | Plan 01's `rankMessages` caps the corpus at 15. `extractClauses` caps at 8 excerpts × 500 chars. Plan 03's CommunicationLog uses `wrap={false}`; React-PDF paginates safely. D-21 perf budget headroom is ample (~2s render). |
| T-15-20 | Repudiation | No audit log of generated packs | accept | D-17 forbids new DB tables. The shared `defense_responses` quota counter is the only persistent record. Revisit in v2 if the audit-table decision changes. |
| T-15-21 | Information Disclosure | Generated PDF contains client PII | mitigate | Response sets `Cache-Control: private, no-store` so intermediaries do not cache. Endpoint requires auth — only the owning user can fetch their own pack. |
| T-15-22 | Tampering / Filename injection | `safeFilename` | mitigate | Project title stripped to `[A-Za-z0-9- ]` then whitespace → dashes, sliced to 40 chars. Final filename is template-safe; the `"..."` quoting in Content-Disposition cannot be broken out because all unsafe characters are pre-stripped. |
| T-15-23 | Tampering / Credit consumption on failure | RPC rollback | mitigate | Every post-RPC failure path (Zod 400, project 404, renderPack throw, empty buffer, outer 500) calls `decrement_defense_responses`. Mirrored from defend/document routes; verified by Task 2 'decrements credit when renderPack throws' test. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run tests/api/dispute-pack.test.ts` exits 0 with all 9 cases passing
- `grep -c 'check_and_increment_defense_responses' 'app/api/projects/[id]/dispute-pack/route.ts'` returns 1
- `grep -c 'decrement_defense_responses' 'app/api/projects/[id]/dispute-pack/route.ts'` returns at least 4 (one per post-RPC failure path: 400, 404, renderPack-throw or empty-buffer, outer catch)
- `grep -cE 'anthropic|Anthropic' 'app/api/projects/[id]/dispute-pack/route.ts'` returns 0 (D-23 — no LLM)
- Manual smoke (post-merge): `curl -X POST -H 'Content-Type: application/json' -d '{"dispute_type":"not_as_described"}' http://localhost:3000/api/projects/<id>/dispute-pack --cookie '<pro session>' --output pack.pdf && file pack.pdf` reports `PDF document`
</verification>

<success_criteria>
- All 9 Vitest cases pass
- Type-check passes
- Route handles all 4 D-06 dispute types
- Pro gate + RPC quota gate + IDOR guard + Zod validation all enforced in the correct order
- 200 returns a real PDF binary with correct headers
- Every post-RPC failure path decrements the quota
</success_criteria>

<output>
After completion, create `.planning/phases/15-chargeback-evidence-pack/15-04-SUMMARY.md` documenting:
- The route file path
- The exact gate sequence (Auth → Pro → RateLimit → RPC → Validate → Fetch → Render → Respond)
- The filename pattern and the character whitelist used in `safeFilename`
- The 9 test cases shipped
- Pointers for Plan 05: the UI modal POSTs `{ dispute_type, case_reference? }` and downloads the response body via a blob URL; PRO_REQUIRED and UPGRADE_REQUIRED are distinct error codes the client discriminates on
</output>
</content>
</invoke>