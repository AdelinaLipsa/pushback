---
phase: 15-chargeback-evidence-pack
plan: 05
type: execute
wave: 4
depends_on: [15-04]
files_modified:
  - lib/api.ts
  - components/project/DisputePackSection.tsx
  - components/project/DisputePackModal.tsx
  - components/project/ProjectDetailClient.tsx
autonomous: false
requirements:
  - PHASE-15 (ROADMAP success criteria 1 — one click flow; 2 — pack contents visible to user via download)
must_haves:
  truths:
    - "Pro user sees a 'Recovery' card with a 'Compile Dispute Pack' button on /projects/[id] inside the Defend view (D-14)"
    - "Clicking the button opens a shadcn/ui Dialog (same Dialog primitive used by ProjectHeader's delete confirmation) containing a 4-option radio group (D-06), an optional case-reference text input capped at 80 chars, and Generate / Cancel buttons"
    - "Modal shows the copy 'Uses 1 of your monthly responses.' (D-18)"
    - "On Generate, the modal calls POST /api/projects/[id]/dispute-pack and triggers a browser download of the returned PDF via a blob URL (D-15)"
    - "Free user sees the button in a locked state (Lock icon + 'Pro feature — upgrade' label) that triggers startCheckout instead of opening the modal (D-16)"
    - "While generating, the Generate button shows a loading state via the shared Button component; the Cancel button is disabled during generation"
    - "After successful download, the modal closes, URL.revokeObjectURL releases the blob, and a Sonner success toast confirms 'Dispute pack downloaded'"
    - "PRO_REQUIRED (free user) and UPGRADE_REQUIRED (quota exhausted) error codes from Plan 04 are surfaced as distinct inline messages"
  artifacts:
    - path: "lib/api.ts"
      provides: "generateDisputePack(projectId, body) client helper returning a typed DisputePackResult union"
      exports: ["generateDisputePack", "DisputePackResult"]
    - path: "components/project/DisputePackSection.tsx"
      provides: "Recovery card on the project detail page — renders trigger button + Pro/free gate; opens DisputePackModal when Pro"
      exports: ["default"]
    - path: "components/project/DisputePackModal.tsx"
      provides: "shadcn Dialog with radio group + case reference input + Generate flow + blob download + error display"
      exports: ["default"]
    - path: "components/project/ProjectDetailClient.tsx"
      provides: "Defend view now renders DisputePackSection between ClientBehaviorCard and DefenseDashboard"
      contains: "DisputePackSection"
  key_links:
    - from: "DisputePackModal"
      to: "lib/api.ts generateDisputePack"
      via: "client helper call"
      pattern: "generateDisputePack\\("
    - from: "generateDisputePack"
      to: "POST /api/projects/[id]/dispute-pack"
      via: "fetch with JSON body, expects application/pdf response"
      pattern: "/dispute-pack"
    - from: "DisputePackSection"
      to: "ProjectDetailClient (Defend view)"
      via: "imported and rendered between ClientBehaviorCard and the defense-dashboard div"
      pattern: "<DisputePackSection"
    - from: "DisputePackModal + ProjectHeader (delete dialog)"
      to: "@/components/ui/dialog"
      via: "same shadcn Dialog primitive — consistent modal UX across the project"
      pattern: "from '@/components/ui/dialog'"
---

<objective>
Wire the user-facing surface that closes Phase 15. A Recovery card on the project detail page shows a Compile Dispute Pack button. Pro users open a shadcn Dialog (same primitive ProjectHeader already uses for its delete confirmation) to pick a dispute type + optional case reference, generate, and download the PDF. Free users see a locked-state button that routes to Stripe checkout. PRO_REQUIRED and UPGRADE_REQUIRED responses from Plan 04 are surfaced as distinct inline messages.

Purpose: without this, Plan 04's route has no in-app caller. This is the single piece of UI Phase 15 adds — placed inside the existing Defend view, no new pages, no new routes.

Output: 1 modified API client (`lib/api.ts`), 2 new components (`DisputePackSection`, `DisputePackModal`), 1 minimal edit to `ProjectDetailClient` to wire the section. Uses the project's existing UI primitives — shadcn `Dialog`, shared `Button`, Lucide icons, lime accent variable, Sonner toasts. No new dependencies. No new design system entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-chargeback-evidence-pack/15-CONTEXT.md
@.planning/phases/15-chargeback-evidence-pack/15-04-SUMMARY.md

@CLAUDE.md
@AGENTS.md

@lib/api.ts
@lib/ui.ts
@lib/checkout.ts
@components/shared/Button.tsx
@components/shared/UpgradePrompt.tsx
@components/ui/dialog.tsx
@components/project/ProjectHeader.tsx
@components/project/PaymentSection.tsx
@components/project/ProjectDetailClient.tsx
@types/index.ts

<interfaces>
**Modal pattern — use the existing shadcn Dialog, NOT a hand-rolled overlay.** `components/ui/dialog.tsx` wraps `@base-ui/react/dialog` and exports `Dialog`, `DialogContent`, `DialogHeader`, `DialogTitle`, `DialogClose`. The canonical in-project usage is the delete-confirmation dialog inside `components/project/ProjectHeader.tsx` (lines ~205-224): `<Dialog open={...} onOpenChange={...}><DialogContent showCloseButton={false} style={dialogContentStyle}><DialogHeader><DialogTitle>...</DialogTitle></DialogHeader>...</DialogContent></Dialog>`. The `showCloseButton={false}` prop is used when the footer's Cancel button replaces the auto-rendered X — that is the right call for our modal (we have a Cancel button). `dialogContentStyle` from `@/lib/ui` is the shared 440px-max-width elevated-surface container. Do NOT roll a custom `<div role="dialog">` overlay — the per-MEMORY directive ("UI rules: lime, no emojis, Lucide icons, no custom dialog primitives") plus the existing ProjectHeader pattern make shadcn Dialog non-negotiable.

**Buttons — use `@/components/shared/Button`.** `Button` accepts `variant: 'primary' | 'outline' | 'ghost' | 'destructive'`, `size: 'sm' | 'md' | 'lg'`, `loading?: boolean` (auto-disables and renders a Spinner), and `icon?: ReactNode`. ProjectHeader uses it for Edit/Delete/Save/Cancel. Use it here for Generate (primary + loading) and Cancel (ghost). Do NOT pass raw `style={btnStyles.primary}` to a native `<button>` — that legacy idiom is only kept for components not yet migrated. New components use `Button`.

**Lime accent + Lucide icons + no emojis.** Per the project MEMORY directive: brand accent is `var(--brand-lime)` only (never amber). All iconography is `lucide-react`. Zero emoji characters anywhere in the strings (the existing `coverLetterTemplates`, ProjectHeader, PaymentSection all comply — match their tone). The Recovery card uses Lucide's `ShieldAlert` for the eyebrow, the locked free-user button uses `Lock`.

**From `@/lib/api.ts` (current — `generateDocument` is the closest sibling pattern, lines ~187-205):** the function POSTs JSON, awaits `res.json()`, discriminates `PRO_REQUIRED` into `{ upgradeRequired: true }`, toasts other errors via Sonner, returns `null` on network failure. `generateDisputePack` follows the same shape BUT the success response is `application/pdf` binary, not JSON — so the new helper must read the response body as `Blob` when `content-type` includes `application/pdf`, and only attempt `res.json()` on error responses.

**From `@/lib/checkout.ts`:** `startCheckout(setLoading: (v: boolean) => void): Promise<void>` — same helper UpgradePrompt uses for the upgrade CTA. The free-user button in `DisputePackSection` wires to this, NOT to a `<Link href="/billing">` redirect.

**From `@/lib/ui.ts`:** `inputStyle`, `labelStyle`, `dialogContentStyle` are the inline-style objects used by ProjectHeader's edit form + delete dialog. Use them for the modal's text input + radio fieldset labels + DialogContent style. Do NOT introduce new Tailwind classes for these — the project mixes Tailwind utility classes with inline styles (see ProjectHeader); stay consistent.

**From `@/components/project/ProjectDetailClient.tsx` (current, lines ~107-156 in the Defend view):** the Defend view renders `<PaymentSection />` → divider → optional escalation nudge → optional `<ClientBehaviorCard />` → `<div id="defense-dashboard"><DefenseDashboard ... /></div>`. The new `<DisputePackSection />` slots between ClientBehaviorCard and the `defense-dashboard` div. The Contract and History views stay untouched.

**From `@/types`:** `Plan = 'free' | 'pro'`; `Project` has `id`, `client_name`, `title`. The Section component needs only the project shape and the plan; the Modal needs `project.id` for the API call.

**Blob download mechanism.** Construct an off-DOM `<a>` element with `href = URL.createObjectURL(blob)` and `download = filename`, append to `document.body`, click, remove, then defer `URL.revokeObjectURL(url)` by ~1500ms to give older browsers time to commit the download. This is the same pattern Stripe/CSV-export examples use and is safe to do client-only inside the modal.

**Toast UX.** Use `import { toast } from 'sonner'` (already a project dep). Success → `toast.success('Dispute pack downloaded')`. Network/unknown errors → `toast.error(...)` from inside `generateDisputePack`. PRO_REQUIRED and UPGRADE_REQUIRED do NOT toast — they become inline errors in the modal so the user has context for the resolution path (upgrade vs wait for reset).

**No DB writes from the UI.** Per CONTEXT D-17 — the only persistent record of pack generation is the shared `defense_responses` quota counter incremented by Plan 04's RPC. There is no "Recent packs" list in v1. Do NOT add `router.refresh()` after a successful download — nothing on the page needs to re-fetch.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add generateDisputePack + DisputePackResult to lib/api.ts</name>
  <files>lib/api.ts</files>
  <read_first>
    - lib/api.ts (full file — `generateDocument` block at lines ~181-205 is the structural sibling; note the existing `toast` import at line 1 and the `PRO_REQUIRED` discrimination pattern at line 198)
    - .planning/phases/15-chargeback-evidence-pack/15-CONTEXT.md (D-15 client triggers blob download; D-16 free-user 403 PRO_REQUIRED; D-18 quota messaging)
    - .planning/phases/15-chargeback-evidence-pack/15-04-SUMMARY.md (the exact server error strings the client discriminates on)
  </read_first>
  <action>
Append a new `// ─── Dispute Pack ───` section to `lib/api.ts`. Place it AFTER the existing `// ─── Documents ───` block and BEFORE `// ─── Responses (fire-and-forget) ───` so related client helpers stay grouped.

Export a TypeScript discriminated union `DisputePackResult` with four arms: `{ upgradeRequired: true }` (free-plan user hit a Pro-only endpoint), `{ quotaExceeded: true }` (Pro user with no monthly budget left), `{ blob: Blob; filename: string; upgradeRequired?: false }` (success), and `null` (network error / unknown 5xx — `generateDisputePack` already toasted on this path). Including `upgradeRequired?: false` on the success arm is a deliberate ergonomic — callers can do `if ('blob' in result)` without TypeScript complaining about the optional discriminator.

Export `async function generateDisputePack(projectId: string, body: { dispute_type: 'not_as_described' | 'not_received' | 'cancelled' | 'unauthorized'; case_reference?: string }): Promise<DisputePackResult>`. The four dispute-type literals match the D-06 set exactly; do NOT widen to `string` and do NOT import `DisputeType` from `@/types` here (keeps the client helper independent of the dispute lib internals; the literal union is the wire contract).

Implementation: wrap the body in `try { ... } catch { toast.error('Network error — check your connection.'); return null }`. Inside the try:

1. `const res = await fetch(\`/api/projects/${projectId}/dispute-pack\`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(body) })`.
2. Read `const contentType = res.headers.get('content-type') ?? ''`.
3. Happy path: `if (res.ok && contentType.includes('application/pdf')) { ... }`. Inside: `const blob = await res.blob()`. Parse the filename from the Content-Disposition header: `const cd = res.headers.get('content-disposition') ?? ''`; `const m = /filename="([^"]+)"/.exec(cd)`; `const filename = m?.[1] ?? \`dispute-pack-${body.dispute_type}.pdf\``. Return `{ blob, filename }`.
4. Error path: attempt `let data: { error?: string } = {}; try { data = await res.json() } catch { /* server returned non-JSON */ }`.
5. Discriminate the two known 403 error codes by string match. `if (res.status === 403 && data.error === 'PRO_REQUIRED') return { upgradeRequired: true }`. `if (res.status === 403 && data.error === 'UPGRADE_REQUIRED') return { quotaExceeded: true }`. Both strings come from `app/api/projects/[id]/dispute-pack/route.ts` — keep them in sync; if Plan 04's SUMMARY says they changed, update accordingly before continuing.
6. Everything else: `toast.error(data?.error ?? 'Pack generation failed — please try again.'); return null`. This catches 400 validation errors, 404 IDOR misses, 429 rate limits, 500s — all of which are user-actionable via the toast.

The helper does NOT trigger the download — the caller (DisputePackModal) constructs the blob URL and clicks the anchor. This keeps `lib/api.ts` DOM-free and matches how `generateDocument` returns text for the caller to render.

Do NOT add a JSDoc block — none of the other helpers in this file have one; stay consistent.
  </action>
  <verify>
    <automated>grep -q "export async function generateDisputePack" lib/api.ts && grep -q "DisputePackResult" lib/api.ts && grep -q "application/pdf" lib/api.ts && grep -q "PRO_REQUIRED" lib/api.ts && grep -q "UPGRADE_REQUIRED" lib/api.ts && grep -q "case_reference" lib/api.ts && grep -q "quotaExceeded" lib/api.ts && npx tsc --noEmit</automated>
  </verify>
  <done>
    `generateDisputePack` and `DisputePackResult` exported from `lib/api.ts`. PRO_REQUIRED maps to `upgradeRequired: true`, UPGRADE_REQUIRED maps to `quotaExceeded: true`, success returns `{ blob, filename }`, other errors toast and return null. Type-check passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build DisputePackSection (Recovery card with Pro/free gate)</name>
  <files>components/project/DisputePackSection.tsx</files>
  <read_first>
    - components/project/PaymentSection.tsx (full file — the inline-style `borderLeft: '3px solid var(--brand-lime)'` accent card pattern + flex layout with a trigger button on the right)
    - components/shared/UpgradePrompt.tsx (the `startCheckout(setLoading)` pattern used here for the free-user CTA)
    - components/shared/Button.tsx (variants, size, loading, icon props)
    - lib/checkout.ts (startCheckout signature)
    - types/index.ts (Plan, Project)
    - .planning/phases/15-chargeback-evidence-pack/15-CONTEXT.md (D-14 'Recovery' section card lives on the project detail page; D-16 free user sees button greyed with UpgradePrompt pattern)
  </read_first>
  <action>
Create `components/project/DisputePackSection.tsx`. First line `'use client'` (the component has interaction state). Import `useState` from `react`, `ShieldAlert` and `Lock` from `lucide-react`, `startCheckout` from `@/lib/checkout`, `Button` from `@/components/shared/Button`, the `Project` and `Plan` types from `@/types`, and `DisputePackModal` from `./DisputePackModal` (built in Task 3 — file may not exist yet when the executor types this; that is fine).

Define `interface DisputePackSectionProps { project: Project; plan: Plan }`.

Default-export the component. State: `const [open, setOpen] = useState(false)` for the modal; `const [upgradeLoading, setUpgradeLoading] = useState(false)` for the free-user Stripe redirect. Derived: `const isPro = plan === 'pro'`. Handler: `async function handleUpgrade() { await startCheckout(setUpgradeLoading) }` — mirrors `UpgradePrompt.tsx`.

Render a `<>...</>` fragment containing:

1. **The Recovery card** — an outer `<div>` styled inline matching PaymentSection's idiom: `marginBottom: '1.5rem'`, `padding: '1rem 1.25rem'`, `backgroundColor: 'var(--bg-surface)'`, `border: '1px solid var(--bg-border)'`, `borderLeft: '3px solid var(--brand-lime)'`, `borderRadius: '0.5rem'`. Add a `className="fade-up"` for the existing fade-in animation used across project components (PaymentSection, ClientBehaviorCard).

2. Inside the card, a flex row with `display: 'flex', alignItems: 'flex-start', justifyContent: 'space-between', gap: '1rem', flexWrap: 'wrap'`. Left side: a `<div>` containing (a) an eyebrow row with `<ShieldAlert size={14} style={{ color: 'var(--text-secondary)' }} />` followed by a `<span>` with `fontSize: '0.7rem', fontWeight: 600, textTransform: 'uppercase', letterSpacing: '0.1em', color: 'var(--text-muted)'` rendering the literal text `Recovery`; (b) a title `<p>` (`fontSize: '0.95rem', fontWeight: 600, margin: '0 0 0.25rem 0'`) reading `Compile a chargeback evidence pack`; (c) a description `<p>` (`fontSize: '0.8rem', color: 'var(--text-secondary)', margin: 0, maxWidth: '52ch'`) reading `Generate a Stripe/PayPal-ready PDF rebuttal with contract excerpt, delivery timeline, ranked communication log, and sign-off proofs.`

3. Right side (the trigger). Branch on `isPro`:
   - Pro: render `<Button variant="outline" size="sm" onClick={() => setOpen(true)} aria-label="Compile dispute pack">Compile Dispute Pack</Button>`. No icon (matches the visual weight of PaymentSection's outline buttons).
   - Free: render `<Button variant="outline" size="sm" loading={upgradeLoading} onClick={handleUpgrade} icon={<Lock size={12} />} aria-label="Pro feature — upgrade to unlock dispute packs">Pro feature — upgrade</Button>`. The `loading` prop swaps the icon for the Spinner and disables the button while Stripe redirects. Do NOT use a tooltip or a "lock card" overlay — the lock icon + label are sufficient signal (matches the project's lightweight gating idiom; the full UpgradePrompt-card pattern is reserved for "Pro user out of monthly budget" surfaces, not for "free user sees a Pro feature").

4. **The modal** — after the card, conditionally render `{isPro && open ? <DisputePackModal project={project} onClose={() => setOpen(false)} /> : null}`. Mount-only-when-open so the Dialog's internal state resets between sessions (DialogContent's animation expects the component to mount on open).

Use the lime CSS variable (`var(--brand-lime)`) for the left-border accent — not a hex literal. Use zero emoji characters anywhere in the JSX or strings. Use Lucide icons exclusively (ShieldAlert, Lock) — no SVG inlines, no custom icon files.
  </action>
  <verify>
    <automated>test -f components/project/DisputePackSection.tsx && grep -q "Recovery" components/project/DisputePackSection.tsx && grep -q "Compile Dispute Pack" components/project/DisputePackSection.tsx && grep -q "Pro feature" components/project/DisputePackSection.tsx && grep -q "from '@/components/shared/Button'" components/project/DisputePackSection.tsx && grep -q "from 'lucide-react'" components/project/DisputePackSection.tsx && grep -q "startCheckout" components/project/DisputePackSection.tsx && grep -q "var(--brand-lime)" components/project/DisputePackSection.tsx && [ "$(grep -cE "amber|#fbbf24|#f59e0b" components/project/DisputePackSection.tsx)" = "0" ] && npx tsc --noEmit</automated>
  </verify>
  <done>
    DisputePackSection renders the Recovery card with the lime left-border accent. Pro path opens the modal; free path triggers `startCheckout` and shows a locked-style button. Uses shared Button + Lucide icons + lime accent variable; zero amber/hex colors; zero emojis. Type-check passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Build DisputePackModal (shadcn Dialog + radio group + case ref + blob download)</name>
  <files>components/project/DisputePackModal.tsx</files>
  <read_first>
    - components/project/ProjectHeader.tsx (lines 205-224 — the canonical in-project shadcn Dialog pattern: `<Dialog open onOpenChange><DialogContent showCloseButton={false} style={dialogContentStyle}><DialogHeader><DialogTitle ...>...</DialogTitle></DialogHeader>...buttons</DialogContent></Dialog>`)
    - components/ui/dialog.tsx (the Dialog primitive exports — note Title, Header are the named pieces; the `showCloseButton={false}` knob hides the auto-rendered X)
    - lib/api.ts (Task 1 — generateDisputePack signature and the DisputePackResult discriminated union)
    - lib/ui.ts (inputStyle, labelStyle, dialogContentStyle)
    - components/shared/Button.tsx (variants, loading, icon props)
    - types/index.ts (Project)
    - .planning/phases/15-chargeback-evidence-pack/15-CONTEXT.md (D-06 4 dispute types — exact labels/descriptions; D-07 small radio-group modal; D-15 client triggers blob download; D-18 'Uses 1 of your monthly responses.' copy)
  </read_first>
  <action>
Create `components/project/DisputePackModal.tsx`. First line `'use client'`. Imports: `useState` from `react`; `toast` from `sonner`; `Dialog`, `DialogContent`, `DialogHeader`, `DialogTitle` from `@/components/ui/dialog`; `Button` from `@/components/shared/Button`; `Download`, `X` from `lucide-react`; `inputStyle`, `labelStyle`, `dialogContentStyle` from `@/lib/ui`; `generateDisputePack` from `@/lib/api`; `Project` from `@/types`.

Define `interface DisputePackModalProps { project: Project; onClose: () => void }`. Define `type DisputeType = 'not_as_described' | 'not_received' | 'cancelled' | 'unauthorized'` locally (matches the wire contract; no need to import the lib-level type here).

Define a module-scope constant `DISPUTE_OPTIONS: { value: DisputeType; label: string; description: string }[]` with 4 entries in this exact order:
- `not_as_described` / `Not as described` / `Client claims the work did not match the agreed scope or deliverables.`
- `not_received` / `Not received` / `Client claims they never received the deliverables paid for.`
- `cancelled` / `Cancelled` / `Client claims the engagement was cancelled before payment was due.`
- `unauthorized` / `Unauthorized` / `Client claims they did not authorize the charge.`

(The descriptions are brief and neutral — they help the user pick correctly without legal jargon.)

Define a private `triggerBlobDownload(blob: Blob, filename: string): void` helper inside the module (not exported). Implementation: create `const url = URL.createObjectURL(blob)`; create `const a = document.createElement('a')`; set `a.href = url`, `a.download = filename`; `document.body.appendChild(a)`; `a.click()`; `document.body.removeChild(a)`; `setTimeout(() => URL.revokeObjectURL(url), 1500)` (defer revocation so Safari and older browsers commit the download before the URL is invalidated). This DOM-touching helper is intentionally kept in the modal — moving it to `lib/api.ts` would force the API helper to touch `document`, defeating that file's DOM-free design.

Default-export the component. State: `const [disputeType, setDisputeType] = useState<DisputeType>('not_as_described')`; `const [caseReference, setCaseReference] = useState('')`; `const [generating, setGenerating] = useState(false)`; `const [error, setError] = useState<string | null>(null)`.

Handler `async function handleGenerate()`:
1. `setGenerating(true); setError(null)`.
2. Call `const result = await generateDisputePack(project.id, { dispute_type: disputeType, case_reference: caseReference.trim() || undefined })`. The `|| undefined` is intentional — empty string would fail the optional Zod check.
3. `setGenerating(false)`.
4. Discriminate the result:
   - `if (!result) { setError('Pack generation failed — please try again.'); return }` (network/5xx — `generateDisputePack` already toasted; this is the inline echo).
   - `if ('upgradeRequired' in result && result.upgradeRequired) { setError('Pro account required.'); return }` (shouldn't normally happen since the section gates by plan — but if a free user somehow opened the modal via a stale prop, this is the graceful fallback).
   - `if ('quotaExceeded' in result && result.quotaExceeded) { setError("You've used this month's response budget. It resets at the start of your next billing period."); return }` (per D-18 messaging).
   - `if ('blob' in result) { triggerBlobDownload(result.blob, result.filename); toast.success('Dispute pack downloaded'); onClose() }`.

Render structure (mirror ProjectHeader's delete-dialog pattern):

`<Dialog open={true} onOpenChange={(o) => { if (!o && !generating) onClose() }}>` — pass `open={true}` because the component only mounts when DisputePackSection sets `open === true`. The `onOpenChange` guard prevents closing mid-generation via backdrop click or Escape.

Inside, `<DialogContent showCloseButton={false} style={{ ...dialogContentStyle, maxWidth: '34rem' }}>` — `showCloseButton={false}` because the footer's Cancel button is the close affordance (same idiom as ProjectHeader). The wider `34rem` override accommodates the 4-row radio group; the base `dialogContentStyle` is 440px.

Inside DialogContent:
- `<DialogHeader>` containing `<DialogTitle style={{ fontSize: '1.05rem', fontWeight: 700, color: 'var(--text-primary)', marginBottom: '0.25rem' }}>Compile dispute pack</DialogTitle>`.
- A `<p style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', marginBottom: '0.25rem' }}>` containing exactly: `We will assemble a PDF from this project's contract, timeline, and communication log. Uses 1 of your monthly responses.` (the trailing quota sentence is the D-18 copy verbatim).
- A `<fieldset style={{ border: 0, padding: 0, margin: '1.25rem 0 0.5rem 0' }}>` with `<legend style={{ ...labelStyle, marginBottom: '0.5rem' }}>Dispute type</legend>`. Map `DISPUTE_OPTIONS` to `<label key={opt.value} style={{ display: 'flex', gap: '0.625rem', alignItems: 'flex-start', padding: '0.5rem 0', cursor: 'pointer' }}>` containing an `<input type="radio" name="dispute_type" value={opt.value} checked={disputeType === opt.value} onChange={() => setDisputeType(opt.value)} style={{ marginTop: '0.25rem' }} />` then a `<span>` wrapping a bold label `<span style={{ fontSize: '0.875rem', fontWeight: 600, display: 'block' }}>{opt.label}</span>` and a muted description `<span style={{ fontSize: '0.8rem', color: 'var(--text-secondary)' }}>{opt.description}</span>`.
- A `<div style={{ marginTop: '1rem' }}>` containing `<label htmlFor="case_reference" style={labelStyle}>Case reference (optional)</label>` and `<input id="case_reference" type="text" maxLength={80} value={caseReference} onChange={(e) => setCaseReference(e.target.value)} placeholder="e.g. STRIPE-DSP-2026-04-001" style={inputStyle} onFocus={e => { e.currentTarget.style.borderColor = 'var(--brand-lime)' }} onBlur={e => { e.currentTarget.style.borderColor = 'var(--bg-border)' }} />`. The focus/blur lime-border behavior matches ProjectHeader's edit inputs. `maxLength={80}` matches both the Zod cap in Plan 04 and the `CASE_REF_MAX` in `assemble.ts`.
- Conditional error display: `{error ? <p role="alert" style={{ marginTop: '0.75rem', color: 'var(--urgency-high)', fontSize: '0.85rem' }}>{error}</p> : null}`.
- Footer button row: `<div className="flex gap-2 justify-end" style={{ marginTop: '1.5rem' }}>` containing `<Button variant="ghost" icon={<X size={13} />} onClick={onClose} disabled={generating}>Cancel</Button>` and `<Button variant="primary" icon={<Download size={14} />} loading={generating} onClick={handleGenerate}>{generating ? 'Generating…' : 'Generate pack'}</Button>`. The `loading={generating}` on the primary Button auto-swaps icon for Spinner and disables the button (`Button.tsx` handles this in the `disabled || loading` line).

Do NOT add a separate close-on-Escape effect — `@base-ui/react/dialog` (which shadcn's Dialog wraps) handles Escape natively. Do NOT trap focus manually — same reason.

Zero emojis. Zero amber/hex literals (use `var(--urgency-high)`, `var(--brand-lime)`, `var(--text-secondary)`, `var(--text-primary)`, `var(--text-muted)`, `var(--bg-border)`). Lucide icons only (`X`, `Download`).
  </action>
  <verify>
    <automated>test -f components/project/DisputePackModal.tsx && grep -q "generateDisputePack" components/project/DisputePackModal.tsx && grep -q "from '@/components/ui/dialog'" components/project/DisputePackModal.tsx && grep -q "DialogContent" components/project/DisputePackModal.tsx && grep -q "not_as_described" components/project/DisputePackModal.tsx && grep -q "not_received" components/project/DisputePackModal.tsx && grep -q "cancelled" components/project/DisputePackModal.tsx && grep -q "unauthorized" components/project/DisputePackModal.tsx && grep -q "Uses 1 of your monthly responses" components/project/DisputePackModal.tsx && grep -q "URL.createObjectURL" components/project/DisputePackModal.tsx && grep -q "URL.revokeObjectURL" components/project/DisputePackModal.tsx && grep -q "maxLength={80}" components/project/DisputePackModal.tsx && [ "$(grep -cE "amber|#fbbf24|#f59e0b" components/project/DisputePackModal.tsx)" = "0" ] && npx tsc --noEmit</automated>
  </verify>
  <done>
    DisputePackModal opens via the shadcn Dialog primitive (same one ProjectHeader uses for deletion). Contains the 4-option radio group with descriptions, the 80-char-capped case reference input, the Generate button with loading state, an inline error region for PRO_REQUIRED / UPGRADE_REQUIRED / generic failures, and a blob-download flow with deferred URL revocation. Success toasts via Sonner and closes the modal. Type-check passes; zero amber/hex/emoji violations.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Wire DisputePackSection into ProjectDetailClient (Defend view)</name>
  <files>components/project/ProjectDetailClient.tsx</files>
  <read_first>
    - components/project/ProjectDetailClient.tsx (full file — locate the Defend view block at lines ~107-156; the new section goes AFTER the `<ClientBehaviorCard ... />` render around line 143 and BEFORE the `<div id="defense-dashboard">` wrapper around line 146)
    - components/project/DisputePackSection.tsx (Task 2 — confirm the props are `{ project, plan }`)
  </read_first>
  <action>
Two edits to `components/project/ProjectDetailClient.tsx`. Both are surgical — do NOT touch anything outside these two regions.

**Edit 1: add the import.** Place `import DisputePackSection from '@/components/project/DisputePackSection'` next to the existing component imports (right after the `ClientBehaviorCard` import on line 10). Keep import ordering consistent with the file's current alphabetic-ish grouping (the file currently goes ProjectHeader → PaymentSection → DefenseDashboard → ClientBehaviorCard → ResponseHistory; place the new import after ClientBehaviorCard for proximity to the section it slots near).

**Edit 2: render the section in the Defend view.** Find the block in the `view === 'defend'` conditional that currently reads (around lines 142-156):

`{risk.composite > 0 && (<ClientBehaviorCard risk={risk} clientName={project.client_name} />)} <div id="defense-dashboard"> <DefenseDashboard ... /> </div>`

Insert a single new line immediately AFTER the closing `)}` of the ClientBehaviorCard conditional and BEFORE the `<div id="defense-dashboard">` opening tag: `<DisputePackSection project={project} plan={plan} />`.

No other markup changes. No new wrapper divs. The Contract view (`view === 'contract'`) and History view (`view === 'history'`) stay untouched. Do NOT modify any other prop being passed to existing components. Do NOT add conditional gating around DisputePackSection at this layer — the section component handles its own Pro/free branching internally.

After the edit, the file should grep to exactly 2 occurrences of `DisputePackSection` (one import, one JSX usage).
  </action>
  <verify>
    <automated>[ "$(grep -c "DisputePackSection" components/project/ProjectDetailClient.tsx)" = "2" ] && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>
    ProjectDetailClient imports `DisputePackSection` once and renders it once within the Defend view, between `ClientBehaviorCard` and the `defense-dashboard` div. No other markup changed. `npm run build` succeeds.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Manual end-to-end verification of the dispute pack flow</name>
  <what-built>
    Plans 01-05 combined: deterministic ranking + clause extraction + assemble (Plan 01), React-PDF shared templates + 7 page composition + golden tests (Plans 02-03), API route + auth/Pro/quota/IDOR stack + 9 test cases (Plan 04), UI Recovery card + modal + blob download (Plan 05). End-to-end: Pro user clicks Compile Dispute Pack → picks dispute type → generates → downloads PDF.
  </what-built>
  <how-to-verify>
    Run `npm run dev` and verify each scenario:

    **A) Pro user happy path — all 4 dispute types**
    1. Sign in as a Pro user. Open a project with at least 1 defense_response on file.
    2. In the Defend view, scroll past the payment + escalation + client-behavior cards to find the **Recovery** card with the lime left-border. Confirm it shows the Compile Dispute Pack outline button on the right and the description copy on the left.
    3. Click the button — the shadcn Dialog opens with the title "Compile dispute pack", the quota note, 4 radio options (Not as described / Not received / Cancelled / Unauthorized) each with a brief description, the Case reference text input, and the Cancel + Generate pack buttons.
    4. Leave "Not as described" selected, leave case reference blank, click **Generate pack**. Within 10 seconds (D-21) the modal closes, a Sonner success toast reads **Dispute pack downloaded**, and a PDF lands in Downloads named `dispute-pack-not_as_described-<title-slug>-<YYYY-MM-DD>.pdf`.
    5. Open the PDF. Confirm 7 pages in this exact order: Cover Letter (with project + transaction summary + statement of position), Contract Excerpt (with highlighted clauses OR the D-11 placeholder), Delivery Timeline, Communication Log, Sign-Off Proofs, Payment Record, Summary Statement. Footer on every page reads `Pushback Dispute Pack · <client> · <user>`.
    6. Repeat with **Not received**, **Cancelled**, **Unauthorized** — each downloads a different PDF with the dispute-type-specific cover-letter paragraph and 4-paragraph summary statement.

    **B) Case reference field**
    1. Open the modal. Type `STRIPE-DSP-2026-05-001` in the Case reference field. Generate.
    2. Open the PDF. Confirm the case reference appears on the cover letter under "Case Reference" and again in the Summary Statement footer ("Ref STRIPE-DSP-2026-05-001").
    3. Re-open the modal. Paste 85 characters into the case reference field — the input caps at 80 characters (browser-enforced via `maxLength={80}`). Generate works.

    **C) Free user gating**
    1. Sign in as a free user. Open a project.
    2. The Recovery card shows an outline button with a Lock icon labeled **Pro feature — upgrade**.
    3. Click it. The button enters a loading state (Spinner replaces the lock) and the browser navigates to the Stripe checkout (same destination as the UpgradePrompt CTA).
    4. As a free user, also try `curl -X POST -H 'Content-Type: application/json' -d '{"dispute_type":"not_as_described"}' http://localhost:3000/api/projects/<your project id>/dispute-pack --cookie '<free session cookie>'`. Server returns `{"error":"PRO_REQUIRED"}` with status 403 — the UI is not the only enforcement.

    **D) Pro user quota exhausted**
    1. As a Pro user, exhaust the monthly `defense_responses` quota (use admin SQL to set `period_responses_used = period_responses_limit`, or mock the RPC during local testing).
    2. Click Compile Dispute Pack, pick a type, Generate.
    3. The modal shows an inline red error: "You've used this month's response budget. It resets at the start of your next billing period." The PDF does NOT download. Cancel closes cleanly.

    **E) Empty / sparse projects**
    1. Create a fresh project with no contract and no defense_responses. Save.
    2. Open it, click Compile Dispute Pack, pick **Not as described**, Generate.
    3. The PDF still downloads (does not error). Contract Excerpt page shows the verbatim D-11 placeholder `Contract not analyzed — upload via Pushback first to include clause excerpts.` Communication Log shows `No defense responses on file for this project.` Payment Record shows the sparse-data placeholder.

    **F) Network failure / 500**
    1. Pro user, open the modal, click Generate, then immediately switch DevTools to Offline before the request resolves.
    2. The modal shows the inline error "Pack generation failed — please try again." A Sonner toast also reads "Network error — check your connection." The button returns to its non-loading state. Cancel closes the modal cleanly.

    **G) Accessibility**
    1. Tab through the modal — focus moves through the radio group, the case reference input, the Cancel button, and the Generate pack button.
    2. Press Escape — confirm the modal closes (handled natively by `@base-ui/react/dialog`).
    3. Try to backdrop-click while generation is in flight — confirm the modal does NOT close (the `onOpenChange` guard blocks it). After generation completes, backdrop click closes normally.
    4. The Recovery card button has the correct aria-label ("Compile dispute pack" for Pro; "Pro feature — upgrade to unlock dispute packs" for free). The error region uses `role="alert"` so screen readers announce it.

    **H) PDF visual sanity**
    1. Open one PDF and confirm: Helvetica throughout (no fancy serif), A4 page size, clauses on the Contract Excerpt page render inside the highlighted yellow blocks, week headers on the Communication Log appear in the `YYYY-Www` format, footer on every page reads `Pushback Dispute Pack · <client> · <user>`, no obvious layout overflow or clipped text.
  </how-to-verify>
  <resume-signal>
    Type **"approved"** if all scenarios A-H behave as described. If a scenario fails, paste the scenario letter and observed behavior so the executor can patch.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /api/projects/[id]/dispute-pack | Already enumerated in Plan 04 |
| Modal state → generateDisputePack call | Plain JSON body, both fields client-validated as a UX measure (server is the authoritative gate) |
| Server response → Blob → client download | `URL.createObjectURL` produces a same-origin `blob:` URL that only the current page can access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-24 | Spoofing (free user UX bypass) | DisputePackSection | mitigate | The locked-state button only triggers `startCheckout`; the actual server gate (Plan 04 PRO_REQUIRED) is authoritative — a free user calling the API directly still gets 403. Verified in scenario C. |
| T-15-25 | Tampering (case reference injection) | DisputePackModal | mitigate | The case reference is sent as a string in JSON body and re-rendered into the PDF via React-PDF text nodes (no HTML interpretation). Plan 01's `sanitizeCaseRef` strips non-printable bytes server-side. Plan 05's `maxLength={80}` is a UX hint; the Zod `.max(80)` in Plan 04 is the enforcement. |
| T-15-26 | Information Disclosure (PDF cached by browser) | generateDisputePack response handler | mitigate | Plan 04 sets `Cache-Control: private, no-store`. Browser stores the blob in memory only; `URL.revokeObjectURL` releases it after ~1.5s. |
| T-15-27 | Information Disclosure (blob URL leaks across tabs) | triggerBlobDownload | accept | Browsers expose `blob:` URLs only within the originating tab; the URL revokes after 1.5s. Acceptable v1 risk; the user already trusts the PDF to be in their Downloads folder. |
| T-15-28 | DoS (user spams Generate) | DisputePackModal | mitigate | The Button's `loading={generating}` auto-disables the button while the request is in flight. Plan 04's rate limit + RPC quota further bound the rate. The `onOpenChange` guard also blocks Cancel + backdrop-close while generating, preventing orphan downloads. |
| T-15-29 | Repudiation (no UI record of generation) | DisputePackModal post-success | accept | No DB write per D-17. The Sonner toast is ephemeral. v2 may add a "Recent packs" list if the audit-table decision is revisited. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npm run build` exits 0
- All 8 scenarios in Task 5 (A-H) pass manual verification
- `grep -c "DisputePackSection" components/project/ProjectDetailClient.tsx` returns 2 (import + render)
- `grep -c "DisputePackSection" components/defense/` returns 0 (lives in `components/project/` only)
- `grep -c "from '@/components/ui/dialog'" components/project/DisputePackModal.tsx` returns 1 (uses the shadcn primitive, not a hand-rolled overlay)
- `grep -cE "amber|#fbbf24|#f59e0b" components/project/DisputePackSection.tsx components/project/DisputePackModal.tsx` returns 0 (lime-only per MEMORY)
</verification>

<success_criteria>
- `generateDisputePack` returns a typed result discriminating success / upgradeRequired / quotaExceeded / null
- DisputePackSection renders both Pro (modal trigger) and free (Stripe checkout) states with the same lime-accented Recovery card
- DisputePackModal uses the shadcn Dialog primitive (same one ProjectHeader uses), contains all 4 dispute-type radio options with descriptions, an 80-char-capped case reference input, a Generate button with loading state, inline error display, and a working blob-download flow with deferred URL revocation
- ProjectDetailClient wires DisputePackSection into the Defend view between ClientBehaviorCard and DefenseDashboard with a single import + a single JSX line
- Manual checkpoint scenarios A-H all pass
- Full app type-checks and builds
</success_criteria>

<output>
After all tasks complete, create `.planning/phases/15-chargeback-evidence-pack/15-05-SUMMARY.md` documenting:
- `generateDisputePack` return-type discrimination (PRO_REQUIRED vs UPGRADE_REQUIRED vs success vs network)
- Use of the shadcn Dialog primitive + `showCloseButton={false}` + `dialogContentStyle` (consistent with ProjectHeader's delete-confirmation pattern)
- The blob download mechanism with `setTimeout(() => URL.revokeObjectURL, 1500)`
- Recovery card placement decision (between ClientBehaviorCard and the defense-dashboard div in the Defend view only)
- Modal accessibility attributes (role="alert" on errors, native Escape handling, `onOpenChange` guard during generation)
- Smoke-test outcomes per scenario A-H
- Any v2 follow-ups (e.g. "Recent packs" list once D-17 is revisited; chargeback-score auto-trigger per Phase 14 wiring)
</output>
</content>
