---
phase: 16-editorial-landing-page-redesign
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - components/hero/PushbackHero.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-01: PushbackHero.tsx contains no WebGL fragment shader source, no WebGL context creation, no requestAnimationFrame loop driving a shader, and no canvas element backed by gl.drawArrays — the fragmentShader / vertexShader template-literal constants are physically deleted from the file"
    - "D-02: The hero background is a two-layer composition: (a) two static CSS radial gradients (lime warm-dark) painted as a single absolutely-positioned <div aria-hidden> covering inset:0, (b) the inline editorial artifact rendered by importing HeroArtifactSVG from Plan 16-01"
    - "D-05: The four-word pillar line (VET. / SIGN. / REPLY. / RECOVER.) renders in Instrument Serif italic via fontFamily: 'var(--font-serif)' + fontStyle: 'italic'; the existing GSAP per-line hover-glow handler is preserved, only the typeface declarations change"
    - "D-17: Each pillar word has a focus-visible state that produces the same lime-glow effect as hover, so keyboard navigation reaches the headline; the focus indicator never fully disappears"
    - "D-14: The hero ships zero requestAnimationFrame loops tied to shader rendering and zero WebGL context allocations; remaining GSAP usage is hover/entrance-only and does not run on a continuous frame loop"
    - "The hero retains all existing copy verbatim: 'pushback.' wordmark, the operating-system tagline trio, 'Four tools. One difficult client.', 'Every move, prepared.', the four-sentence Vet/Sign/Reply/Recover paragraph, the two CTA buttons with current labels, and the 'Free to start. No card required.' line"
    - "The HeroArtifactSVG renders only on md+ viewports (the desktop hero); on mobile the gradient layer stands alone (the hero remains visually intact and uncluttered on narrow screens)"
  artifacts:
    - path: "components/hero/PushbackHero.tsx"
      provides: "Editorial hero with static gradient + inline contract-page artifact + serif pillar headline"
      contains: "HeroArtifactSVG"
  key_links:
    - from: "components/hero/PushbackHero.tsx"
      to: "components/hero/HeroArtifactSVG.tsx"
      via: "default import 'HeroArtifactSVG from @/components/hero/HeroArtifactSVG'"
      pattern: "from '@/components/hero/HeroArtifactSVG'"
    - from: "SituationLine pillar word"
      to: "Instrument Serif via --font-serif"
      via: "inline style fontFamily: 'var(--font-serif)' + fontStyle: 'italic'"
      pattern: "var\\(--font-serif\\)"
---

<objective>
Rewrite `components/hero/PushbackHero.tsx` to the editorial visual grammar: delete the WebGL shader path entirely, replace it with a two-layer background (static gradient + inline HeroArtifactSVG from Plan 16-01), retype the four-word pillar line in Instrument Serif italic, and keep every line of copy + the GSAP hover-glow intact.

Purpose: This is the central visual move of Phase 16. The shader was the "modern SaaS template" signal; one real artifact + one serif headline is the editorial replacement. After this plan, the hero already reads correctly even though the 4-pillar grid below it still uses the old card grammar (Plan 03 cleans that up).

Output: One modified file. The shader source and the shader-eligible gating both disappear; the artifact appears on desktop; the headline renders in serif italic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-editorial-landing-page-redesign/16-CONTEXT.md
@.planning/phases/16-editorial-landing-page-redesign/16-01-PLAN.md
@CLAUDE.md
@AGENTS.md
@components/hero/PushbackHero.tsx

<interfaces>
<!-- HeroArtifactSVG (created in Plan 16-01) consumer signature -->

The artifact component shipped by 16-01:

```tsx
// components/hero/HeroArtifactSVG.tsx
export default function HeroArtifactSVG({ className }: { className?: string }) { /* ... */ }
```

Consumer usage in PushbackHero:

```tsx
import HeroArtifactSVG from '@/components/hero/HeroArtifactSVG'

// Inside the hero <section>, after the gradient layer, before the foreground content:
<div
  aria-hidden
  className="hidden md:block"
  style={{
    position: 'absolute',
    right: '4%',
    top: '50%',
    transform: 'translateY(-50%) rotate(2deg)',
    width: '32%',
    maxWidth: 560,
    minWidth: 320,
    opacity: 0.85,
    pointerEvents: 'none',
    zIndex: 1,
  }}
>
  <HeroArtifactSVG />
</div>
```

The `rotate(2deg)` tilt + 0.85 opacity reads as "editorial illustration sitting in the page composition" rather than "screenshot pinned to the wall". The hidden-on-mobile keeps the small-screen hero uncluttered (per D-discretion — mobile gets the gradient + headline only).

<!-- Existing fallback gradient pattern (PushbackHero.tsx lines 452-461) -->

The mobile fallback already proves the two-radial-gradient stack that Plan 16-02 promotes to universal default:

```ts
background:
  "radial-gradient(ellipse 80% 60% at 70% 20%, rgba(132,204,22,0.22) 0%, transparent 60%), " +
  "radial-gradient(ellipse 60% 50% at 20% 80%, rgba(132,204,22,0.12) 0%, transparent 65%), " +
  "linear-gradient(180deg, #0a0602 0%, #0a0a0a 100%)"
```

This is the gradient stack to ship — it's already on the page for mobile users, so the visual change vs. the current mobile experience is just the artifact + serif headline, not the colors (per D-discretion constraint).

<!-- SituationLine GSAP hover handlers (PushbackHero.tsx lines 220-269) -->

The existing component does three things:
1. Entrance tween (gsap.fromTo on opacity + x with delay = index * 0.12).
2. Hover handlers (mouseenter / mouseleave) that tween `x` and toggle a React `isHovered` state to swap text-shadow + color.
3. Renders a `<span>` with massive font size, weight 900, letter-spacing -0.03em, and text-shadow glow.

The rewrite must:
- Preserve (1) and (2) verbatim.
- Add focus/blur handlers mirroring mouseenter/mouseleave so keyboard nav lights up the headline (D-17). Use `tabIndex={0}` on the span so it is focusable.
- Change the rendered span's typography ONLY: add `fontFamily: 'var(--font-serif)'`, `fontStyle: 'italic'`. Keep font-size, weight, letter-spacing, color, and text-shadow logic identical.
- Optionally adjust `letter-spacing` from `-0.03em` to a slightly looser value (e.g. `-0.01em`) because serif italics need more horizontal breathing than the geometric sans — this is planner discretion within the constraint "look right".

<!-- Pieces in the file that DO get deleted -->

These are the chunks to physically remove:
- The `vertexShader` template literal (lines 7-13).
- The `fragmentShader` template literal (lines 14-213) — the giant 200-line GLSL block.
- All `useRef` declarations for WebGL handles (`canvasRef`, `glRef`, `programRef`, `bufferRef`, `positionLocationRef`, `timeLocationRef`, `resolutionLocationRef`, `mouseLocationRef`, `intensityLocationRef`, `mouseRef`, `startTimeRef`, `globalIntensityRef`, `rafRef`, `animateFrameFnRef`).
- The `[isCanvasReady, setIsCanvasReady]` state hook AND its loading-overlay JSX (lines 464-487).
- The `[shaderEligible, setShaderEligible]` hook + its mediaQuery useEffect (lines 293-299).
- The `createShader` helper (lines 308-319).
- The "WebGL init" useEffect (lines 321-383).
- The "animateFrame" useEffect (lines 394-420).
- The IntersectionObserver useEffect for pausing the RAF (lines 422-441).
- The `<canvas ref={canvasRef} ... />` element (lines 446-450).
- The conditional shader-eligible ternary that wraps the canvas-vs-fallback choice (line 445 + closing).

<!-- Pieces in the file that STAY -->

- `'use client'` directive (still needed for GSAP per-line hover handlers).
- React imports + GSAP import.
- `SituationLine` sub-component (entrance + hover tweens stay; only typography changes per D-05/D-17).
- `topRef`, `ctaRef` refs and their entrance tweens (lines 385-392).
- The whole foreground content JSX (lines 494-555): wordmark, tagline trio, pillar list, CTA copy, buttons, "Free to start" line. NOT changed.
- `sectionRef` (still useful for IntersectionObserver if we keep it — but with WebGL gone the observer can be removed too; the section ref becomes unused — fine to drop along with all WebGL state).

The acceptance criteria below grep on the negative space (deletion confirmations) AND the positive additions (HeroArtifactSVG import, --font-serif usage, focus handlers).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete WebGL shader path + all supporting refs/effects from PushbackHero.tsx</name>
  <files>components/hero/PushbackHero.tsx</files>
  <read_first>
    - components/hero/PushbackHero.tsx (entire file, ~560 lines) — every line must be read once to know exactly what to remove vs. keep; in particular the boundary between deletable shader code and retained foreground JSX
    - .planning/phases/16-editorial-landing-page-redesign/16-CONTEXT.md — D-01 (delete shader on ALL devices), D-14 (zero RAF, zero shader source), D-15 (mobile lighthouse ≥85)
    - .planning/phases/16-editorial-landing-page-redesign/16-01-PLAN.md — confirm HeroArtifactSVG default export name, signature, file path
    - CLAUDE.md / AGENTS.md — "this is NOT the Next.js you know"; the file is a client component using GSAP (a third-party DOM library) — those imports stay
  </read_first>
  <behavior>
    - After this task, `PushbackHero.tsx` contains no `gl_FragColor`, no `precision mediump float`, no `WebGLRenderingContext`, no `getContext('webgl')`, no `createShader`, no `gl.drawArrays`, no `requestAnimationFrame` calls tied to shader rendering.
    - The file's overall size shrinks by ~250 lines (the 200-line shader literal plus the ~50 lines of init/animate/cleanup/observer effects).
    - The `<section ref={sectionRef}>` is allowed to drop the `sectionRef` if its only consumer was the deleted IntersectionObserver.
    - The mobile-fallback gradient div becomes the universal background div (no longer behind a `shaderEligible ? canvas : fallback` ternary).
    - The loading overlay (the centered "Pushback." mark that fades out once `isCanvasReady`) is deleted entirely — without a shader to wait on, there's nothing to wait for. The `hero-dot-glow` keyframes block inside the loading overlay's `<style>` tag is deleted with it.
    - All existing copy in the foreground content JSX is preserved verbatim — no rewording, no rephrasing, no reformatting.
  </behavior>
  <action>
Edit `components/hero/PushbackHero.tsx`. This is a substantial deletion; do it in three passes inside one edit session.

**Pass 1 — Delete the shader source (lines ~7 to ~213).** Remove the entire `const vertexShader = \`...\`` template literal AND the entire `const fragmentShader = \`...\`` template literal. After this pass the only top-level code before `interface SituationLineProps` should be the `'use client'` directive and the `import` lines.

**Pass 2 — Delete all WebGL-related refs and state in `PushbackHero` body (currently around lines 271-441).** Specifically remove:
- The `canvasRef`, `glRef`, `programRef`, `bufferRef`, `positionLocationRef`, `timeLocationRef`, `resolutionLocationRef`, `mouseLocationRef`, `intensityLocationRef`, `mouseRef`, `startTimeRef`, `globalIntensityRef`, `rafRef`, `animateFrameFnRef` `useRef` declarations.
- The `[isCanvasReady, setIsCanvasReady]` useState.
- The `[shaderEligible, setShaderEligible]` useState AND its useEffect that reads matchMedia for mobile + reduced-motion.
- The `createShader` helper function.
- The "WebGL init" useEffect (the one that calls `createShader`, links the program, sets up resize + mousemove listeners). Delete the whole useEffect including its cleanup return.
- The "animateFrame" useEffect (RAF loop driving uniforms + drawArrays). Delete it entirely.
- The IntersectionObserver useEffect that pauses/resumes the RAF on visibility (lines ~422-441). Delete it entirely.

After this pass, the body of `PushbackHero` retains only:
- `const topRef = useRef<HTMLDivElement>(null)`
- `const ctaRef = useRef<HTMLDivElement>(null)`
- `const pillars = ["VET.", "SIGN.", "REPLY.", "RECOVER."]`
- ONE useEffect for the `topRef` + `ctaRef` entrance tweens (the one currently around lines 385-392).
- The return statement.

If `sectionRef` is referenced anywhere outside the deleted observer effect, keep it; if not, delete it too. Likely it can go.

**Pass 3 — Replace the JSX background + delete the loading overlay.** In the returned JSX:
- Remove the `shaderEligible ? (...) : (...)` ternary entirely. Replace it with the gradient div alone — the same `<div aria-hidden className="absolute inset-0 w-full h-full" style={{ background: "radial-gradient(...)"... }} />` that the current code uses inside the ternary's `else` branch (matches the gradient stack documented in `<interfaces>` above).
- Remove the loading-overlay block: the entire `<div className="absolute inset-0 z-30 flex flex-col items-center justify-center pointer-events-none" style={{ background: '#0a0602', opacity: isCanvasReady ? 0 : 1, ... }}> ... </div>` including its inner `<style>{` `@keyframes hero-dot-glow { ... }` `}</style>` AND the "Pushback." brand mark inside it. The loading overlay no longer has anything to wait for.
- The `<section ref={sectionRef} className="relative h-screen w-full" style={{ overflow: 'clip', background: "#0a0602" }}>` root remains. If `sectionRef` is dropped, replace with just `<section className="relative h-screen w-full" style={{ overflow: 'clip', background: "#0a0602" }}>`.
- Keep the bottom-fade `<div className="absolute inset-x-0 bottom-0 h-2/5 pointer-events-none" style={{ background: "linear-gradient(to top, rgba(10,6,2,0.85) 0%, transparent 100%)" }} />` — it grounds the foreground copy and the artifact will sit between it and the gradient.
- Keep the `<div className="relative z-10 h-full flex flex-col justify-between p-8 md:p-14">` foreground container and ALL of its existing child content (wordmark, tagline trio, pillars, CTA block, "Free to start" line) verbatim.

This task is **deletion + gradient-only background**. It does NOT yet add the HeroArtifactSVG import or the serif typography — those land in Task 2. After Task 1 the page should still render correctly (gradient background, normal Geist headline, no canvas, no console errors), just without the editorial artifact yet.

Do NOT touch the copy inside `topRef`'s div, the `pillars` array values, the CTA block's text, or the "Free to start" paragraph. All copy is locked (per CLAUDE.md context, D-13).

Do NOT rename `SituationLine` or change its signature.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "components/hero/PushbackHero.tsx" | grep -v '^#' | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - No WebGL context creation: `grep -c "getContext('webgl')" components/hero/PushbackHero.tsx` returns `0` AND `grep -c "getContext(\"webgl\")" components/hero/PushbackHero.tsx` returns `0`
    - No fragment shader source: `grep -c "gl_FragColor" components/hero/PushbackHero.tsx` returns `0` AND `grep -c "precision mediump float" components/hero/PushbackHero.tsx` returns `0`
    - No vertex shader source: `grep -c "gl_Position" components/hero/PushbackHero.tsx` returns `0`
    - No shader-eligible state: `grep -c "shaderEligible" components/hero/PushbackHero.tsx` returns `0`
    - No createShader helper: `grep -c "createShader" components/hero/PushbackHero.tsx` returns `0`
    - No drawArrays call: `grep -c "drawArrays" components/hero/PushbackHero.tsx` returns `0`
    - No RAF call: `grep -c "requestAnimationFrame" components/hero/PushbackHero.tsx` returns `0`
    - No cancelAnimationFrame call: `grep -c "cancelAnimationFrame" components/hero/PushbackHero.tsx` returns `0`
    - No isCanvasReady state: `grep -c "isCanvasReady" components/hero/PushbackHero.tsx` returns `0`
    - No hero-dot-glow keyframes: `grep -c "hero-dot-glow" components/hero/PushbackHero.tsx` returns `0`
    - No <canvas element: `grep -cE "<canvas" components/hero/PushbackHero.tsx` returns `0`
    - Gradient stack still rendered: `grep -F "radial-gradient(ellipse 80% 60% at 70% 20%, rgba(132,204,22,0.22)" components/hero/PushbackHero.tsx | wc -l | tr -d ' '` returns at least `1`
    - SituationLine component still exported (unchanged signature): `grep -c "function SituationLine" components/hero/PushbackHero.tsx` returns `1`
    - GSAP still imported (per-line hover tween stays): `grep -c 'from "gsap"' components/hero/PushbackHero.tsx` returns `1`
    - 'use client' directive still present: `head -1 components/hero/PushbackHero.tsx | grep -c "^'use client'$"` returns `1`
    - Pillar copy intact: `grep -c "\"VET.\"" components/hero/PushbackHero.tsx` returns at least `1` AND `grep -c "\"RECOVER.\"" components/hero/PushbackHero.tsx` returns at least `1`
    - Hero copy intact (Four tools line): `grep -F "Four tools. One difficult client." components/hero/PushbackHero.tsx | wc -l | tr -d ' '` returns at least `1`
    - Hero copy intact (Free to start line): `grep -F "Free to start. No card required." components/hero/PushbackHero.tsx | wc -l | tr -d ' '` returns at least `1`
    - Two CTAs intact: `grep -F "Run your first situation free" components/hero/PushbackHero.tsx | wc -l | tr -d ' '` returns at least `1` AND `grep -F "See how it works" components/hero/PushbackHero.tsx | wc -l | tr -d ' '` returns at least `1`
    - File shrank substantially: `[ "$(wc -l < components/hero/PushbackHero.tsx | tr -d ' ')" -lt 300 ] && echo OK` outputs `OK`
    - tsc clean for this file: `npx tsc --noEmit 2>&1 | grep "components/hero/PushbackHero.tsx" | grep -v '^#' | wc -l | tr -d ' '` returns `0`
  </acceptance_criteria>
  <done>
    `components/hero/PushbackHero.tsx` contains no shader code, no WebGL context, no RAF loop, and no loading overlay. The gradient div is the sole background. The foreground content (wordmark, tagline trio, four pillars, CTA block, "Free to start" line) is byte-identical to before. SituationLine still exists with its entrance + hover GSAP tweens. tsc passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Compose the editorial hero — import HeroArtifactSVG, serif-italic the pillar line, add focus-visible state</name>
  <files>components/hero/PushbackHero.tsx</files>
  <read_first>
    - components/hero/PushbackHero.tsx (current state AFTER Task 1) — confirm SituationLine internals and the structure of the returned JSX after shader removal
    - components/hero/HeroArtifactSVG.tsx (created in Plan 16-01) — confirm prop signature is `{ className?: string }` and the SVG renders at viewBox 0 0 600 800
    - .planning/phases/16-editorial-landing-page-redesign/16-CONTEXT.md — D-02 (two-layer composition), D-05 (one major heading in serif italic, all four pillar words), D-06 (rest stays Geist), D-17 (focus-visible parity with hover)
    - Memory `feedback_lime_color.md` — lime literal only
  </read_first>
  <behavior>
    - `HeroArtifactSVG` is imported via `import HeroArtifactSVG from '@/components/hero/HeroArtifactSVG'` at the top of the file.
    - A new `<div aria-hidden className="hidden md:block" style={{ position: 'absolute', right: '4%', top: '50%', transform: 'translateY(-50%) rotate(2deg)', width: '32%', maxWidth: 560, minWidth: 320, opacity: 0.85, pointerEvents: 'none', zIndex: 1 }}><HeroArtifactSVG /></div>` is placed inside the `<section>` AFTER the gradient layer and BEFORE the bottom-fade gradient. It sits visually between the background gradient and the foreground content — which is z-index 10 — and is hidden on mobile via Tailwind `hidden md:block`.
    - Inside `SituationLine`, the rendered `<span>` gains two new inline-style declarations: `fontFamily: 'var(--font-serif)'` and `fontStyle: 'italic'`. Nothing else inside `SituationLine` is changed (entrance tween, hover handlers, hover state, color, text-shadow logic, font-size, font-weight, letter-spacing all stay).
    - `SituationLine` adds `tabIndex={0}` to the `<span>` so keyboard users can focus it.
    - Two new event handlers `handleFocus` and `handleBlur` are wired alongside the existing `handleEnter` / `handleLeave`. They mirror the same body: `handleFocus` calls `setIsHovered(true)` + `gsap.to(el, { x: 10, duration: 0.4, ease: 'power3.out' })`; `handleBlur` calls `setIsHovered(false)` + `gsap.to(el, { x: 0, duration: 0.5, ease: 'power3.out' })`. They are added/removed in the existing `useEffect` cleanup pair.
    - The `<span>` style adds `outline: 'none'` (the lime glow IS the focus indicator, so the default browser outline is suppressed) AND continues to rely on `isHovered` for the visible state — meaning focus-via-tab triggers `isHovered = true` and the lime glow appears. This satisfies WCAG 2.4.7 because the focus indicator is visually distinct (lime color + 40px glow).
  </behavior>
  <action>
Edit `components/hero/PushbackHero.tsx` (the post-Task-1 file).

**Step 1 — Add import.** After the existing `import { gsap } from "gsap"` line, add:
```tsx
import HeroArtifactSVG from '@/components/hero/HeroArtifactSVG'
```

**Step 2 — Update `SituationLine` typography + focus handlers.**

Inside `SituationLine`, in the existing `useEffect`, find the `handleEnter` / `handleLeave` definitions and add two new handlers:
```tsx
const handleFocus = () => {
  setIsHovered(true)
  gsap.to(el, { x: 10, duration: 0.4, ease: "power3.out" })
}
const handleBlur = () => {
  setIsHovered(false)
  gsap.to(el, { x: 0, duration: 0.5, ease: "power3.out" })
}
```

Wire them onto the element and into the cleanup return:
```tsx
el.addEventListener("mouseenter", handleEnter)
el.addEventListener("mouseleave", handleLeave)
el.addEventListener("focus", handleFocus)
el.addEventListener("blur", handleBlur)
return () => {
  el.removeEventListener("mouseenter", handleEnter)
  el.removeEventListener("mouseleave", handleLeave)
  el.removeEventListener("focus", handleFocus)
  el.removeEventListener("blur", handleBlur)
}
```

In the returned `<span>`, add `tabIndex={0}` and update the inline `style` object to include:
- `fontFamily: 'var(--font-serif)'`
- `fontStyle: 'italic'`
- `outline: 'none'`

Keep the existing `fontSize: 'clamp(2.8rem, 7vw, 6.5rem)'`, `fontWeight: 900`, `letterSpacing: '-0.03em'` (the serif italic at weight 900 reads as condensed — leave letter-spacing as-is for the first cut; adjusting it is a follow-up if needed), color logic, transition, and text-shadow logic UNCHANGED. The `className` `"block leading-[0.95] cursor-default select-none"` stays.

**Step 3 — Insert the HeroArtifactSVG layer in the hero JSX.**

Locate the gradient `<div aria-hidden className="absolute inset-0 w-full h-full" style={{ background: "radial-gradient(...)... }} />` (the only background div now after Task 1). Immediately AFTER that div and BEFORE the bottom-fade gradient `<div className="absolute inset-x-0 bottom-0 h-2/5 pointer-events-none" ... />`, insert:

```tsx
<div
  aria-hidden
  className="hidden md:block"
  style={{
    position: 'absolute',
    right: '4%',
    top: '50%',
    transform: 'translateY(-50%) rotate(2deg)',
    width: '32%',
    maxWidth: 560,
    minWidth: 320,
    opacity: 0.85,
    pointerEvents: 'none',
    zIndex: 1,
  }}
>
  <HeroArtifactSVG />
</div>
```

The artifact is hidden on mobile (`hidden md:block`) — at the small-screen breakpoint the hero stays as gradient + foreground only.

Do NOT change any copy. Do NOT change the CTA buttons. Do NOT change the bottom-fade gradient. Do NOT introduce any new animation library, new GSAP timelines, or `requestAnimationFrame` calls. The page must stay zero-RAF.

Do NOT serif-ify any other element — only the four pillar lines render in Instrument Serif italic. The wordmark, the tagline trio, the CTA block, and the "Free to start" line all keep the existing Geist sans typography (per D-05 / D-06: one major heading, not a system change).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "components/hero/PushbackHero.tsx" | grep -v '^#' | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - HeroArtifactSVG import present: `grep -c "^import HeroArtifactSVG from '@/components/hero/HeroArtifactSVG'$" components/hero/PushbackHero.tsx` returns `1`
    - HeroArtifactSVG invoked: `grep -c "<HeroArtifactSVG />" components/hero/PushbackHero.tsx` returns `1`
    - Artifact wrapper hidden on mobile: `grep -F 'className="hidden md:block"' components/hero/PushbackHero.tsx | wc -l | tr -d ' '` returns at least `1`
    - Artifact wrapper rotation literal: `grep -F "translateY(-50%) rotate(2deg)" components/hero/PushbackHero.tsx | wc -l | tr -d ' '` returns at least `1`
    - SituationLine span uses --font-serif: `grep -F "fontFamily: 'var(--font-serif)'" components/hero/PushbackHero.tsx | wc -l | tr -d ' '` returns at least `1`
    - SituationLine span uses italic: `grep -F "fontStyle: 'italic'" components/hero/PushbackHero.tsx | wc -l | tr -d ' '` returns at least `1`
    - tabIndex=0 on SituationLine span: `grep -c "tabIndex={0}" components/hero/PushbackHero.tsx` returns at least `1`
    - Focus handler added: `grep -c "handleFocus" components/hero/PushbackHero.tsx` returns at least `1` AND `grep -c "handleBlur" components/hero/PushbackHero.tsx` returns at least `1`
    - Focus event listener wired: `grep -c 'addEventListener("focus"' components/hero/PushbackHero.tsx` returns at least `1`
    - Blur event listener wired: `grep -c 'addEventListener("blur"' components/hero/PushbackHero.tsx` returns at least `1`
    - Focus listener cleanup wired: `grep -c 'removeEventListener("focus"' components/hero/PushbackHero.tsx` returns at least `1`
    - Hero copy still intact (regression check): `grep -F "Four tools. One difficult client." components/hero/PushbackHero.tsx | wc -l | tr -d ' '` returns at least `1`
    - Pillar array still has all four words: `grep -c '"VET\."' components/hero/PushbackHero.tsx` returns at least `1` AND `grep -c '"SIGN\."' components/hero/PushbackHero.tsx` returns at least `1` AND `grep -c '"REPLY\."' components/hero/PushbackHero.tsx` returns at least `1` AND `grep -c '"RECOVER\."' components/hero/PushbackHero.tsx` returns at least `1`
    - No new RAF / canvas re-introduction: `grep -c "requestAnimationFrame" components/hero/PushbackHero.tsx` returns `0` AND `grep -cE "<canvas" components/hero/PushbackHero.tsx` returns `0`
    - No var(--brand-amber): `grep -c "var(--brand-amber)" components/hero/PushbackHero.tsx` returns `0`
    - tsc clean: `npx tsc --noEmit 2>&1 | grep "components/hero/PushbackHero.tsx" | grep -v '^#' | wc -l | tr -d ' '` returns `0`
  </acceptance_criteria>
  <done>
    `components/hero/PushbackHero.tsx` imports `HeroArtifactSVG` from `@/components/hero/HeroArtifactSVG`, places it inside the hero `<section>` between the gradient layer and the bottom-fade gradient (hidden on mobile, tilted 2deg, opacity 0.85, sized 32% width), and serif-italics the four pillar lines via inline `fontFamily: 'var(--font-serif)'` + `fontStyle: 'italic'` on the `SituationLine` `<span>`. The span gains `tabIndex={0}`, `outline: 'none'`, and focus/blur GSAP handlers mirroring the existing mouseenter/mouseleave handlers. All hero copy is byte-identical to before. No RAF, no canvas. tsc passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser render | All hero content is hardcoded; the only dynamic input is the GSAP tween state — same trust posture as before. |
| GPU shader | DELETED. After this plan no fragment shader runs in the user's browser — strictly reduces attack surface (GPU driver bugs, WebGL fingerprinting) compared to the previous state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.02-01 | DoS (memory leak) | SituationLine event listeners | mitigate | useEffect cleanup return removes all four listeners (mouseenter, mouseleave, focus, blur). Acceptance criterion grep-verifies the four `removeEventListener` calls. |
| T-16.02-02 | DoS (GPU exhaustion) | WebGL shader (pre-plan) | mitigate | DELETED in Task 1. Grep verifies no `getContext('webgl')`, no `drawArrays`, no shader source remains. |
| T-16.02-03 | Accessibility regression | Italic serif at weight 900 | accept | Pillar words are short (VET. / SIGN. / REPLY. / RECOVER.) and rendered at huge sizes — italic legibility risk is low. WCAG focus-visible compliance addressed by D-17 mitigation. |
| T-16.02-04 | Accessibility regression | Focus indicator suppression | mitigate | The `outline: 'none'` on the pillar span is COMPENSATED by the lime color shift + text-shadow glow triggered by the focus handler. The visible state contrast ratio between focused (#84cc16 on #0a0602) and unfocused (#fafafa-ish on #0a0602) exceeds 3:1 — meets WCAG 2.4.13 (focus appearance). |
| T-16.02-05 | XSS | HeroArtifactSVG inline render | accept | SVG content is hardcoded literals in Plan 16-01's component; no user input flows in. No new attack surface. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with zero errors mentioning `components/hero/PushbackHero.tsx`.
- `npx eslint components/hero/PushbackHero.tsx --quiet` returns 0 errors.
- `npm run build` (or `npx next build`) completes; the hero bundle no longer contains the GLSL string.
- Manual smoke: run `npm run dev`, visit `/`. Confirm:
  1. Hero renders with gradient + tilted contract-page artifact on desktop, gradient only on mobile.
  2. The four pillar lines (VET., SIGN., REPLY., RECOVER.) render in Instrument Serif italic.
  3. Hovering any pillar line lights up the lime glow as before.
  4. Pressing Tab moves focus from "Try free →" sticky nav button (or whichever element precedes the hero) through each pillar line — each focused line gets the lime glow.
  5. No canvas element exists in the hero DOM (verify via DevTools Elements panel: `document.querySelector('canvas')` returns null inside the hero section).
  6. DevTools Performance trace shows no continuous RAF activity on the hero section after initial paint.
</verification>

<success_criteria>
- D-01 met: WebGL shader path completely removed (no canvas, no shader source, no RAF loop tied to GPU rendering).
- D-02 met: hero background is gradient + HeroArtifactSVG (two-layer static composition).
- D-05 met: the four pillar words render in Instrument Serif italic; no other element is serif-ified.
- D-14 met: hero ships zero shader-driven RAF; remaining GSAP usage is hover/entrance only.
- D-17 met: keyboard focus on a pillar line produces the lime-glow state.
- All hero copy is byte-identical to the pre-Phase-16 state.
- File line count drops below 300 (from the original ~560).
</success_criteria>

<output>
After completion, write `.planning/phases/16-editorial-landing-page-redesign/16-02-SUMMARY.md` covering: lines deleted (rough count), the exact import line added, the exact wrapper-div style used to position the artifact, the exact inline-style fields added to the SituationLine span, and the four event-listener pairs now wired in SituationLine's useEffect.
</output>
