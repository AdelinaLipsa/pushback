---
phase: 16-editorial-landing-page-redesign
plan: 03
type: execute
wave: 3
depends_on:
  - 16-02
files_modified:
  - app/page.tsx
  - app/globals.css
autonomous: false
requirements: []
must_haves:
  truths:
    - "D-07: The current `<div className=\"grid grid-cols-2 md:grid-cols-4 gap-3\">` 4-card grid in the 'What's inside' section is replaced with a semantic `<ol>` containing four `<li>` rows; no card backgrounds, no borders around items, no Lucide icons"
    - "D-08: Each row visually shows a large serif numeral '01.' '02.' '03.' '04.' on the left in --brand-lime (Instrument Serif via --font-serif), a pillar name in Geist sans bold (--text-primary), and a description in Geist sans regular (--text-secondary)"
    - "D-09: The list is a single-column stack constrained to max-w-3xl, with ~2rem vertical rhythm between rows, separated by hairline 1px solid var(--bg-border) rules"
    - "D-10: The ticker section (the `<div style={{ backgroundColor: 'var(--bg-surface)', borderTop:..., overflow: 'clip', padding: '0.875rem 0' }}>` containing the `ticker-track` div with the 14 client-situation strings) is removed from app/page.tsx; the TICKER_ITEMS module-scope constant is also removed; the `ticker-track` keyframe/CSS declarations in app/globals.css are removed if unused elsewhere"
    - "D-11/12/13: The 3D coverflow ToolCarousel, payment-tracking mockup section, document-generation 3-card section, reply-threading section, pricing section, and all copy strings are unchanged"
    - "D-18: The new list uses semantic `<ol>` + `<li>` (not `<div>` rows); numerals are rendered inline in a `<span aria-hidden>` adjacent to the pillar name, so screen readers announce the ordered sequence naturally"
    - "Lucide imports in app/page.tsx are pruned of icons no longer used (ShieldCheck, FileSearch, MessageSquareWarning, BanknoteArrowDown were the 4-card icons); icons used elsewhere (Check) stay"
    - "D-15: A mobile lighthouse run on the rebuilt landing page reports performance score ≥85 — the run is performed as a human-verify checkpoint at the end of this plan"
  artifacts:
    - path: "app/page.tsx"
      provides: "Landing page with numbered editorial pillar list, ticker removed, unused Lucide imports pruned"
      contains: "<ol"
    - path: "app/globals.css"
      provides: "Globals cleared of `.ticker-track` keyframes/CSS if unused elsewhere"
      contains: ""
  key_links:
    - from: "app/page.tsx 'What's inside' section"
      to: "Instrument Serif via --font-serif"
      via: "inline style fontFamily: 'var(--font-serif)' on the numeral span"
      pattern: "var\\(--font-serif\\)"
    - from: "app/page.tsx"
      to: "removed ticker DOM"
      via: "deletion of TICKER_ITEMS const + ticker-section JSX"
      pattern: "TICKER_ITEMS"
---

<objective>
Three coordinated edits to `app/page.tsx` (+ a small `app/globals.css` cleanup) that close out the editorial redesign: (1) replace the 4-card pillar grid with a numbered semantic `<ol>`, (2) delete the ticker section + its module-scope data + its CSS, (3) prune the now-unused Lucide imports. Then a mobile lighthouse human-verify gate to confirm D-15 (performance ≥85).

Purpose: The hero (Plans 01 + 02) is editorial. This plan brings the next-section-down and the page-flow into the same grammar. After this plan the page reads hero → editorial pillars → live demo, with the ticker interlude gone, the cards-in-a-row gone, and the lighthouse confirming no perf regression.

Output: Two modified files, one human-verify lighthouse checkpoint. No new files, no new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-editorial-landing-page-redesign/16-CONTEXT.md
@.planning/phases/16-editorial-landing-page-redesign/16-01-PLAN.md
@.planning/phases/16-editorial-landing-page-redesign/16-02-PLAN.md
@CLAUDE.md
@AGENTS.md
@app/page.tsx
@app/globals.css

<interfaces>
<!-- Current 4-card pillar grid (app/page.tsx lines ~409-454) -->

The section to rewrite begins at:

```tsx
{/* What's inside — 4-pillar strip. ... */}
<section style={{ borderTop: '1px solid var(--bg-border)', backgroundColor: 'var(--bg-base)' }} className="py-16 md:py-20">
  <div className="max-w-5xl mx-auto px-6">
    <div className="text-center mb-10" data-animate>
      <p style={{ color: 'var(--brand-lime)', ... }}>What&apos;s inside</p>
      <h2 ...>Four connected tools. One difficult-client problem.</h2>
      <p ...>Pushback covers the full lifecycle ...</p>
    </div>
    <div className="grid grid-cols-2 md:grid-cols-4 gap-3">
      {[
        { Icon: ShieldCheck, label: 'Vet prospects', desc: '...', step: '01' },
        { Icon: FileSearch, label: 'Analyze contracts', desc: '...', step: '02' },
        { Icon: MessageSquareWarning, label: 'Reply to pushback', desc: '...', step: '03' },
        { Icon: BanknoteArrowDown, label: 'Recover payment', desc: '...', step: '04' },
      ].map(({ Icon, label, desc, step }, idx) => (
        <div ... style={{ backgroundColor: 'var(--bg-surface)', border: '1px solid var(--bg-border)', borderRadius: '0.75rem', padding: '1.25rem', ... }}>
          <Icon ... />
          <span ...>{step}</span>
          <p ...>{label}</p>
          <p ...>{desc}</p>
        </div>
      ))}
    </div>
  </div>
</section>
```

The replacement keeps:
- The wrapper `<section>` with the same borderTop + background color + py classes (the section frame is unchanged so flanking sections still slot in correctly).
- The eyebrow `"What's inside"`, the H2 `"Four connected tools. One difficult-client problem."`, and the subheading `"Pushback covers the full lifecycle — not just the reply. Each tool feeds the next, so when a client escalates you're already three steps ahead."` — all three texts are byte-identical to before (D-13 keeps copy).
- The four `(label, desc)` pairs map 1:1 from the old data array — `Vet prospects` / `Analyze contracts` / `Reply to pushback` / `Recover payment` and their descriptions. No copy rewrite.

The replacement changes:
- The inner container: `max-w-5xl` → `max-w-3xl` (D-09 narrower editorial column).
- The `<div className="grid grid-cols-2 md:grid-cols-4 gap-3">` becomes `<ol>` with no list-style markers, single-column, gap ~2rem.
- Each `<div>` row becomes `<li>` with NO background, NO border, NO Lucide icon. A hairline rule (`borderTop: '1px solid var(--bg-border)'`) separates rows (apply to each `<li>` except the first; or to each non-last `<li>` via `:not(:last-child)` — implementation discretion; the visible effect is what matters).

<!-- Ticker section to delete (app/page.tsx lines ~456-466) -->

```tsx
{/* Ticker */}
<div style={{ backgroundColor: 'var(--bg-surface)', borderTop: '1px solid var(--bg-border)', borderBottom: '1px solid var(--bg-border)', overflow: 'clip', padding: '0.875rem 0' }}>
  <div className="ticker-track flex gap-8 whitespace-nowrap w-max">
    {[...TICKER_ITEMS, ...TICKER_ITEMS].map((item, i) => (
      <span key={i} className="flex items-center gap-8">
        <span style={{ color: 'var(--brand-lime)', fontWeight: 600, fontSize: '0.9rem', letterSpacing: '0.03em' }}>{item}</span>
        <span style={{ color: 'var(--text-muted)' }}>·</span>
      </span>
    ))}
  </div>
</div>
```

This block is deleted entirely. The `TICKER_ITEMS` constant at the top of the file (currently lines 18-33) is also deleted, since no other consumer references it.

After deletion the page DOM flow is: `<PushbackHero>` → `<div id="hero-end">` → "What's inside" section → `<LiveDemo />`.

<!-- ticker-track CSS in app/globals.css -->

The class `ticker-track` is referenced ONLY from the deleted ticker JSX (confirmed by the only `className="ticker-track ..."` in the repo being the block above). If `app/globals.css` declares a `.ticker-track` class with `@keyframes ticker-scroll` (or similar), those declarations are dead after the JSX deletion and should be removed.

The executor must `grep -rn "ticker-track\|ticker-scroll" app/ components/ lib/` before deleting CSS — if any other consumer is found, leave the CSS in place and note in the SUMMARY. Otherwise, remove the rule + keyframes.

<!-- Lucide imports to prune (app/page.tsx line 6) -->

Current:
```tsx
import { Check, ShieldCheck, FileSearch, MessageSquareWarning, BanknoteArrowDown } from 'lucide-react'
```

After the 4-card grid is gone, `ShieldCheck` / `FileSearch` / `MessageSquareWarning` / `BanknoteArrowDown` are unused in this file. `Check` stays (it's used in the payment-tracking, reply-threading, and pricing sections per a quick scan of the current file).

Replacement import line:
```tsx
import { Check } from 'lucide-react'
```

The executor must `grep -nE "ShieldCheck|FileSearch|MessageSquareWarning|BanknoteArrowDown" app/page.tsx` to confirm no other usages remain before pruning. If any are found (unlikely), keep them in the import.

<!-- Lighthouse checkpoint -->

The final task is a `checkpoint:human-verify` gate. The human runs lighthouse on the deployed (or `next build && next start`) landing page in mobile mode and confirms score ≥85. This is necessary because lighthouse cannot run inside Claude's environment headlessly without additional tooling not present in this project.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace 4-card pillar grid with numbered semantic <ol> editorial list</name>
  <files>app/page.tsx</files>
  <read_first>
    - app/page.tsx (entire file, ~790 lines — large, but read once since multiple sections are touched in this plan)
    - .planning/phases/16-editorial-landing-page-redesign/16-CONTEXT.md — D-07, D-08, D-09, D-18 (numbered editorial list spec, semantics, layout, color tokens)
    - app/globals.css (already partly read in 16-01) — confirm the names `--brand-lime`, `--text-primary`, `--text-secondary`, `--bg-base`, `--bg-border` are unchanged
    - Memory `feedback_clean_reusable_code.md` — extract repeated style chunks if helpful; do not duplicate
  </read_first>
  <behavior>
    - The section header trio (eyebrow / H2 / subheading) is byte-identical to before.
    - The four data items map 1:1 from the old array to four `<li>` rows. Old keys (Icon, step) are dropped; only `label` and `desc` carry forward. The numeral `01.` … `04.` is generated inline from the index (`String(idx + 1).padStart(2, '0') + '.'`).
    - Each row is a horizontal flex layout on md+ (`flex md:flex-row` analogue using inline style or Tailwind), with the numeral in a fixed-width left column (~4.5rem) and the (label, desc) content in the rest of the row. On mobile the row can stack (numeral above, then label, then desc) — planner discretion within "feels editorial".
    - The list element is `<ol>` with `style={{ listStyle: 'none', padding: 0, margin: 0 }}` so no default markers leak through, and `<li>` rows separated by a 1px `var(--bg-border)` rule. Recommended: every `<li>` gets `borderTop: '1px solid var(--bg-border)'` EXCEPT the first one (use a conditional check on `idx` or apply the rule to every li with `idx > 0` via inline style).
    - Vertical rhythm: each `<li>` has `padding: '2rem 0'` (2rem top + 2rem bottom) to give the requested generous spacing per D-09.
    - The numeral renders with `fontFamily: 'var(--font-serif)'`, `fontStyle` may be either normal or italic — recommend `normal` so the numeric weight/serif identity reads as "editorial heading" rather than "magazine pull quote" (planner discretion). Size around `clamp(2.4rem, 4vw, 3.5rem)`. Color `var(--brand-lime)`. `aria-hidden="true"` on the numeral span (so screen readers rely on the `<ol>` ordering, not the visual numeral — D-18).
    - The pillar name is a `<p>` (or `<span>` rendered as block) in `fontWeight: 700`, `fontSize: '1rem'`, `color: 'var(--text-primary)'`, `letterSpacing: '-0.01em'`.
    - The description is a `<p>` in `fontSize: '0.875rem'`, `color: 'var(--text-secondary)'`, `lineHeight: 1.65`.
    - The existing `data-animate` entrance affordances may be retained on the section header and on the list rows (stagger by `idx * 80` ms) to match the prior section's feel — discretion to keep or drop.
  </behavior>
  <action>
Edit `app/page.tsx`.

**Step 1 — Locate the section.** Find the comment `{/* What's inside — 4-pillar strip. ... */}` (currently around line 409). The section extends from there through its closing `</section>` (currently around line 454).

**Step 2 — Rewrite the section body.** Replace the inner `<div className="max-w-5xl mx-auto px-6">…</div>` block with a `<div className="max-w-3xl mx-auto px-6">` wrapper containing:

a) The existing centered header trio, byte-identical:
```tsx
<div className="text-center mb-10" data-animate>
  <p style={{ color: 'var(--brand-lime)', fontSize: '0.7rem', letterSpacing: '0.15em', textTransform: 'uppercase', fontWeight: 600, marginBottom: '0.875rem' }}>
    What&apos;s inside
  </p>
  <h2 style={{ fontWeight: 800, fontSize: 'clamp(1.6rem, 3.5vw, 2.4rem)', letterSpacing: '-0.025em', lineHeight: 1.1, marginBottom: '0.875rem' }}>
    Four connected tools. One difficult-client problem.
  </h2>
  <p style={{ color: 'var(--text-secondary)', fontSize: '0.9rem', lineHeight: 1.65, maxWidth: '52ch', margin: '0 auto' }}>
    Pushback covers the full lifecycle — not just the reply. Each tool feeds the next, so when a client escalates you&apos;re already three steps ahead.
  </p>
</div>
```

b) A new ordered list replacing the grid div:

```tsx
<ol style={{ listStyle: 'none', padding: 0, margin: 0 }}>
  {[
    { label: 'Vet prospects', desc: 'Paste their first message — Pushback flags red flags before you reply.' },
    { label: 'Analyze contracts', desc: 'Upload a contract — surface missing clauses, hidden risks, negotiation priorities.' },
    { label: 'Reply to pushback', desc: '23 prepared response tools — scope creep, chargebacks, ghosting, IP grabs.' },
    { label: 'Recover payment', desc: 'Track overdue invoices, send escalating notices, generate kill-fee invoices.' },
  ].map(({ label, desc }, idx) => (
    <li
      key={label}
      data-animate
      data-animate-delay={String(idx * 80)}
      style={{
        display: 'flex',
        flexDirection: 'row',
        alignItems: 'flex-start',
        gap: '1.5rem',
        padding: '2rem 0',
        borderTop: idx === 0 ? 'none' : '1px solid var(--bg-border)',
      }}
    >
      <span
        aria-hidden="true"
        style={{
          fontFamily: 'var(--font-serif)',
          fontSize: 'clamp(2.4rem, 4vw, 3.5rem)',
          fontWeight: 400,
          lineHeight: 1,
          color: 'var(--brand-lime)',
          minWidth: '4.5rem',
          letterSpacing: '-0.02em',
        }}
      >
        {String(idx + 1).padStart(2, '0')}.
      </span>
      <div style={{ flex: 1 }}>
        <p style={{ fontWeight: 700, fontSize: '1rem', color: 'var(--text-primary)', letterSpacing: '-0.01em', marginBottom: '0.4rem' }}>
          {label}
        </p>
        <p style={{ color: 'var(--text-secondary)', fontSize: '0.875rem', lineHeight: 1.65 }}>
          {desc}
        </p>
      </div>
    </li>
  ))}
</ol>
```

(Copy strings — `Vet prospects` / `Paste their first message — Pushback flags red flags before you reply.` etc. — are byte-identical to the old grid's data array. Verify before pasting; if any character differs, prefer the OLD version since D-13 locks copy.)

The wrapping `<section style={{ borderTop: '1px solid var(--bg-border)', backgroundColor: 'var(--bg-base)' }} className="py-16 md:py-20">` opening tag and its closing `</section>` are unchanged.

Do NOT keep any Lucide icon imports referenced only from this section — Task 3 prunes them.

Do NOT add card backgrounds, borders around `<li>` items (only the top divider), or icon-in-tile layouts. D-07 is explicit: no card chrome.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "app/page.tsx" | grep -v '^#' | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - <ol> element introduced in the file: `grep -c "<ol style={{ listStyle: 'none'" app/page.tsx` returns at least `1`
    - Old grid div removed: `grep -c 'className="grid grid-cols-2 md:grid-cols-4 gap-3"' app/page.tsx` returns `0`
    - Numeric numeral generation present: `grep -F "String(idx + 1).padStart(2, '0')" app/page.tsx | wc -l | tr -d ' '` returns at least `1`
    - Serif numeral wired: `grep -F "fontFamily: 'var(--font-serif)'" app/page.tsx | wc -l | tr -d ' '` returns at least `1`
    - Lime color on numeral: `grep -F "color: 'var(--brand-lime)'" app/page.tsx | wc -l | tr -d ' '` returns at least `1`
    - aria-hidden on numeral: `grep -F 'aria-hidden="true"' app/page.tsx | wc -l | tr -d ' '` returns at least `1`
    - max-w-3xl wrapper (D-09): `grep -F 'className="max-w-3xl mx-auto px-6"' app/page.tsx | wc -l | tr -d ' '` returns at least `1`
    - Pillar copy intact: `grep -F "'Vet prospects'" app/page.tsx | wc -l | tr -d ' '` returns at least `1` AND `grep -F "'Analyze contracts'" app/page.tsx | wc -l | tr -d ' '` returns at least `1` AND `grep -F "'Reply to pushback'" app/page.tsx | wc -l | tr -d ' '` returns at least `1` AND `grep -F "'Recover payment'" app/page.tsx | wc -l | tr -d ' '` returns at least `1`
    - Eyebrow text unchanged: `grep -F "What&apos;s inside" app/page.tsx | wc -l | tr -d ' '` returns at least `1`
    - H2 unchanged: `grep -F "Four connected tools. One difficult-client problem." app/page.tsx | wc -l | tr -d ' '` returns at least `1`
    - No card-border style left in this section (no `border: '1px solid var(--bg-border)'` on the new <li>): `grep -E "borderTop: idx === 0 \\? 'none' : '1px solid var\\(--bg-border\\)'" app/page.tsx | wc -l | tr -d ' '` returns at least `1` (the only border styling on rows should be the conditional top rule)
    - tsc clean: `npx tsc --noEmit 2>&1 | grep "app/page.tsx" | grep -v '^#' | wc -l | tr -d ' '` returns `0`
  </acceptance_criteria>
  <done>
    The "What's inside" section in `app/page.tsx` is now a `max-w-3xl` centered column containing the unchanged eyebrow/H2/subheading and a semantic `<ol>` with four `<li>` rows. Each row is a horizontal flex layout: a serif lime numeral (`01.` `02.` `03.` `04.`) on the left, pillar name + description on the right, separated by a hairline `var(--bg-border)` rule. No card backgrounds, no borders, no Lucide icons. Copy strings are byte-identical to the previous data array. tsc passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete the ticker section, TICKER_ITEMS constant, and ticker-track CSS (if unused)</name>
  <files>app/page.tsx, app/globals.css</files>
  <read_first>
    - app/page.tsx (state AFTER Task 1) — confirm the ticker block is unchanged from the original (it should be; Task 1 only touched the "What's inside" section)
    - app/globals.css — search for `ticker-track`, `ticker-scroll`, `@keyframes ticker` to know which rules belong to the ticker and where they live
    - .planning/phases/16-editorial-landing-page-redesign/16-CONTEXT.md — D-10 (ticker section deleted, keyframes/CSS removed if unused elsewhere)
  </read_first>
  <behavior>
    - The `{/* Ticker */}` JSX block (currently lines ~456-466 in the post-Task-1 file) is removed entirely.
    - The module-scope `TICKER_ITEMS` constant (currently lines 18-33) is removed entirely.
    - The post-removal section flow is: hero → hero-end sentinel → editorial pillar list (new from Task 1) → LiveDemo. Nothing replaces the ticker; the next section starts immediately.
    - `.ticker-track` CSS class + any associated keyframes (e.g. `@keyframes ticker-scroll`) are removed from `app/globals.css` IF AND ONLY IF a project-wide grep confirms no remaining consumer. The executor MUST verify via `grep -rn "ticker-track\\|ticker-scroll" app/ components/ lib/ types/ 2>/dev/null` before deleting CSS. If any other file references either name, leave the CSS alone and note in the SUMMARY which file blocked the cleanup.
  </behavior>
  <action>
Edit `app/page.tsx`:

**Step 1 — Delete the ticker JSX block.** Find the JSX comment `{/* Ticker */}` and the immediately-following `<div>` opening with `backgroundColor: 'var(--bg-surface)'` and `className=` reference to `ticker-track flex gap-8 whitespace-nowrap w-max`. Delete from the `{/* Ticker */}` comment through the matching closing `</div>` (the OUTER div containing the inner `ticker-track` div). Do not leave a placeholder comment behind — the section is fully removed.

**Step 2 — Delete the TICKER_ITEMS constant.** At the top of the file (currently lines 18-33), remove the entire `const TICKER_ITEMS = [ ... ]` declaration including all 14 string entries and the closing `]`.

**Step 3 — Verify no other ticker consumers.** Run:
```bash
grep -rn "TICKER_ITEMS" app/ components/ lib/ types/ 2>/dev/null
```
Expected output: empty (no references after the deletion). If anything appears, do not commit until resolved.

Edit `app/globals.css`:

**Step 4 — Conditionally remove ticker CSS.** Run:
```bash
grep -rn "ticker-track\|ticker-scroll" app/ components/ lib/ types/ 2>/dev/null
```
- If output is empty (or only references `app/globals.css` itself), delete the `.ticker-track` class definition AND any `@keyframes` named `ticker-scroll` (or whichever name pairs with it) from `app/globals.css`.
- If output contains any other consumer, DO NOT delete the CSS. Note in the SUMMARY which file blocked the cleanup.

If `app/globals.css` does not contain a `.ticker-track` rule at all (because the animation may have been defined as Tailwind utilities or as an inline style elsewhere), simply skip the CSS edit and note this in the SUMMARY.

Do NOT touch any other section of `app/page.tsx`. Specifically: ToolCarousel, LiveDemo, the ContractReveal section, the payment-tracking section, the document-generation section, the reply-threading section, and the pricing section all stay byte-identical (per D-11 / D-12 / D-13). Do NOT modify the existing IntersectionObserver entrance animation `useEffect` in LandingPage — it still applies to remaining `data-animate` rows.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "app/page.tsx" | grep -v '^#' | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - TICKER_ITEMS deleted: `grep -c "TICKER_ITEMS" app/page.tsx` returns `0`
    - Ticker JSX comment gone: `grep -c "{/\\* Ticker \\*/}" app/page.tsx` returns `0`
    - ticker-track class no longer referenced in app/page.tsx: `grep -c "ticker-track" app/page.tsx` returns `0`
    - All 14 ticker strings gone (sample check on three of them): `grep -F "Client wants free revisions" app/page.tsx | wc -l | tr -d ' '` returns `0` AND `grep -F "Invoice is 3 weeks overdue" app/page.tsx | wc -l | tr -d ' '` returns `0` AND `grep -F "Client went silent" app/page.tsx | wc -l | tr -d ' '` returns `0`
    - Other sections intact (ToolCarousel still present): `grep -c "function ToolCarousel" app/page.tsx` returns `1`
    - Other sections intact (pricing eyebrow present): `grep -F "30-day money-back" app/page.tsx | wc -l | tr -d ' '` returns at least `1`
    - LiveDemo still rendered: `grep -c "<LiveDemo />" app/page.tsx` returns `1`
    - ContractReveal still rendered: `grep -c "<ContractReveal />" app/page.tsx` returns `1`
    - Global ticker-track CSS gone OR documented as blocked: `grep -c "ticker-track" app/globals.css` returns `0` (if the rule existed; if it never existed, returns `0` either way)
    - No orphan ticker consumer elsewhere: `grep -rn "ticker-track\\|ticker-scroll\\|TICKER_ITEMS" app/ components/ lib/ types/ 2>/dev/null | wc -l | tr -d ' '` returns `0`
    - tsc clean: `npx tsc --noEmit 2>&1 | grep "app/page.tsx" | grep -v '^#' | wc -l | tr -d ' '` returns `0`
  </acceptance_criteria>
  <done>
    The ticker JSX block and the `TICKER_ITEMS` module-scope constant are removed from `app/page.tsx`. The `.ticker-track` class definition + its keyframes are removed from `app/globals.css` (or, if blocked by another consumer, the SUMMARY documents which file). No other section of `app/page.tsx` is modified. tsc passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Prune unused Lucide imports in app/page.tsx</name>
  <files>app/page.tsx</files>
  <read_first>
    - app/page.tsx (state AFTER Tasks 1 + 2) — confirm which Lucide icons are still referenced
  </read_first>
  <behavior>
    - After Tasks 1 + 2 the icons `ShieldCheck`, `FileSearch`, `MessageSquareWarning`, `BanknoteArrowDown` are no longer referenced in `app/page.tsx`.
    - `Check` is still referenced in the payment-tracking section, reply-threading section, and pricing section.
    - The import statement is reduced to `import { Check } from 'lucide-react'`.
  </behavior>
  <action>
Edit `app/page.tsx`:

**Step 1 — Verify which icons remain in use.** Run:
```bash
for icon in ShieldCheck FileSearch MessageSquareWarning BanknoteArrowDown Check; do
  echo -n "$icon: "; grep -c "$icon" app/page.tsx
done
```

Expected (after Tasks 1+2):
- `ShieldCheck: 1` (only the import line)
- `FileSearch: 1` (only the import line)
- `MessageSquareWarning: 1` (only the import line)
- `BanknoteArrowDown: 1` (only the import line)
- `Check: 4` (the import + three call sites in payment-tracking / reply-threading / pricing)

If any of the first four icons shows >1, search for where it's still used and either (a) keep that icon in the import, or (b) remove the residual usage if it's a leftover — report which path was taken in the SUMMARY. Default expectation is (a).

**Step 2 — Rewrite the import line.** Replace:
```tsx
import { Check, ShieldCheck, FileSearch, MessageSquareWarning, BanknoteArrowDown } from 'lucide-react'
```
with:
```tsx
import { Check } from 'lucide-react'
```

If any of the four old icons still has a non-import usage after Tasks 1+2 (verified in Step 1), keep that icon in the import.

Do NOT touch any other import line. Specifically `Link` from `next/link`, `useEffect`/`useRef`/`useState` from `react`, `dynamic` from `next/dynamic`, `DEFENSE_TOOLS`, `PLANS`, `PushbackHero`, `Footer` all stay.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "app/page.tsx" | grep -v '^#' | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - Lucide import is minimal: `grep -c "^import { Check } from 'lucide-react'$" app/page.tsx` returns `1`
    - ShieldCheck gone: `grep -c "ShieldCheck" app/page.tsx` returns `0`
    - FileSearch gone: `grep -c "FileSearch" app/page.tsx` returns `0`
    - MessageSquareWarning gone: `grep -c "MessageSquareWarning" app/page.tsx` returns `0`
    - BanknoteArrowDown gone: `grep -c "BanknoteArrowDown" app/page.tsx` returns `0`
    - Check still imported AND used: `grep -c "Check" app/page.tsx` returns at least `4`
    - eslint reports no unused-imports for this file: `npx eslint app/page.tsx --quiet 2>&1 | grep -E "(no-unused-vars|@typescript-eslint/no-unused-vars)" | wc -l | tr -d ' '` returns `0`
    - tsc clean: `npx tsc --noEmit 2>&1 | grep "app/page.tsx" | grep -v '^#' | wc -l | tr -d ' '` returns `0`
  </acceptance_criteria>
  <done>
    `app/page.tsx` imports only `Check` from `lucide-react`. The four card-icon imports are gone. eslint + tsc both pass. No other imports in the file are modified.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Mobile lighthouse audit confirms D-15 (perf ≥85)</name>
  <what-built>
    Phase 16 is complete: editorial hero (gradient + contract-page SVG, serif italic pillar headline), numbered editorial pillar list, no ticker, no card chrome, no WebGL canvas, no continuously-running RAF.
  </what-built>
  <how-to-verify>
    1. Build and start the production server locally:
       ```
       npm run build
       npm run start
       ```
       (Or skip to a deployed Vercel preview URL if available.)

    2. Open Chrome / Chromium DevTools → Lighthouse panel.

    3. Configure the audit:
       - Mode: Navigation
       - Device: Mobile
       - Categories: Performance ONLY (other categories are not the success-criteria gate here)
       - Throttling: Lighthouse default (simulated slow 4G + 4x CPU slowdown — the default for the mobile preset)

    4. Run the audit against `http://localhost:3000/` (or your preview URL).

    5. Confirm the Performance score is **≥ 85** per D-15.

    6. Additional manual sanity checks (no separate scoring, but confirm before approving):
       - The four pillar lines (VET. / SIGN. / REPLY. / RECOVER.) render in Instrument Serif italic on mobile + desktop.
       - The hero shows the gradient + tilted contract-page artifact on desktop; mobile shows the gradient only (no artifact visible).
       - The "What's inside" section is a numbered list (01. 02. 03. 04. in serif lime) with no card backgrounds.
       - The ticker section is gone — the next section after the editorial pillar list is the LiveDemo, with no scrolling-strip interlude.
       - The 3D coverflow carousel, payment-tracking mockup, document-generation cards, reply-threading section, and pricing section are all unchanged.
       - No JavaScript console errors on initial load.
       - Tabbing through the page reaches each pillar word and lights it up in lime.

    7. Reply with the actual Performance score (e.g. "92") OR with a list of issues found.
  </how-to-verify>
  <resume-signal>Reply "approved – lighthouse mobile perf [SCORE]" or describe any issues. If perf is below 85, list which audit items dragged it down (LCP, TBT, CLS, etc.) so a follow-up plan can address them.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser render | All pillar-list content is hardcoded module-scope literals — no user input. Identical posture to previous 4-card grid. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.03-01 | Accessibility regression | numbered <ol> with aria-hidden numerals | mitigate | Semantic `<ol>` element guarantees screen readers announce the ordered sequence regardless of whether the visual numeral is hidden. Numeral span has `aria-hidden="true"` so it is not double-announced. D-18 mitigation. |
| T-16.03-02 | Performance regression | new layout shift from list rewrite | accept | The list has stable known dimensions (each row is fixed height per content; no images loaded lazily). CLS risk minimal. Lighthouse run in Task 4 confirms numerically. |
| T-16.03-03 | Dead code | unused Lucide icons in bundle | mitigate | Task 3 prunes the import; bundler tree-shakes the now-unreferenced icons. Acceptance criterion grep-verifies the import line is minimal. |
| T-16.03-04 | Dead code | unused .ticker-track CSS rule | mitigate | Task 2 deletes the rule iff no other consumer references it. SUMMARY documents the outcome. |
| T-16.03-05 | Regression in untouched sections | accidental edits to ToolCarousel/pricing/etc. | mitigate | Acceptance criteria spot-check that ToolCarousel function, LiveDemo render, ContractReveal render, and pricing copy ("30-day money-back") all remain. D-11/D-12/D-13. |
| T-16.03-06 | Build failure | Instrument Serif loaded but not consumed correctly | accept | Task 1's numeral span uses `var(--font-serif)`. If Plan 16-01 did not actually wire the variable onto `<html>`, the numeral will fall back to system serif. Plan 16-01 is a hard dependency and its acceptance criteria gate this — no separate handling needed here. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with zero errors for `app/page.tsx`.
- `npx eslint app/page.tsx --quiet` returns 0 errors.
- `grep -rn "ticker-track\\|ticker-scroll\\|TICKER_ITEMS" app/ components/ lib/ types/ 2>/dev/null` returns no matches.
- `npm run build` completes; bundle for the landing page does not contain the four pruned Lucide icon paths.
- Lighthouse mobile performance score ≥85 (human-verified in Task 4).
</verification>

<success_criteria>
- D-07 / D-08 / D-09 met: numbered `<ol>` with serif lime numerals, no card chrome, hairline dividers, max-w-3xl column.
- D-10 met: ticker JSX + data + CSS removed (CSS conditionally on no-other-consumer).
- D-13 met: every copy string in the rewritten section is byte-identical to the previous section.
- D-15 met: mobile lighthouse performance ≥85 (human-verified).
- D-18 met: semantic `<ol>` + `<li>` with `aria-hidden` numerals.
- No other section of `app/page.tsx` is modified.
- The Lucide import is reduced to `Check` only.
</success_criteria>

<output>
After completion, write `.planning/phases/16-editorial-landing-page-redesign/16-03-SUMMARY.md` covering:
- The exact `<ol>` JSX block that replaced the grid (paste it).
- Whether the `.ticker-track` CSS was deleted from `app/globals.css` or left in place (and if left, which file blocked the cleanup).
- The exact pruned Lucide import line.
- The lighthouse mobile performance score reported in Task 4.
- Any deviations from the spec.
</output>
