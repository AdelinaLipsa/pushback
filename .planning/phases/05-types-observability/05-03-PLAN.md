---
phase: 05-types-observability
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - instrumentation.ts
  - instrumentation-client.ts
  - sentry.server.config.ts
  - next.config.ts
  - .env.example
user_setup:
  - service: sentry
    why: "Error capture in Route Handlers with request context (OBS-01)"
    env_vars:
      - name: NEXT_PUBLIC_SENTRY_DSN
        source: "Sentry Dashboard — Create Next.js project then Settings → Client Keys (DSN)"
      - name: SENTRY_AUTH_TOKEN
        source: "Sentry Dashboard — Settings → Developer Settings → Auth Tokens (needs project:releases scope for source map upload)"
      - name: SENTRY_ORG
        source: "Sentry Dashboard — Settings → General (organization slug)"
      - name: SENTRY_PROJECT
        source: "Sentry Dashboard — Settings → Projects (project slug for the new Next.js project)"
    dashboard_config:
      - task: "Create a new Sentry project of type Next.js"
        location: "https://sentry.io → + Create Project → Platform: Next.js"
      - task: "Copy DSN and auth token into Vercel env vars (Production, Preview, Development)"
        location: "Vercel Dashboard → Project → Settings → Environment Variables"
  - service: anthropic
    why: "Billing threshold alert (OBS-02) — manual dashboard step, no code"
    env_vars: []
    dashboard_config:
      - task: "Configure a $10/month spend alert"
        location: "https://console.anthropic.com → Settings → Billing → Spend alerts"
autonomous: false
requirements:
  - OBS-01
  - OBS-02
tags:
  - observability
  - sentry
  - nextjs-16
  - csp

must_haves:
  truths:
    - "Unhandled errors thrown inside any App Router Route Handler (app/api/**/route.ts) are captured by Sentry with request path, method, and headers context"
    - "Unhandled errors on the client (React render errors in dashboard components) are captured by Sentry"
    - "Stack traces in Sentry show original TypeScript file names and line numbers (source maps uploaded at build time)"
    - "Content-Security-Policy connect-src allows https://sentry.io and https://*.sentry.io — client-side Sentry event POSTs are not blocked"
    - "next.config.ts default export is wrapped with withSentryConfig() — all existing security headers and poweredByHeader:false are preserved"
    - "Performance monitoring and Session Replay are NOT enabled (tracesSampleRate 0, replaysSessionSampleRate 0) per D-09"
    - "Anthropic console has a $10/month billing threshold alert configured (manual step, verified by user)"
  artifacts:
    - path: "instrumentation.ts"
      provides: "Next.js 16 server-side instrumentation hook + onRequestError wired to Sentry"
      contains: "export const onRequestError = Sentry.captureRequestError"
    - path: "instrumentation-client.ts"
      provides: "Next.js 16 client-side Sentry init (replaces legacy sentry.client.config.ts pattern)"
      contains: "Sentry.init"
    - path: "sentry.server.config.ts"
      provides: "Server-side Sentry init loaded dynamically by instrumentation.ts"
      contains: "Sentry.init"
    - path: "next.config.ts"
      provides: "Sentry webpack plugin wrapping existing Next.js config"
      contains: "withSentryConfig"
  key_links:
    - from: "instrumentation.ts"
      to: "sentry.server.config.ts"
      via: "dynamic import under process.env.NEXT_RUNTIME guard"
      pattern: "import.*sentry.server.config"
    - from: "next.config.ts"
      to: "@sentry/nextjs withSentryConfig"
      via: "export default withSentryConfig(nextConfig, ...)"
      pattern: "withSentryConfig"
    - from: "CSP connect-src"
      to: "sentry.io ingest endpoints"
      via: "https://sentry.io https://*.sentry.io tokens in cspHeader"
      pattern: "connect-src.*sentry.io"
---

<objective>
Integrate @sentry/nextjs for production error capture (OBS-01) and document the manual Anthropic billing-threshold alert step (OBS-02). This is the observability slice of Phase 5 — runs parallel to Plan 01 (no dependency on Supabase types).

Purpose:
- OBS-01: Unhandled errors in Route Handlers must appear in Sentry within seconds with request context. @sentry/nextjs auto-instruments via instrumentation.ts + onRequestError hook (Next.js 16 convention) — no manual try/catch wrapping required.
- OBS-02: A $10/month billing threshold alert must be configured in the Anthropic console before deploy — documented here as a blocking checkpoint since no code change satisfies it.

Output:
- @sentry/nextjs added to package.json dependencies
- instrumentation.ts at project root (Next.js 16 convention) with register() importing sentry.server.config.ts and onRequestError = Sentry.captureRequestError
- instrumentation-client.ts at project root (Next.js 15.3+ / 16 convention per node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/instrumentation-client.md) with client Sentry.init
- sentry.server.config.ts at project root with Sentry.init (server DSN)
- next.config.ts wrapped with withSentryConfig(); CSP connect-src extended with https://sentry.io and https://*.sentry.io
- .env.example updated with NEXT_PUBLIC_SENTRY_DSN, SENTRY_AUTH_TOKEN, SENTRY_ORG, SENTRY_PROJECT placeholders
- A blocking checkpoint task confirms the Anthropic spend alert was configured in the dashboard (OBS-02)

Next.js 16 convention note (per AGENTS.md "this is NOT the Next.js you know"): PATTERNS.md suggested a sentry.client.config.ts file at root. Next.js 16 supersedes that with instrumentation-client.ts (introduced Next.js 15.3, confirmed in node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/instrumentation-client.md). @sentry/nextjs supports both patterns; we use instrumentation-client.ts to align with framework conventions and avoid deprecation on future upgrades.

No sentry.edge.config.ts: the codebase does not use edge runtime (proxy.ts runs in the edge-style middleware slot but is handled by @sentry/nextjs's proxy instrumentation separately if needed; no current edge route handlers). If a future task adds edge routes, add instrumentation-edge.ts at that time.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-types-observability/05-CONTEXT.md
@.planning/phases/05-types-observability/05-PATTERNS.md
@next.config.ts
@package.json
@lib/anthropic.ts
@node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/instrumentation.md
@node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/instrumentation-client.md

<interfaces>
Next.js 16 instrumentation.ts API (from node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/instrumentation.md):

export function register(): void | Promise<void>
  // Called once at server start; must complete before server handles requests
  // Runtime detection: process.env.NEXT_RUNTIME === 'nodejs' | 'edge'

export const onRequestError: Instrumentation.onRequestError = async (
  err,
  request,
  context
) => { /* ... */ }
  // Fires on server-captured errors (Route Handlers, Server Components, Server Actions)
  // context.routeType: 'render' | 'route' | 'action' | 'proxy'
  // context.renderSource: 'react-server-components' | 'react-server-components-payload' | 'server-rendering'

Next.js 16 instrumentation-client.ts API (from node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/instrumentation-client.md):

// Top-level side-effect code runs after HTML load, before React hydration.
// Optional export: onRouterTransitionStart(url, navigationType) for nav tracking.
// No register() function required for client-side.

@sentry/nextjs exports we rely on (latest stable, 2026-compatible):
- Sentry.init(options)  — installs SDK
- Sentry.captureRequestError — matches Instrumentation.onRequestError signature, auto-captures the error with request+context as breadcrumbs
- withSentryConfig(nextConfig, sentryBuildOptions)  — wraps next.config.ts for build-time source map upload
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @sentry/nextjs and update .env.example</name>
  <files>
    package.json
    package-lock.json
    .env.example
  </files>
  <read_first>
    - package.json (current dependencies — confirm no @sentry/* already present)
    - .env.example if it exists (ls .env.example; read if present — add new vars while preserving existing entries)
  </read_first>
  <action>
    Step 1 — Install @sentry/nextjs:

    Run: npm install @sentry/nextjs

    This adds @sentry/nextjs to dependencies in package.json. Do not pin a specific version — let npm resolve the latest stable (2026 will produce a v9+ range compatible with Next.js 16.2.4 and React 19).

    Step 2 — Update .env.example:

    If .env.example does not exist, create it. Append (or create with) these four entries. If the file has an existing [Sentry] section, replace it; otherwise add at the bottom:

        # Sentry (OBS-01)
        NEXT_PUBLIC_SENTRY_DSN=
        SENTRY_AUTH_TOKEN=
        SENTRY_ORG=
        SENTRY_PROJECT=

    Preserve all existing entries in .env.example verbatim — do not reorder, rename, or delete any existing env var. Only add the four new ones.

    If .env.example does not exist in the repo at all: create it with just the four Sentry entries under a leading comment header:

        # Environment variables template — copy to .env.local and fill in values
        # Sentry (OBS-01)
        NEXT_PUBLIC_SENTRY_DSN=
        SENTRY_AUTH_TOKEN=
        SENTRY_ORG=
        SENTRY_PROJECT=

    Rationale: NEXT_PUBLIC_SENTRY_DSN must be exposed to the browser bundle (prefix rule from PATTERNS.md "Environment Variable Naming"); SENTRY_AUTH_TOKEN is server-only (never expose to client); SENTRY_ORG + SENTRY_PROJECT are build-time only for withSentryConfig source map upload.

    Step 3 — Commit lockfile. Do not hand-edit package-lock.json; npm install manages it.
  </action>
  <verify>
    Run all four checks — all must pass:
    1. grep -q "@sentry/nextjs" package.json exits 0
    2. test -d node_modules/@sentry/nextjs exits 0
    3. grep -q "NEXT_PUBLIC_SENTRY_DSN=" .env.example exits 0
    4. grep -q "SENTRY_AUTH_TOKEN=" .env.example exits 0
  </verify>
  <acceptance_criteria>
    - grep -c "@sentry/nextjs" package.json >= 1 (in dependencies block)
    - test -d node_modules/@sentry/nextjs exits 0
    - grep -q "^NEXT_PUBLIC_SENTRY_DSN=" .env.example exits 0
    - grep -q "^SENTRY_AUTH_TOKEN=" .env.example exits 0
    - grep -q "^SENTRY_ORG=" .env.example exits 0
    - grep -q "^SENTRY_PROJECT=" .env.example exits 0
    - grep -c "\"dev\": \"next dev\"" package.json >= 1 (existing scripts unchanged)
    - Any pre-existing entries in .env.example are still present (count lines before and after — new total = old total + 4 plus optional comment line)
  </acceptance_criteria>
  <done>@sentry/nextjs installed and locked; .env.example documents all four Sentry env vars with empty values for the developer to populate.</done>
</task>

<task type="auto">
  <name>Task 2: Create instrumentation.ts and sentry.server.config.ts at project root</name>
  <files>
    instrumentation.ts
    sentry.server.config.ts
  </files>
  <read_first>
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/instrumentation.md (register + onRequestError API + NEXT_RUNTIME guard pattern)
    - .planning/phases/05-types-observability/05-PATTERNS.md "instrumentation.ts" and "sentry.server.config.ts" sections
    - .planning/phases/05-types-observability/05-CONTEXT.md D-06 (full @sentry/nextjs not manual wrapper), D-09 (no tracing or replay)
    - lib/anthropic.ts (side-effect SDK init analog pattern)
  </read_first>
  <action>
    Create two new files at the project root — same directory as next.config.ts (NOT inside app/ or src/).

    File 1 — instrumentation.ts (create new):

        import * as Sentry from '@sentry/nextjs'

        export async function register() {
          if (process.env.NEXT_RUNTIME === 'nodejs') {
            await import('./sentry.server.config')
          }
        }

        export const onRequestError = Sentry.captureRequestError

    Notes on this exact content:
    - The top-level import of @sentry/nextjs is required because onRequestError is exported synchronously at module scope.
    - The dynamic import of ./sentry.server.config under the NEXT_RUNTIME === 'nodejs' guard follows the PATTERNS.md and Next.js docs convention — ensures the server SDK loads only in the Node.js runtime, not edge.
    - No sentry.edge.config.ts branch — project has no edge route handlers (confirmed in CONTEXT.md D-discretion).
    - Sentry.captureRequestError has the exact signature of Next.js Instrumentation.onRequestError — no adapter needed.

    File 2 — sentry.server.config.ts (create new):

        import * as Sentry from '@sentry/nextjs'

        Sentry.init({
          dsn: process.env.NEXT_PUBLIC_SENTRY_DSN,
          tracesSampleRate: 0,
          debug: false,
        })

    Notes on this exact content:
    - Side-effect module (no exports) — matches the lib/anthropic.ts analog pattern.
    - dsn uses NEXT_PUBLIC_SENTRY_DSN without the ! assertion — Sentry.init accepts undefined DSN gracefully (disables reporting in local dev without crashing the server). This matches PATTERNS.md "Non-nullable env var assertion" guidance.
    - tracesSampleRate 0 disables performance monitoring per D-09.
    - debug: false — production-only logging to Sentry, no console spam.

    File location rule (from Next.js 16 docs + PATTERNS.md): instrumentation.ts MUST be at the project root alongside next.config.ts, not inside app/. If the project ever adopts a src/ directory layout, both files move into src/ together. Currently the project has no src/ (confirmed by ls — app/ and lib/ are at root).
  </action>
  <verify>
    Run all five checks — all must pass:
    1. test -f instrumentation.ts exits 0
    2. test -f sentry.server.config.ts exits 0
    3. grep -q "export const onRequestError = Sentry.captureRequestError" instrumentation.ts exits 0
    4. grep -q "process.env.NEXT_RUNTIME === 'nodejs'" instrumentation.ts exits 0
    5. grep -q "tracesSampleRate: 0" sentry.server.config.ts exits 0
  </verify>
  <acceptance_criteria>
    - File exists: /Users/adelinalipsa/Documents/GitHub/pushback/instrumentation.ts
    - File exists: /Users/adelinalipsa/Documents/GitHub/pushback/sentry.server.config.ts
    - grep -q "^import \* as Sentry from '@sentry/nextjs'" instrumentation.ts exits 0
    - grep -q "export async function register" instrumentation.ts exits 0
    - grep -q "process.env.NEXT_RUNTIME === 'nodejs'" instrumentation.ts exits 0
    - grep -q "await import('./sentry.server.config')" instrumentation.ts exits 0
    - grep -q "export const onRequestError = Sentry.captureRequestError" instrumentation.ts exits 0
    - grep -c "sentry.edge.config" instrumentation.ts == 0 (no edge branch)
    - grep -q "^import \* as Sentry from '@sentry/nextjs'" sentry.server.config.ts exits 0
    - grep -q "Sentry.init({" sentry.server.config.ts exits 0
    - grep -q "dsn: process.env.NEXT_PUBLIC_SENTRY_DSN" sentry.server.config.ts exits 0
    - grep -q "tracesSampleRate: 0" sentry.server.config.ts exits 0
    - grep -c "export" sentry.server.config.ts == 0 (side-effect module, no exports)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Server-side Sentry wired via Next.js 16 instrumentation.ts. onRequestError hooks every Route Handler error automatically (no manual try/catch changes needed). Edge branch omitted — no edge routes exist.</done>
</task>

<task type="auto">
  <name>Task 3: Create instrumentation-client.ts at project root</name>
  <files>instrumentation-client.ts</files>
  <read_first>
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/instrumentation-client.md (Next.js 16 client-init convention — top-level side-effect code, no register() required)
    - .planning/phases/05-types-observability/05-PATTERNS.md "sentry.client.config.ts" section (target Sentry.init options — content applies; filename differs per Next.js 16 convention)
    - .planning/phases/05-types-observability/05-CONTEXT.md D-08 (client scope captured), D-09 (no replay)
  </read_first>
  <action>
    Create one new file at the project root — instrumentation-client.ts.

    Why instrumentation-client.ts instead of sentry.client.config.ts:
    PATTERNS.md proposed sentry.client.config.ts following older @sentry/nextjs wizard output. Next.js 15.3+ (and 16.x) introduces instrumentation-client.ts as the framework-owned client init hook (see node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/instrumentation-client.md). @sentry/nextjs detects and uses whichever is present — the instrumentation-client.ts path is framework-aligned and future-proof. Per AGENTS.md ("this is NOT the Next.js you know"), align with the current framework convention.

    File content (exact):

        import * as Sentry from '@sentry/nextjs'

        Sentry.init({
          dsn: process.env.NEXT_PUBLIC_SENTRY_DSN,
          tracesSampleRate: 0,
          replaysSessionSampleRate: 0,
          replaysOnErrorSampleRate: 0,
          debug: false,
        })

    Notes on this exact content:
    - Top-level side-effect init — no exports, no wrapping function (per instrumentation-client.md: "Unlike server-side instrumentation, you do not need to export any specific functions").
    - Same DSN env var as server — NEXT_PUBLIC_ prefix makes it available in the browser bundle.
    - replaysSessionSampleRate and replaysOnErrorSampleRate both 0 — Session Replay DISABLED per D-09 (GDPR surface reduction).
    - tracesSampleRate 0 — performance monitoring DISABLED per D-09.
    - debug: false — no console noise in dev.
    - No onRouterTransitionStart export — navigation tracking is not in scope (only error capture per OBS-01).

    File location rule: project root, same directory as next.config.ts. Execution timing (per instrumentation-client.md): after HTML load, before React hydration — correct timing for error-tracking setup.
  </action>
  <verify>
    Run all four checks — all must pass:
    1. test -f instrumentation-client.ts exits 0
    2. grep -q "import \* as Sentry from '@sentry/nextjs'" instrumentation-client.ts exits 0
    3. grep -q "dsn: process.env.NEXT_PUBLIC_SENTRY_DSN" instrumentation-client.ts exits 0
    4. grep -q "replaysSessionSampleRate: 0" instrumentation-client.ts exits 0
  </verify>
  <acceptance_criteria>
    - File exists: /Users/adelinalipsa/Documents/GitHub/pushback/instrumentation-client.ts
    - grep -q "^import \* as Sentry from '@sentry/nextjs'" instrumentation-client.ts exits 0
    - grep -q "Sentry.init({" instrumentation-client.ts exits 0
    - grep -q "tracesSampleRate: 0" instrumentation-client.ts exits 0
    - grep -q "replaysSessionSampleRate: 0" instrumentation-client.ts exits 0
    - grep -q "replaysOnErrorSampleRate: 0" instrumentation-client.ts exits 0
    - grep -c "export" instrumentation-client.ts == 0 (side-effect module)
    - test ! -f sentry.client.config.ts (legacy file NOT created — instrumentation-client.ts replaces it)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Client-side Sentry init aligned with Next.js 16 instrumentation-client.ts convention. Performance monitoring and session replay explicitly disabled. Legacy sentry.client.config.ts NOT created.</done>
</task>

<task type="auto">
  <name>Task 4: Wrap next.config.ts with withSentryConfig and extend CSP connect-src</name>
  <files>next.config.ts</files>
  <read_first>
    - next.config.ts (current state — all 51 lines; must preserve cspHeader, headers() block, poweredByHeader: false)
    - .planning/phases/05-types-observability/05-PATTERNS.md "next.config.ts" section (exact target pattern)
    - .planning/phases/05-types-observability/05-CONTEXT.md D-07 (source map upload requires widenClientFileUpload: true)
    - .planning/STATE.md D-06 CSP notes (existing connect-src includes supabase.co — must be preserved)
  </read_first>
  <action>
    Edit next.config.ts with two coordinated changes: (A) add withSentryConfig wrapper, (B) extend cspHeader connect-src with sentry.io domains.

    Change A — Extend cspHeader connect-src:

    BEFORE (current line 9):

        connect-src 'self' https://*.supabase.co wss://*.supabase.co;

    AFTER (replace that line exactly with):

        connect-src 'self' https://*.supabase.co wss://*.supabase.co https://sentry.io https://*.sentry.io;

    Preserve all other CSP directives verbatim — default-src, script-src, style-src, img-src, font-src, object-src, base-uri, form-action, frame-ancestors, upgrade-insecure-requests. Only the connect-src line changes.

    Change B — Add withSentryConfig wrapper:

    1. Add import after the existing `import type { NextConfig } from 'next'` (line 1):

           import { withSentryConfig } from '@sentry/nextjs'

    2. Change the final `export default nextConfig` (line 50) to:

           export default withSentryConfig(nextConfig, {
             org: process.env.SENTRY_ORG,
             project: process.env.SENTRY_PROJECT,
             silent: !process.env.CI,
             widenClientFileUpload: true,
             hideSourceMaps: true,
             disableLogger: true,
             automaticVercelMonitors: false,
           })

    Rationale for each withSentryConfig option (from D-07 + PATTERNS.md):
    - org + project: read from env at build time — identifies where source maps upload
    - silent: !process.env.CI  — suppress verbose build logs locally, show in CI/Vercel
    - widenClientFileUpload: true — required per D-07 for comprehensive source map upload coverage (default excludes some client chunks)
    - hideSourceMaps: true — maps uploaded to Sentry but stripped from publicly-served bundles (security: do not leak source to browser)
    - disableLogger: true — drop Sentry's bundled logger from the client bundle (smaller payload)
    - automaticVercelMonitors: false — we already have Vercel Analytics / don't auto-create cron monitors

    DO NOT MODIFY:
    - poweredByHeader: false (line 18)
    - The entire headers() async function (lines 19-47) — CSP replacement is INSIDE this function's existing cspHeader template literal; the headers array itself does not change
    - All five header entries (Content-Security-Policy, Strict-Transport-Security, X-Frame-Options, Referrer-Policy, X-Content-Type-Options)
    - cspHeader template literal structure — only the one connect-src line changes

    Build-time behavior note: If SENTRY_ORG, SENTRY_PROJECT, or SENTRY_AUTH_TOKEN are missing at build time, withSentryConfig emits a warning but does NOT fail the build — source maps simply are not uploaded. Vercel build will succeed even before the user sets env vars; source map upload starts working once env vars land.
  </action>
  <verify>
    Run all five checks — all must pass:
    1. grep -q "import { withSentryConfig } from '@sentry/nextjs'" next.config.ts exits 0
    2. grep -q "export default withSentryConfig(nextConfig" next.config.ts exits 0
    3. grep -q "https://sentry.io https://\*.sentry.io" next.config.ts exits 0
    4. grep -q "widenClientFileUpload: true" next.config.ts exits 0
    5. npx tsc --noEmit exits 0 && npm run build exits 0
  </verify>
  <acceptance_criteria>
    - grep -q "^import { withSentryConfig } from '@sentry/nextjs'" next.config.ts exits 0
    - grep -q "^export default withSentryConfig(nextConfig, {" next.config.ts exits 0
    - grep -q "org: process.env.SENTRY_ORG" next.config.ts exits 0
    - grep -q "project: process.env.SENTRY_PROJECT" next.config.ts exits 0
    - grep -q "silent: !process.env.CI" next.config.ts exits 0
    - grep -q "widenClientFileUpload: true" next.config.ts exits 0
    - grep -q "hideSourceMaps: true" next.config.ts exits 0
    - grep -q "disableLogger: true" next.config.ts exits 0
    - grep -q "automaticVercelMonitors: false" next.config.ts exits 0
    - grep -q "connect-src 'self' https://\*.supabase.co wss://\*.supabase.co https://sentry.io https://\*.sentry.io" next.config.ts exits 0
    - grep -q "poweredByHeader: false" next.config.ts exits 0 (preserved)
    - grep -q "Strict-Transport-Security" next.config.ts exits 0 (preserved)
    - grep -q "frame-ancestors 'none'" next.config.ts exits 0 (preserved)
    - grep -c "^export default" next.config.ts == 1 (only the wrapped export exists)
    - npx tsc --noEmit exits 0
    - npm run build exits 0 (Sentry-wrapped config compiles end-to-end; env-var absence is a warning not a failure)
  </acceptance_criteria>
  <done>next.config.ts wraps the existing config with withSentryConfig. All pre-existing security headers and CSP directives are preserved. CSP connect-src now permits client-side Sentry ingestion endpoints. Build succeeds with or without Sentry env vars populated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Manual verification — Sentry project + env vars + deploy + test error capture</name>
  <what-built>
    @sentry/nextjs installed, instrumentation.ts + instrumentation-client.ts + sentry.server.config.ts created, next.config.ts wrapped with withSentryConfig, .env.example documents the four Sentry env vars, CSP permits sentry.io endpoints.

    What this checkpoint verifies: that the wired code actually produces Sentry events in production with usable stack traces — OBS-01's observable truth ("errors visible in Sentry dashboard within seconds of occurring").
  </what-built>
  <how-to-verify>
    Step 1 — Create Sentry project (one-time):
    1. Go to https://sentry.io (create a free account if needed)
    2. Click "+ Create Project"
    3. Platform: Next.js
    4. Project name: pushback (or your preference)
    5. Create project — you will land on the setup page; copy the DSN shown (format: https://abc123@o456.ingest.sentry.io/789)

    Step 2 — Capture env vars:
    - NEXT_PUBLIC_SENTRY_DSN: the DSN from Step 1
    - SENTRY_ORG: organization slug from Sentry URL (sentry.io/organizations/<ORG>/)
    - SENTRY_PROJECT: project slug (created in Step 1)
    - SENTRY_AUTH_TOKEN: Sentry Dashboard → Settings → Developer Settings → Auth Tokens → Create with scope project:releases

    Step 3 — Add env vars to Vercel:
    1. Vercel Dashboard → pushback project → Settings → Environment Variables
    2. Add all four for scopes: Production, Preview, Development
    3. Redeploy latest commit from the Vercel UI (Deployments → latest → ... → Redeploy)

    Step 4 — Trigger a test error from production:
    1. Temporarily add a throw to app/api/projects/[id]/defend/route.ts just inside the try block (or use any protected route):

           throw new Error('Sentry smoke test — delete immediately')

    2. Commit on a throwaway branch, push, wait for Vercel preview deploy
    3. Call the route (e.g., via the UI or `curl -X POST https://<preview>/api/projects/<id>/defend` with a logged-in session)
    4. Within 30 seconds, check Sentry dashboard → Issues → a new issue should appear

    Step 5 — Confirm the captured error:
    - Issue title: "Error: Sentry smoke test — delete immediately"
    - Stack trace shows: app/api/projects/[id]/defend/route.ts:<line> with readable TS code (not minified one-letter-var JS)
    - Request context: method=POST, path=/api/projects/<id>/defend, headers present
    - Environment: production (or preview)

    Step 6 — Revert the test throw:
    Remove the throw, commit, push. Delete the test branch.

    Step 7 — OBS-02 Anthropic spend alert:
    1. Log in to https://console.anthropic.com
    2. Settings → Billing → Spend alerts
    3. Configure: $10/month threshold notification (email to your account address)
    4. Save
    5. Confirm in this checkpoint that the alert is visible in the Spend alerts list.

    Step 8 — Report back:
    Paste the following into the resume signal:
    - Sentry issue URL (redacted org/project OK)
    - Screenshot or note confirming stack trace shows original TS file+line
    - Note confirming $10/month Anthropic spend alert is active
  </how-to-verify>
  <resume-signal>
    Type one of:
    - "approved: sentry-issue=<url>, anthropic-alert=active" — if both OBS-01 (Sentry event captured) and OBS-02 (spend alert configured) are verified
    - "issue: <description>" — if either verification failed; describe the failure (e.g., "DSN rejected", "source maps missing", "Sentry issue has minified stack")
  </resume-signal>
  <acceptance_criteria>
    - Human has confirmed a real test error appeared in the Sentry dashboard (issue URL provided)
    - Human has confirmed the stack trace shows original TypeScript source (not minified JS) — proves source map upload works
    - Human has confirmed the $10/month Anthropic spend alert is configured in console.anthropic.com → Settings → Billing → Spend alerts
    - Test throw has been reverted — no Error('Sentry smoke test ...') remains in app/api/**
    - grep -r "Sentry smoke test" app lib components 2>/dev/null returns no results
  </acceptance_criteria>
  <done>OBS-01 end-to-end verified in production; OBS-02 alert configured. Test error reverted. Phase 5 observability slice fully operational.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Node.js runtime → Sentry ingest | Server errors + request context posted to https://*.sentry.io |
| Browser → Sentry ingest | Client-side errors posted to https://*.sentry.io (gated by CSP connect-src) |
| Build system → Sentry API | Source maps uploaded at build time using SENTRY_AUTH_TOKEN |
| .env.example (repo) → developer machines | Template of required env vars; no secret values committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | I (Information disclosure) | Request headers captured by Sentry.captureRequestError may include Cookie, Authorization | mitigate | Sentry's Next.js integration scrubs Cookie and Authorization headers by default (sendDefaultPii stays at default false — explicit in Sentry.init by omission). SENTRY_AUTH_TOKEN + NEXT_PUBLIC_SENTRY_DSN are the only secrets in env; no PII crosses the boundary unless explicitly added to breadcrumbs. |
| T-05-10 | I (Information disclosure) | Uploaded source maps served to clients could expose source code | mitigate | hideSourceMaps: true in withSentryConfig strips maps from public bundles; maps exist only inside Sentry (access controlled by Sentry org membership). |
| T-05-11 | S (Spoofing) | SENTRY_AUTH_TOKEN compromised — attacker uploads fake source maps or modifies Sentry project | mitigate | Token is server-only env var (no NEXT_PUBLIC_ prefix); stored in Vercel secret store. Scoped to project:releases only — cannot modify other Sentry resources. Rotatable from Sentry dashboard. |
| T-05-12 | T (Tampering) | CSP change (adding sentry.io to connect-src) broadens client outbound allowlist | mitigate | sentry.io + *.sentry.io are narrow, vendor-controlled domains. Adding them does not weaken default-src 'self' or script-src — only outbound fetch for telemetry. No wildcard or data: additions. |
| T-05-13 | D (Denial of service) | Sentry outage blocks error reporting — app should continue serving requests | accept | @sentry/nextjs SDK fails silently on ingest errors; does not block request handling. onRequestError is fire-and-forget (Sentry.captureRequestError returns a void promise not awaited by the framework error boundary). |
| T-05-14 | I (Information disclosure) | Error messages in Sentry issues may leak user data (project titles, client names from notes fields) | accept | Low-severity for v1. No PII surfaces unless a user pastes PII into a situation field that then lands in an error message. Mitigation path for v2: beforeSend hook with regex scrubbing. Documented as accepted risk. |
| T-05-15 | E (Elevation of privilege) | Anthropic billing alert missing → unbounded spend → attacker could drain budget via authenticated abuse | mitigate | OBS-02 spend alert at $10/month gives early warning. Secondary controls already in place: Phase 1 atomic gating (3-response cap per free user), Phase 1 rate limits on defend + analyze routes. Alert is a defense-in-depth layer, not sole defense. |
</threat_model>

<verification>
1. grep -q "@sentry/nextjs" package.json exits 0
2. test -f instrumentation.ts && test -f instrumentation-client.ts && test -f sentry.server.config.ts all exit 0
3. grep -q "withSentryConfig" next.config.ts exits 0
4. grep -q "connect-src 'self' https://\*.supabase.co wss://\*.supabase.co https://sentry.io https://\*.sentry.io" next.config.ts exits 0
5. npm run build exits 0
6. npm run lint exits 0 (no new errors introduced by new files)
7. Human checkpoint (Task 5) approves with Sentry issue URL + Anthropic alert confirmation
</verification>

<success_criteria>
- OBS-01: Server-side unhandled errors in any Route Handler (app/api/**/route.ts) produce a Sentry issue within seconds; stack trace shows original TS source; request path/method/headers visible in issue context
- OBS-01 (client): React render errors in dashboard components produce Sentry issues via instrumentation-client.ts
- OBS-01 (CSP): Client-side Sentry event POSTs are not blocked — verified by successful client error capture in the checkpoint
- OBS-01 (source maps): SENTRY_AUTH_TOKEN + SENTRY_ORG + SENTRY_PROJECT at build time cause maps to upload; hideSourceMaps prevents public exposure
- OBS-02: $10/month Anthropic spend alert visible in console.anthropic.com → Settings → Billing → Spend alerts
- D-09 respected: tracesSampleRate 0 and replaysSessionSampleRate 0 in both server and client inits (no performance monitoring, no session replay)
- D-07 respected: widenClientFileUpload: true in withSentryConfig
- D-06 respected: full @sentry/nextjs integration used — no manual @sentry/node wrapping in route handlers
- Existing Phase 2 security headers (HSTS, X-Frame-Options, Referrer-Policy, X-Content-Type-Options, CSP for non-connect-src directives, poweredByHeader:false) preserved verbatim
</success_criteria>

<output>
After completion, create .planning/phases/05-types-observability/05-03-SUMMARY.md documenting:
- Installed @sentry/nextjs version (from package-lock.json)
- Confirmation that instrumentation.ts + instrumentation-client.ts + sentry.server.config.ts exist (paths + sizes)
- Confirmation that sentry.client.config.ts was NOT created (Next.js 16 convention divergence from PATTERNS.md)
- Sentry issue URL from the smoke test (Task 5 checkpoint)
- Screenshot filename or note confirming source map restoration in stack trace
- Confirmation that Anthropic $10/month spend alert is active
- npm run build output confirming withSentryConfig compiles
- Any accepted risks carried forward (e.g., sendDefaultPii default handling, beforeSend scrubbing for v2)
</output>
