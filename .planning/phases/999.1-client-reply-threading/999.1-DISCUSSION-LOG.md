# Phase 999.1: Client Reply Threading - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-26
**Phase:** 999.1-client-reply-threading
**Areas discussed:** Free-tier access, UI entry point, Risk signal display, Regeneration

---

## Free-Tier Access

| Option | Description | Selected |
|--------|-------------|----------|
| Consumes credit pool | Same gate as analyze-message — free users spend their 1 credit on whichever AI call they make first. Simpler code, consistent mental model. | ✓ |
| Pro-only | Free users see locked/upgrade state. One extra plan check before RPC gate. | |
| Completely free | No credits consumed. Risky — free users could abuse for unlimited Anthropic calls. | |

**User's choice:** Consumes credit pool (recommended)
**Notes:** Consistent with how analyze-message works. No separate gating mechanism needed.

---

## UI Entry Point

| Option | Description | Selected |
|--------|-------------|----------|
| History page only | Textarea in expanded ResponseHistory card on /projects/[id]/history | ✓ |
| History page + project detail inline | Also shows "Did they reply?" section below ResponseOutput after marking sent | |

**Visibility:** Always visible (not conditional on was_sent) ✓

**Expand style:** Collapsed by default, expands inline on click ✓

**User's choice:** History page only, always visible, collapsed inline
**Notes:** Natural return point after sending a message; simpler than also surfacing on project detail page.

---

## Risk Signal Display

| Option | Description | Selected |
|--------|-------------|----------|
| Color-coded pill badge | Small pill with lime/orange/red colors matching urgency system | |
| Text label only | Colored text, no badge | |
| Icon + label | Lucide icon next to text label | ✓ |

**Icon choice:** Claude's discretion (planner picks semantically appropriate Lucide icons)

**Follow-up display:** Same pattern as ResponseOutput — pre-formatted text block with CopyButton, shown immediately below signal label ✓

**User's choice:** Lucide icon + text label, Claude picks icons, follow-up visible immediately
**Notes:** More expressive than a pill badge; consistent with Lucide icon usage throughout the app.

---

## Regeneration

| Option | Description | Selected |
|--------|-------------|----------|
| No — one-shot in v1 | No regenerate button; unique constraint enforces single reply per defense response | ✓ |
| Yes — allow regeneration | Regenerate button replaces stored row; costs another credit | |

**User's choice:** One-shot in v1
**Notes:** Keeps v1 simple. Unique index on defense_response_id enforces this at DB level. Regeneration noted as v2 option.

---

## Claude's Discretion

- Exact Lucide icon names for each stance (e.g. TrendingDown, ChevronDown, AlertTriangle, HelpCircle)
- Exact label text per stance ("Backing down", "Doubling down", "Escalating", "Unclear")
- Minor expand/collapse animation style for the reply textarea

## Deferred Ideas

- Multi-turn conversation threading (v2) — allow multiple reply rounds per defense response
- Regeneration of follow-up (v2) — cost another credit, replace stored thread
- Pro-only gating for reply threading — one extra plan check if product direction changes
