---
plan: 999.1-02
phase: 999.1
status: complete
completed_at: 2026-04-26
self_check: PASSED
---

# Plan 999.1-02 Summary: POST /api/responses/[id]/reply

## What Was Built

`POST /api/responses/{id}/reply` — full API route for client reply threading. Authenticates, rate-limits, gates credits atomically, fetches parent IDOR-safely, calls Anthropic with two-message context, validates JSON output, persists to `reply_threads`, handles 23505 race idempotently.

Also regenerated `types/database.types.ts` to include the new `reply_threads` table.

## Key Files

### Created
- `app/api/responses/[id]/reply/route.ts` — POST handler (~170 lines)

### Updated
- `types/database.types.ts` — regenerated via `supabase gen types` to include `reply_threads`

## Compensating Decrement Sites (D-11)

8 total `decrement_defense_responses` calls (>= 6 required):
1. Zod input validation failure
2. defense_response not found (IDOR-safe 404)
3. Anthropic slot busy (503)
4. extractJson throws
5. Output schema (replyResponseSchema) validation fails
6. 23505 conflict → return existing row (refund second attempt)
7. Insert failure (non-23505)
8. Outer catch-all

## Smoke Test Results (Task 2 — Manual Checkpoint)

- Step 5: 200 happy path — `{ id, risk_signal, signal_explanation, follow_up }` ✓
- Step 6: Double-submit returned 200 with same `id` (D-14 idempotency) ✓
- Step 7: Short input returned 400 Zod error ✓
- Step 8: Cross-tenant id returned 404 ✓
- Step 9: Row confirmed in Supabase Studio `reply_threads` ✓
- User approved ✓

## Endpoint Contract for Plan 03

```
POST /api/responses/{id}/reply
Body: { client_reply: string }  // 10-5000 chars
200: { id, risk_signal, signal_explanation, follow_up }
400: { error: "client_reply: ..." }  // Zod validation
401: { error: "Unauthorized" }
403: { error: "UPGRADE_REQUIRED" }  // or RPC reason
404: { error: "Not found" }  // IDOR-safe
500: { error: "AI analysis failed — please try again" }
500: { error: "Failed to save reply — your credit was not used. Please try again." }
```

## Deviations
None — D-01/D-10/D-11/D-12/D-13/D-14/D-15 all followed exactly.
