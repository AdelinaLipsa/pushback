---
phase: 04-missing-ui
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - components/ui/dialog.tsx
  - lib/ui.ts
  - app/globals.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "shadcn Dialog component exists at components/ui/dialog.tsx and can be imported"
    - "lib/ui.ts exports btnStyles, inputStyle, labelStyle, dialogContentStyle with exact values"
    - "--brand-lime CSS variable is defined in app/globals.css :root block"
  artifacts:
    - path: "components/ui/dialog.tsx"
      provides: "shadcn Dialog primitives (Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription)"
    - path: "lib/ui.ts"
      provides: "Shared inline-style constants for Wave 1 components"
    - path: "app/globals.css"
      provides: "--brand-lime: #84cc16 in :root block"
  key_links:
    - from: "components/project/ProjectHeader.tsx (Wave 1)"
      to: "lib/ui.ts"
      via: "named imports: btnStyles, inputStyle, labelStyle, dialogContentStyle"
      pattern: "from '@/lib/ui'"
    - from: "components/contract/ContractDeleteButton.tsx (Wave 1)"
      to: "lib/ui.ts"
      via: "named imports: btnStyles, dialogContentStyle"
      pattern: "from '@/lib/ui'"
---

<objective>
Install the shadcn Dialog component and create the shared style constants lib — both prerequisites that Wave 1 plans depend on.

Purpose: Wave 1 plans (04-02 and 04-03) import Dialog from @/components/ui/dialog and style constants from @/lib/ui.ts. Neither exists yet. This Wave 0 plan creates them so Wave 1 can execute in parallel without module-not-found errors.

Output:
- components/ui/dialog.tsx — shadcn Dialog installed via CLI
- lib/ui.ts — shared btnStyles, inputStyle, labelStyle, dialogContentStyle
- app/globals.css — --brand-lime: #84cc16 added to :root block
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-missing-ui/04-UI-SPEC.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn Dialog and add --brand-lime to globals.css</name>
  <files>components/ui/dialog.tsx, app/globals.css</files>
  <read_first>
    - app/globals.css (already read — :root block lines 60-105, --brand-lime is NOT present)
    - components/ui/ (already confirmed: only button.tsx and sonner.tsx exist — dialog.tsx missing)
  </read_first>
  <action>
Run the shadcn CLI to install the Dialog component:

```bash
npx shadcn add dialog
```

This creates components/ui/dialog.tsx. Accept all prompts (no interactive input needed for a component already scaffolded in components.json).

After the CLI completes, read the installed components/ui/dialog.tsx to confirm the exact export names (Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter). These names are needed by Wave 1 tasks.

Then open app/globals.css and add ONE line to the :root block. The :root block currently ends at line 105 (just before the closing `}`). Insert between `--text-muted: #52525b;` (line 104) and the closing `}` (line 105):

```css
  --brand-lime: #84cc16;
```

The resulting :root block tail should look like:
```css
  --text-muted: #52525b;
  --brand-lime: #84cc16;
}
```

Do NOT modify the @theme inline block (lines 7-58) — only the :root block needs the change. The @theme inline block uses `--color-*` prefixed variables which are different from the `--*` CSS custom properties used by inline styles in components.
  </action>
  <verify>
    - `ls components/ui/dialog.tsx` exits 0
    - `grep -c "DialogContent" components/ui/dialog.tsx` returns a number > 0
    - `grep "brand-lime" app/globals.css` outputs `--brand-lime: #84cc16;`
    - `grep "brand-lime" app/globals.css | head -1` appears inside the :root block (after `--text-muted` line)
  </verify>
  <done>
    - components/ui/dialog.tsx exists and exports Dialog, DialogContent, DialogHeader, DialogTitle at minimum
    - app/globals.css :root block contains --brand-lime: #84cc16
  </done>
</task>

<task type="auto">
  <name>Task 2: Create lib/ui.ts with shared style constants</name>
  <files>lib/ui.ts</files>
  <read_first>
    - components/project/NewProjectForm.tsx (already read — inputStyle lines 49-53, labelStyle lines 55-57)
    - .planning/phases/04-missing-ui/04-UI-SPEC.md (already read — Button Variants section, Modal/Dialog Contract)
    - .planning/codebase/CONVENTIONS.md (already read — inline styles pattern, named exports from lib files)
  </read_first>
  <action>
Create lib/ui.ts as a new file with the following EXACT content. This file uses named exports (matching lib module convention — no default export).

Important: use the name `btnStyles` NOT `buttonVariants` — `buttonVariants` is already exported from `components/ui/button.tsx` as a cva() function for Tailwind classes. Using the same name would cause confusion if both are imported in the same file.

The accent color for primary button, input focus rings, nudge strip border, and upgrade CTA text uses `var(--brand-lime)` per user override (not `var(--brand-amber)` as written in the UI-SPEC).

```typescript
// Shared inline-style constants for Phase 4 components.
// Import from here instead of duplicating per-component.
// Convention: named exports only (lib module pattern).

export const inputStyle: React.CSSProperties = {
  width: '100%',
  backgroundColor: 'var(--bg-base)',
  border: '1px solid var(--bg-border)',
  borderRadius: '0.5rem',
  padding: '0.75rem',
  color: 'var(--text-primary)',
  fontSize: '0.9rem',
  outline: 'none',
}

export const labelStyle: React.CSSProperties = {
  display: 'block',
  color: 'var(--text-secondary)',
  fontSize: '0.85rem',
  marginBottom: '0.5rem',
}

export const btnStyles = {
  primary: {
    backgroundColor: 'var(--brand-lime)',
    color: '#0a0a0a',
    fontWeight: 700,
    padding: '0.75rem 1.5rem',
    borderRadius: '0.5rem',
    border: 'none',
    fontSize: '0.9rem',
    cursor: 'pointer',
  } as React.CSSProperties,
  ghost: {
    background: 'none',
    border: 'none',
    cursor: 'pointer',
    color: 'var(--text-muted)',
    fontSize: '0.85rem',
    padding: '0.5rem 0.75rem',
  } as React.CSSProperties,
  outline: {
    background: 'none',
    border: '1px solid var(--bg-border)',
    borderRadius: '0.5rem',
    padding: '0.5rem 1rem',
    color: 'var(--text-muted)',
    fontSize: '0.85rem',
    cursor: 'pointer',
  } as React.CSSProperties,
  destructive: {
    backgroundColor: 'var(--urgency-high-dim)',
    border: '1px solid var(--urgency-high)',
    borderRadius: '0.5rem',
    padding: '0.5rem 1rem',
    color: 'var(--urgency-high)',
    fontSize: '0.9rem',
    fontWeight: 700,
    cursor: 'pointer',
  } as React.CSSProperties,
}

export const dialogContentStyle: React.CSSProperties = {
  backgroundColor: 'var(--bg-elevated)',
  border: '1px solid var(--bg-border)',
  borderRadius: '0.875rem',
  padding: '1.5rem',
  maxWidth: '440px',
  width: '100%',
}
```

Note: `React.CSSProperties` type annotation requires React to be in scope as a type import. Add `import type React from 'react'` at the top of the file (type-only import, no runtime cost).

Full file with import:

```typescript
import type React from 'react'

export const inputStyle: React.CSSProperties = {
  width: '100%',
  backgroundColor: 'var(--bg-base)',
  border: '1px solid var(--bg-border)',
  borderRadius: '0.5rem',
  padding: '0.75rem',
  color: 'var(--text-primary)',
  fontSize: '0.9rem',
  outline: 'none',
}

export const labelStyle: React.CSSProperties = {
  display: 'block',
  color: 'var(--text-secondary)',
  fontSize: '0.85rem',
  marginBottom: '0.5rem',
}

export const btnStyles = {
  primary: {
    backgroundColor: 'var(--brand-lime)',
    color: '#0a0a0a',
    fontWeight: 700,
    padding: '0.75rem 1.5rem',
    borderRadius: '0.5rem',
    border: 'none',
    fontSize: '0.9rem',
    cursor: 'pointer',
  } as React.CSSProperties,
  ghost: {
    background: 'none',
    border: 'none',
    cursor: 'pointer',
    color: 'var(--text-muted)',
    fontSize: '0.85rem',
    padding: '0.5rem 0.75rem',
  } as React.CSSProperties,
  outline: {
    background: 'none',
    border: '1px solid var(--bg-border)',
    borderRadius: '0.5rem',
    padding: '0.5rem 1rem',
    color: 'var(--text-muted)',
    fontSize: '0.85rem',
    cursor: 'pointer',
  } as React.CSSProperties,
  destructive: {
    backgroundColor: 'var(--urgency-high-dim)',
    border: '1px solid var(--urgency-high)',
    borderRadius: '0.5rem',
    padding: '0.5rem 1rem',
    color: 'var(--urgency-high)',
    fontSize: '0.9rem',
    fontWeight: 700,
    cursor: 'pointer',
  } as React.CSSProperties,
}

export const dialogContentStyle: React.CSSProperties = {
  backgroundColor: 'var(--bg-elevated)',
  border: '1px solid var(--bg-border)',
  borderRadius: '0.875rem',
  padding: '1.5rem',
  maxWidth: '440px',
  width: '100%',
}
```
  </action>
  <verify>
    - `grep "export const btnStyles" lib/ui.ts` returns a match
    - `grep "export const inputStyle" lib/ui.ts` returns a match
    - `grep "export const labelStyle" lib/ui.ts` returns a match
    - `grep "export const dialogContentStyle" lib/ui.ts` returns a match
    - `grep "brand-lime" lib/ui.ts` returns `backgroundColor: 'var(--brand-lime)',` in primary variant
    - `grep "buttonVariants" lib/ui.ts` returns NO match (name collision prevented)
    - `npm run build 2>&1 | grep "lib/ui"` should show no errors for this file
  </verify>
  <done>
    - lib/ui.ts exists with all four named exports
    - btnStyles.primary uses var(--brand-lime) not var(--brand-amber)
    - No export named buttonVariants in lib/ui.ts
    - File compiles without TypeScript errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build toolchain | npx shadcn downloads and writes files to components/ui/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | npx shadcn add dialog | accept | Official shadcn CLI, pinned to shadcn@^4.4.0 in devDependencies; output is a local file written to components/ui/ |
</threat_model>

<verification>
After both tasks complete:
1. `ls components/ui/dialog.tsx` — file exists
2. `grep "brand-lime" app/globals.css` — outputs `--brand-lime: #84cc16;`
3. `grep "export const btnStyles" lib/ui.ts` — match found
4. `grep "export const inputStyle" lib/ui.ts` — match found
5. `grep "export const dialogContentStyle" lib/ui.ts` — match found
6. `grep "brand-lime" lib/ui.ts` — match found in primary variant
7. `npm run build` — no TypeScript errors in lib/ui.ts or app/globals.css
</verification>

<success_criteria>
- components/ui/dialog.tsx is installed and exports Dialog, DialogContent, DialogHeader, DialogTitle
- lib/ui.ts exports btnStyles (with primary/ghost/outline/destructive variants using brand-lime for primary), inputStyle, labelStyle, dialogContentStyle
- app/globals.css :root block contains --brand-lime: #84cc16
- Wave 1 plans can import from @/components/ui/dialog and @/lib/ui without module-not-found errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-missing-ui/04-01-SUMMARY.md`
</output>
