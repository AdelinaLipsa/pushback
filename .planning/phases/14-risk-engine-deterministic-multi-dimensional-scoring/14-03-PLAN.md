---
phase: 14-risk-engine-deterministic-multi-dimensional-scoring
plan: 03
type: execute
wave: 3
depends_on:
  - 14-02
files_modified:
  - components/project/ClientRiskBadge.tsx
  - components/project/ClientBehaviorCard.tsx
  - components/project/ProjectCard.tsx
  - components/project/ProjectDetailClient.tsx
  - app/(dashboard)/dashboard/page.tsx
autonomous: true
requirements:
  - PHASE-14 (no REQ-IDs mapped — addresses ROADMAP success criteria 3, 4, 5; D-21, D-22, D-23)
must_haves:
  truths:
    - "ProjectCard renders the new composite score and level via the existing ClientRiskBadge — visual pill style unchanged from Phase 12 D-04"
    - "ClientBehaviorCard on the project detail page renders four stacked horizontal bars (composite + payment + scope + chargeback) with the locked level colors"
    - "ClientBehaviorCard renders the per-signal evidence table below the bars: columns Signal | Source | Points (D-23, success criteria 4)"
    - "Dashboard shows a top-risk insight surfacing the top 3 clients by composite, each with their level pill and the deterministic nextAction sentence"
    - "The Phase 12 'Needs Attention' single-row risk injection from app/(dashboard)/dashboard/page.tsx is replaced by the new top-3 insight; ghost-client/overdue/stalled attention items still render unchanged"
    - "Every consumer imports from '@/lib/risk' — not from '@/lib/clientRisk' (Plan 04 keeps that path alive as a compat shim, but new UI imports the new module directly)"
  artifacts:
    - path: "components/project/ClientRiskBadge.tsx"
      provides: "Updated to consume RiskLevel from '@/lib/risk' (amber replaces yellow). Visual pill unchanged."
      exports: ["default"]
      min_lines: 20
    - path: "components/project/ClientBehaviorCard.tsx"
      provides: "Expanded card: composite bar, three dimension bars (payment/scope/chargeback), per-signal evidence table, deterministic nextAction line, optional topMitigation callout when red"
      exports: ["default"]
      min_lines: 180
    - path: "components/project/ProjectCard.tsx"
      provides: "Switched from computeClientRisk to computeRisk; passes composite + level to ClientRiskBadge"
      exports: ["default"]
      min_lines: 80
    - path: "components/project/ProjectDetailClient.tsx"
      provides: "Switched from computeClientRisk to computeRisk; passes the full RiskResult to ClientBehaviorCard"
      exports: ["default"]
      min_lines: 100
    - path: "app/(dashboard)/dashboard/page.tsx"
      provides: "Top Risk insight section showing top-3 clients by composite with level pills + nextAction; the existing single-row 'client-risk' AttentionAlert is removed in favor of this dedicated section"
      exports: ["default"]
      min_lines: 400
  key_links:
    - from: "components/project/ProjectCard.tsx"
      to: "lib/risk/index.ts"
      via: "import { computeRisk } from '@/lib/risk'"
      pattern: "from '@/lib/risk'"
    - from: "components/project/ProjectDetailClient.tsx"
      to: "lib/risk/index.ts"
      via: "import { computeRisk } from '@/lib/risk'"
      pattern: "from '@/lib/risk'"
    - from: "app/(dashboard)/dashboard/page.tsx"
      to: "lib/risk/index.ts"
      via: "import { computeRisk, RISK_LEVEL_COLORS } from '@/lib/risk'"
      pattern: "from '@/lib/risk'"
    - from: "components/project/ClientBehaviorCard.tsx"
      to: "lib/risk/index.ts"
      via: "RiskResult / RiskLevel / RiskSignal types"
      pattern: "from '@/lib/risk'"
---

<objective>
Rewire the three Phase 12 UI surfaces to consume the new Phase 14 engine output, and add the new dashboard top-3 risk insight (D-21). Each surface picks up exactly the data it needs from the locked D-06 `RiskResult` shape — no client-side recomputation (D-23: signal arrays render straight from engine output).

Purpose: This is where the engine becomes visible. ProjectCard gets the new composite. The project detail page gets per-dimension bars + the evidence table that satisfies success criteria 4 (auditability). The dashboard gets the new top-3 panel that satisfies success criteria 3 + 5 (composite + traffic light + recommended action).

Output: Two existing components rewritten (ClientBehaviorCard, ClientRiskBadge), two existing consumers swapped (ProjectCard, ProjectDetailClient), and the dashboard page extended with the new insight. No new routes (D-22).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-risk-engine-deterministic-multi-dimensional-scoring/14-CONTEXT.md
@.planning/phases/14-risk-engine-deterministic-multi-dimensional-scoring/14-02-SUMMARY.md
@.planning/phases/12-client-risk/12-UI-SPEC.md
@components/project/ClientRiskBadge.tsx
@components/project/ClientBehaviorCard.tsx
@components/project/ProjectCard.tsx
@components/project/ProjectDetailClient.tsx
@app/(dashboard)/dashboard/page.tsx

<interfaces>
<!-- The public surface of @/lib/risk after Plan 02 ships. -->

```typescript
import {
  computeRisk,
  RISK_LEVEL_COLORS,            // { green: '#22c55e', amber: '#f97316', red: '#ef4444' }
  LEVEL_THRESHOLDS,             // { amber: 26, red: 66 }
  COMPOSITE_WEIGHTS,
  levelFromScore,
  type RiskResult,
  type RiskSignal,
  type RiskLevel,
  type RiskDimension,
  type DimensionScore,
} from '@/lib/risk'

// RiskResult shape (locked D-06):
type RiskResult = {
  composite: number              // 0–100 int
  level: 'green' | 'amber' | 'red'
  nextAction: string             // one sentence, deterministic
  dimensions: {
    payment:    { score: number; signals: RiskSignal[] }
    scope:      { score: number; signals: RiskSignal[] }
    chargeback: { score: number; signals: RiskSignal[] }
  }
  topMitigation: { dimension: RiskDimension; action: string; deltaPoints: number } | null
}
type RiskSignal = { code: string; label: string; points: number; source: 'projects' | 'responses' | 'contracts' }
```

Phase 12 ClientRiskBadge (existing): takes `{ score, level }`. Visual pill: dark translucent background, 1px border in level color, level-color text, "Client 47" label. Keep exactly this shape — only the `level` type widens to include `'amber'` instead of `'yellow'`.

Phase 12 ClientBehaviorCard (existing): renders one header row (clientName + score + label) plus a vertical signal list with Lucide icons resolved by name. This is the surface we expand.

Existing dashboard top-risk injection (lines 397-419 of app/(dashboard)/dashboard/page.tsx): picks the single highest computeClientRisk project with score > 25 and injects an extra AttentionAlert row with `severity: 'client-risk'` and a colored left border. Replace this with a dedicated panel sitting **above** "Needs attention" so the two are visually distinct.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ClientRiskBadge + ClientBehaviorCard to consume RiskResult</name>
  <files>components/project/ClientRiskBadge.tsx, components/project/ClientBehaviorCard.tsx</files>
  <read_first>
    - components/project/ClientRiskBadge.tsx (current 23-line pill)
    - components/project/ClientBehaviorCard.tsx (current 110-line card)
    - .planning/phases/14-risk-engine-deterministic-multi-dimensional-scoring/14-CONTEXT.md (D-21: bars stacked vertically; D-23: per-signal table required; D-22: nextAction is text only)
    - .planning/phases/12-client-risk/12-UI-SPEC.md (visual carryover rules)
    - lib/clientRisk.ts (the icon-resolution helper pattern using lucide-react)
  </read_first>
  <action>
**ClientRiskBadge.tsx** — minimal change. Replace `import { CLIENT_RISK_COLORS, type ClientRiskLevel } from '@/lib/clientRisk'` with `import { RISK_LEVEL_COLORS, type RiskLevel } from '@/lib/risk'`. Rename the prop type to `level: RiskLevel`. The visible label stays "Client {score}". The pill geometry and font sizes do not change (Phase 12 D-04 visual lock).

**ClientBehaviorCard.tsx** — substantial expansion. New prop type: `{ risk: RiskResult; clientName: string }`. Use the existing `resolveIcon(name)` helper pattern for any Lucide icons referenced inside signal labels (no change to that helper).

Layout (top to bottom inside the card):

1. **Header row** — same as Phase 12: "Client Risk · {clientName}" on the left, "{composite}" big number in level color, "· {LEVEL_LABEL}" muted suffix. `LEVEL_LABEL` is now derived locally inside this file as `const LEVEL_LABELS: Record<RiskLevel, string> = { green: 'Healthy', amber: 'Watch closely', red: 'Act now' }` (D-21 expands the prior copy; lock these strings for v1).

2. **Composite bar** — full-width horizontal bar 8px tall, dark `--bg-elevated` track, filled width `{composite}%` in the level color. No label inside the bar — the big number above is the label.

3. **Three dimension bars** — three rows stacked, each 6px tall. Each row contains: a 90px label column (left-aligned, uppercase mini-caps style: "Payment", "Scope", "Chargeback"), a flexible bar track, and a right-aligned 32px numeric column showing the per-dimension `score`. Fill color uses the level the *individual dimension score* maps to via `levelFromScore(dimensionScore)` — so a dimension that's amber inside a red composite still renders amber.

4. **nextAction line** — a single muted-text line below the bars: `risk.nextAction`. Per D-22, this is text only — no link, no button. Font size 0.875rem, color `--text-secondary`.

5. **topMitigation callout** — only render when `risk.topMitigation !== null` AND `risk.level !== 'green'`. A two-line callout styled like the existing escalation-nudge banner in ProjectDetailClient: lime left border, slightly raised background. Line 1 (bold): "Biggest lever: {risk.topMitigation.action}". Line 2 (muted): "Removing this signal would drop the composite by ~{risk.topMitigation.deltaPoints} points." The "~" is literal — emphasizes deterministic-but-simulated nature.

6. **Per-signal evidence table** (D-23 — required for success criteria 4) — a `<table>` with three columns: Signal | Source | Points. Rows come from concatenating `risk.dimensions.payment.signals + .scope.signals + .chargeback.signals` in that order. Each row: signal label, source string (capitalized), points number with `+`/`-` prefix. Sub-header rows (`<tr>` spanning all three columns) introduce each dimension group: "PAYMENT", "SCOPE", "CHARGEBACK". When a dimension has zero signals, render a single muted "No signals" row inside that group so the structure is consistent. Table styling: small font, `--bg-elevated` row backgrounds alternating, no harsh borders. Do NOT recompute anything client-side — render `signals[].points` verbatim per D-23.

Everything in this card is presentation only. No state, no event handlers. Server-renderable (no `'use client'` directive).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -E "from '@/lib/risk'" components/project/ClientRiskBadge.tsx components/project/ClientBehaviorCard.tsx && grep -c "Signal\|Source\|Points" components/project/ClientBehaviorCard.tsx</automated>
  </verify>
  <done>
    - ClientRiskBadge imports from `@/lib/risk` and accepts `level: RiskLevel`
    - ClientBehaviorCard accepts `{ risk: RiskResult; clientName: string }`
    - All four bars (composite + payment + scope + chargeback) render unconditionally when the card is shown
    - Evidence table renders the three signal arrays in order without recomputation
    - topMitigation callout only renders when red/amber AND non-null
    - tsc clean; no `'use client'` directive
  </done>
</task>

<task type="auto">
  <name>Task 2: Switch ProjectCard + ProjectDetailClient to computeRisk</name>
  <files>components/project/ProjectCard.tsx, components/project/ProjectDetailClient.tsx</files>
  <read_first>
    - components/project/ProjectCard.tsx (current — uses computeClientRisk + ClientRiskBadge)
    - components/project/ProjectDetailClient.tsx (current — uses computeClientRisk + ClientBehaviorCard at line 71/143)
    - lib/risk/index.ts (Plan 02 — RiskResult shape)
  </read_first>
  <action>
**ProjectCard.tsx**:
- Replace `import { computeClientRisk } from '@/lib/clientRisk'` with `import { computeRisk } from '@/lib/risk'`.
- Replace `const clientRisk = computeClientRisk(project)` with `const risk = computeRisk(project)`.
- Update the two `<ClientRiskBadge score={...} level={...} />` call sites to pass `score={risk.composite}` and `level={risk.level}`.
- The conditional `score > 0` check that decides whether to render the badge in the "no contract" path stays the same shape — replace `clientRisk.score > 0` with `risk.composite > 0`.
- No other changes. The pill geometry and ordering of badges (contract risk, client risk, overdue) is unchanged.

**ProjectDetailClient.tsx**:
- Replace `import { computeClientRisk } from '@/lib/clientRisk'` with `import { computeRisk } from '@/lib/risk'`.
- Replace line 71 (`const clientRisk = computeClientRisk(project)`) with `const risk = computeRisk(project)`.
- Update the existing call site at line 143 from `<ClientBehaviorCard score={...} level={...} signals={...} clientName={...} />` to the new prop shape: `<ClientBehaviorCard risk={risk} clientName={project.client_name} />`.
- The `clientRisk.score > 0` guard becomes `risk.composite > 0` (D-23 implies always render when there is any score; the green/zero case is suppressed by this gate to avoid an empty "Healthy 0" card on brand-new projects).

Cast safety: the existing `ProjectDetailClientProps.project` type lists `defense_responses?: Array<{ id: string; tool_type: DefenseTool; created_at: string; was_sent: boolean }>` — that's compatible with the engine's expectations (the engine only reads `tool_type`, `was_sent`, `created_at`). No type widening needed.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -E "from '@/lib/risk'" components/project/ProjectCard.tsx components/project/ProjectDetailClient.tsx && grep -c "computeRisk" components/project/ProjectCard.tsx components/project/ProjectDetailClient.tsx</automated>
  </verify>
  <done>
    - Both files import `computeRisk` from `@/lib/risk`
    - ProjectCard passes `risk.composite` / `risk.level` to ClientRiskBadge in both code paths (the `riskLevel || isOverdue` branch and the `!riskLevel` fallback)
    - ProjectDetailClient passes `risk={risk}` to ClientBehaviorCard
    - No remaining `computeClientRisk` references in either file
    - tsc clean
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Top Risk dashboard insight section + remove old single-row injection</name>
  <files>app/(dashboard)/dashboard/page.tsx</files>
  <read_first>
    - app/(dashboard)/dashboard/page.tsx (full file — focus on lines 1-30 imports, 380-425 risk injection block, 490-555 Needs Attention render)
    - lib/risk/index.ts (Plan 02 — computeRisk + RISK_LEVEL_COLORS exports)
    - .planning/phases/14-risk-engine-deterministic-multi-dimensional-scoring/14-CONTEXT.md (D-21 #3 dashboard insight spec, D-25 perf budget)
  </read_first>
  <action>
**Imports**: Replace `import { computeClientRisk, LEVEL_LABELS, CLIENT_RISK_COLORS } from '@/lib/clientRisk'` with `import { computeRisk, RISK_LEVEL_COLORS, type RiskResult } from '@/lib/risk'`. Anywhere else in this file that referenced `CLIENT_RISK_COLORS` switches to `RISK_LEVEL_COLORS`.

**Remove the old single-row injection** at lines ~397-423: delete the `topRiskItem` / `topRiskBorder` variables, the `scored` array, the dedupe block at lines 420-423, and the conditional render `{topRiskItem && <AttentionAlert .../>}` inside the Needs Attention list at lines 547-551. The `attentionItems` list (overdue / due-soon / ghost / stalled) is **kept exactly as is** — only the client-risk row is removed.

**Add the new Top Risk panel** (D-21 #3) — render this section **above** the "Needs attention" section so the two are visually distinct, both inside the `totalProjects > 0` guard.

Server-side computation, performed once per request:
```
const today = new Date()
const ranked: Array<{ project: ProjectWithResponses; risk: RiskResult }> =
  projectList
    .map(p => ({ project: p, risk: computeRisk(p, today) }))
    .filter(entry => entry.risk.composite >= LEVEL_THRESHOLDS.amber)   // hide green clients from the panel
    .sort((a, b) => b.risk.composite - a.risk.composite)
const top3 = ranked.slice(0, 3)
```

Render the panel:

- Section header (matches "Needs attention" header style): "TOP CLIENT RISK"
- When `top3.length === 0`: a single muted row "All clients in the green — no action needed."
- When `top3.length > 0`: a column of up to three cards. Each card:
  - Left border 4px in the level color (`RISK_LEVEL_COLORS[risk.level]`)
  - Row 1: project title (bold) · client_name (muted) · `<ClientRiskBadge score={risk.composite} level={risk.level} />` (right-aligned)
  - Row 2: `risk.nextAction` rendered as 0.875rem `--text-secondary` text. Per D-22, NOT a link.
  - Row 3 (only when `risk.topMitigation !== null`): a lime-accented mini-line "Lever: {risk.topMitigation.action} (~{deltaPoints} pts)"
  - The whole card is wrapped in a `<Link href={\`/projects/\${project.id}\`}>` so clicking navigates to the project detail page (existing `ProjectCard` link pattern).
- Section margin-bottom `2rem` matching the other dashboard sections.

Do NOT add this card to projects whose composite is 0 (filter above already enforces this). Do NOT show top-3 if `projectList` is empty (already guarded by `totalProjects > 0`).

**Perf note (D-25)**: With ≤30 active projects each at <50ms, the total scan stays under 1.5s — well within the server-component budget. No `Promise.all` needed (engine is sync CPU).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -E "from '@/lib/risk'" "app/(dashboard)/dashboard/page.tsx" && ! grep -q "computeClientRisk\|CLIENT_RISK_COLORS\|LEVEL_LABELS" "app/(dashboard)/dashboard/page.tsx"</automated>
  </verify>
  <done>
    - No `computeClientRisk` / `CLIENT_RISK_COLORS` / `LEVEL_LABELS` references remain in the dashboard page
    - New section header "TOP CLIENT RISK" is present
    - Top-3 cards render with composite + level pill + nextAction + optional topMitigation line
    - Old `severity: 'client-risk'` AttentionAlert row is gone
    - tsc clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server component → engine | Dashboard + project detail page already fetch RLS-scoped projects; engine sees only the current user's data. |
| Engine output → DOM | nextAction / action / labels are static strings or project-owned data. No untrusted HTML rendered. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-09 | Tampering | Signal table client-side recompute | mitigate | Per D-23, the table renders `signals[].points` directly from engine output — no client-side recomputation. |
| T-14-10 | Information Disclosure | Top-3 dashboard panel | accept | Top-3 is computed from `projectList` already filtered to `user_id`. RLS layer above guarantees no cross-tenant leakage. |
| T-14-11 | Repudiation | Inconsistent score between badge and detail | mitigate | Both surfaces call `computeRisk(project)` with the project's full DB row → same composite. Dashboard pins `today` once per request so all top-3 entries agree. |
</threat_model>

<verification>
- `grep -RE "computeClientRisk|CLIENT_RISK_COLORS" components/ app/` returns no hits (Plan 04 will keep the shim path, but no Phase 14 UI imports it)
- `grep -E "from '@/lib/risk'" components/project/ProjectCard.tsx components/project/ProjectDetailClient.tsx components/project/ClientBehaviorCard.tsx components/project/ClientRiskBadge.tsx "app/(dashboard)/dashboard/page.tsx" | wc -l` returns 5
- `npx tsc --noEmit` passes
- The new dashboard section "TOP CLIENT RISK" appears above "Needs attention" in the rendered HTML
</verification>

<success_criteria>
- ROADMAP success criteria 3: dashboard shows top 3 high-risk clients with composite + level + nextAction — satisfied by the new panel
- ROADMAP success criteria 4: per-signal breakdown auditability — satisfied by the new evidence table in ClientBehaviorCard (D-23)
- ROADMAP success criteria 5: red threshold yields a named single mitigation — satisfied by the topMitigation callout (D-20)
- ProjectCard, ProjectDetailClient, dashboard page all consume the new engine without recomputing scores client-side
</success_criteria>

<output>
After completion, create `.planning/phases/14-risk-engine-deterministic-multi-dimensional-scoring/14-03-SUMMARY.md` documenting: the four updated components + dashboard section, the prop-shape change from `(score, level, signals)` → `risk: RiskResult`, screenshot-checkpoint notes for human verification (the executor should manually verify the dashboard panel renders and the per-signal table appears on a project that has at least one sent defense response). Plan 04 will then add the compat shim + integration tests.
</output>
