---
phase: 13-how-to
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/how-it-works/page.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-05: The page file is located at app/how-it-works/page.tsx (outside the (dashboard) route group) — no auth required, unauthenticated visitors can access it"
    - "D-06: Uses marketing layout (minimal centered logo header + Footer) matching app/page.tsx shell — not the dashboard Navbar/sidebar"
    - "The page shows the 3 use modes (paste a message, pick a tool, attach a contract) as scannable sections"
    - "D-08: All 21 defense tools from DEFENSE_TOOLS render in 4 named scenario groups (Payment Issues, Scope & Delivery, Client Behavior, Pricing & Rates)"
    - "D-09: Each tool entry shows bold label + tool.description — description IS the 'when to use' explanation, rendered as a list item"
    - "D-10: Tool labels and descriptions are imported from lib/defenseTools.ts — never hardcoded"
    - "The page includes a contract analysis explanation section and a FAQ"
    - "The page renders without any 'use client' directive (Server Component)"
  artifacts:
    - path: "app/how-it-works/page.tsx"
      provides: "Public Server Component page at /how-it-works with marketing layout, tool directory, FAQ"
      contains: "DEFENSE_TOOLS"
  key_links:
    - from: "app/how-it-works/page.tsx"
      to: "lib/defenseTools.ts"
      via: "import { DEFENSE_TOOLS } from '@/lib/defenseTools'"
      pattern: "import \\{ DEFENSE_TOOLS \\}"
    - from: "app/how-it-works/page.tsx"
      to: "components/shared/Footer"
      via: "import Footer from '@/components/shared/Footer'"
      pattern: "import Footer"
---

<objective>
Create the public `/how-it-works` page outside the `(dashboard)` route group. The page is a Server Component with marketing-style layout (minimal logo header, centered content, Footer) explaining how Pushback works in three modes, listing every defense tool grouped by scenario, explaining contract analysis, and answering the most common new-user questions.

Purpose: First-time users (and prospects) understand what Pushback does and how to use it without reading docs or watching a video.

Output: One new file at `app/how-it-works/page.tsx`. No layout file — root `app/layout.tsx` provides fonts + Toaster.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-how-to/13-CONTEXT.md
@.planning/phases/13-how-to/13-RESEARCH.md
@.planning/phases/13-how-to/13-PATTERNS.md
@.planning/phases/13-how-to/13-UI-SPEC.md
@CLAUDE.md
@AGENTS.md

<interfaces>
<!-- DEFENSE_TOOLS is the only data source for the tool directory. Pulled from lib/defenseTools.ts -->

```typescript
// From types/index.ts (referenced via DefenseToolMeta) — the relevant shape:
export type DefenseTool =
  | 'scope_change' | 'payment_first' | 'payment_second' | 'payment_final'
  | 'revision_limit' | 'kill_fee' | 'delivery_signoff' | 'dispute_response'
  | 'ghost_client' | 'feedback_stall' | 'moving_goalposts'
  | 'discount_pressure' | 'retroactive_discount' | 'rate_increase_pushback'
  | 'rush_fee_demand' | 'ip_dispute' | 'chargeback_threat' | 'spec_work_pressure'
  | 'post_handoff_request' | 'review_threat' | 'disputed_hours'

export interface DefenseToolMeta {
  type: DefenseTool
  label: string
  description: string
  icon: string
  urgency: 'low' | 'medium' | 'high'
  contextFields: { key: string; label: string; placeholder: string; type: 'text' | 'number'; required: boolean }[]
}
```

```typescript
// From lib/defenseTools.ts:
export const DEFENSE_TOOLS: DefenseToolMeta[] = [...]
// 21 entries total. Use `DEFENSE_TOOLS.find(t => t.type === type)` to look up by type.
```

```typescript
// app/privacy/page.tsx is the canonical analog for a public, server-rendered, marketing-laid-out page outside any route group.
// Its shell pattern (logo header, centered max-w content, Footer at bottom) is replicated here.
```

**Group definitions (locked by D-08 — exact scenario keys, group ordering is up to executor for the heading copy):**
- Payment Issues: `payment_first`, `payment_second`, `payment_final`, `retroactive_discount`, `disputed_hours`
- Scope & Delivery: `scope_change`, `revision_limit`, `moving_goalposts`, `post_handoff_request`, `delivery_signoff`
- Client Behavior: `ghost_client`, `feedback_stall`, `chargeback_threat`, `review_threat`, `dispute_response`, `spec_work_pressure`
- Pricing & Rates: `discount_pressure`, `rate_increase_pushback`, `rush_fee_demand`, `kill_fee`, `ip_dispute`

(Total: 5 + 5 + 6 + 5 = 21 — matches `DEFENSE_TOOLS.length`.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create public /how-it-works page with marketing shell, intro, and 3 use modes section</name>
  <files>app/how-it-works/page.tsx</files>
  <read_first>
    - app/privacy/page.tsx (full file) — canonical pattern for a public Server Component page with logo header, centered prose card, no auth, root layout only
    - app/layout.tsx — confirm root layout provides fonts + globals.css + Toaster (no extra imports needed in this page)
    - .planning/phases/13-how-to/13-CONTEXT.md — D-05 (location outside dashboard), D-06 (marketing layout), D-08 (group keys)
    - .planning/phases/13-how-to/13-PATTERNS.md — `app/how-it-works/page.tsx` analog section (shell + tool directory pattern)
    - .planning/phases/13-how-to/13-UI-SPEC.md — Copywriting Contract section (page title, group headings)
    - lib/defenseTools.ts — verify DEFENSE_TOOLS export name and DefenseToolMeta shape
    - CLAUDE.md / AGENTS.md — Next.js 16 conventions reminder
  </read_first>
  <behavior>
    - File path EXACTLY at `app/how-it-works/page.tsx` (NOT inside `app/(dashboard)/...` — D-05 explicitly overrides any earlier scaffolding).
    - File MUST NOT contain a `'use client'` directive — Server Component.
    - Page renders: minimal logo header (linking back to /), H1 page title, intro paragraph, "Three ways to use Pushback" section with three numbered cards (paste a message, pick a tool, attach a contract), Footer at bottom.
    - This task creates the file and the FIRST half of the content (shell through 3-use-modes). The tool directory and FAQ are added in Task 2 of this plan.
    - Layout uses inline `style` objects with CSS vars (per project convention) — no Tailwind layout classes. Tailwind utility classes acceptable for `transition-colors` and `hover:text-white`.
    - Zero new dependencies, zero new components.
  </behavior>
  <action>
Create the file `app/how-it-works/page.tsx`.

**Imports (exact, top of file):**
```tsx
import Link from 'next/link'
import { DEFENSE_TOOLS } from '@/lib/defenseTools'
import Footer from '@/components/shared/Footer'
```
Do NOT add `'use client'`. Do NOT import `useState`/`useEffect` — Server Components have no client hooks.

**Module-scope constant for tool groups (UPPER_SNAKE_CASE; locked by D-08):**
```tsx
const TOOL_GROUPS: { heading: string; types: string[] }[] = [
  {
    heading: 'Payment Issues',
    types: ['payment_first', 'payment_second', 'payment_final', 'retroactive_discount', 'disputed_hours'],
  },
  {
    heading: 'Scope & Delivery',
    types: ['scope_change', 'revision_limit', 'moving_goalposts', 'post_handoff_request', 'delivery_signoff'],
  },
  {
    heading: 'Client Behavior',
    types: ['ghost_client', 'feedback_stall', 'chargeback_threat', 'review_threat', 'dispute_response', 'spec_work_pressure'],
  },
  {
    heading: 'Pricing & Rates',
    types: ['discount_pressure', 'rate_increase_pushback', 'rush_fee_demand', 'kill_fee', 'ip_dispute'],
  },
]
```

**Default export — `export default function HowItWorksPage() { return (... ) }`** with the following JSX skeleton (exact structure; Task 2 fills in the tool directory + FAQ at the marker):

```tsx
return (
  <div style={{ backgroundColor: 'var(--bg-base)', minHeight: '100vh' }}>

    {/* Minimal logo header — same as privacy/terms pages */}
    <div style={{ textAlign: 'center', padding: '3rem 1.5rem 2rem' }}>
      <Link href="/" style={{ display: 'inline-flex', alignItems: 'center', gap: '0.15rem', textDecoration: 'none' }}>
        <span style={{ fontWeight: 800, fontSize: '1.5rem', color: 'var(--text-primary)' }}>Pushback</span>
        <span style={{ color: 'var(--brand-lime)', fontWeight: 800, fontSize: '1.5rem' }}>.</span>
      </Link>
    </div>

    {/* Page content */}
    <div style={{ width: '100%', maxWidth: '760px', margin: '0 auto', padding: '0 1.5rem 4rem' }}>

      {/* Eyebrow + H1 */}
      <p style={{ color: 'var(--brand-lime)', fontSize: '0.7rem', letterSpacing: '0.15em', textTransform: 'uppercase', fontWeight: 600, marginBottom: '1rem', textAlign: 'center' }}>
        How it works
      </p>
      <h1 style={{ color: 'var(--text-primary)', fontSize: 'clamp(2.2rem, 5vw, 3.5rem)', fontWeight: 600, lineHeight: 1.05, letterSpacing: '-0.02em', marginBottom: '1.5rem', textAlign: 'center' }}>
        Pushback — How It Works
      </h1>
      <p style={{ color: 'var(--text-secondary)', fontSize: '1rem', lineHeight: 1.65, marginBottom: '3rem', textAlign: 'center' }}>
        Pushback gives freelancers a professional, ready-to-send response to any uncomfortable client situation in under 30 seconds. No legal background, no copywriting required.
      </p>

      {/* Three ways to use Pushback */}
      <section style={{ marginBottom: '3.5rem' }}>
        <h2 style={{ color: 'var(--text-primary)', fontSize: '1.5rem', fontWeight: 600, marginBottom: '1.5rem' }}>
          Three ways to use Pushback
        </h2>

        {/* Mode 1 */}
        <div style={{ backgroundColor: 'var(--bg-surface)', border: '1px solid var(--bg-border)', borderRadius: '0.75rem', padding: '1.5rem', marginBottom: '1rem' }}>
          <p style={{ color: 'var(--brand-lime)', fontSize: '0.7rem', letterSpacing: '0.12em', textTransform: 'uppercase', fontWeight: 600, marginBottom: '0.5rem' }}>Mode 1</p>
          <h3 style={{ color: 'var(--text-primary)', fontSize: '1.05rem', fontWeight: 600, marginBottom: '0.5rem' }}>Paste a client message</h3>
          <p style={{ color: 'var(--text-secondary)', fontSize: '0.9rem', lineHeight: 1.6 }}>
            Drop the email or DM into the analyze box. Pushback identifies the situation type — scope creep, late payment, ghosting, rate pressure — and pre-fills the right response tool. One click and you have a professional reply.
          </p>
        </div>

        {/* Mode 2 */}
        <div style={{ backgroundColor: 'var(--bg-surface)', border: '1px solid var(--bg-border)', borderRadius: '0.75rem', padding: '1.5rem', marginBottom: '1rem' }}>
          <p style={{ color: 'var(--brand-lime)', fontSize: '0.7rem', letterSpacing: '0.12em', textTransform: 'uppercase', fontWeight: 600, marginBottom: '0.5rem' }}>Mode 2</p>
          <h3 style={{ color: 'var(--text-primary)', fontSize: '1.05rem', fontWeight: 600, marginBottom: '0.5rem' }}>Pick a defense tool directly</h3>
          <p style={{ color: 'var(--text-secondary)', fontSize: '0.9rem', lineHeight: 1.6 }}>
            Already know what you&apos;re dealing with? Pick the tool from the sidebar — Scope Change, Late Payment, Ghost Client, Kill Fee, and 17 more. Add a few details about your situation, and Pushback drafts a tailored response.
          </p>
        </div>

        {/* Mode 3 */}
        <div style={{ backgroundColor: 'var(--bg-surface)', border: '1px solid var(--bg-border)', borderRadius: '0.75rem', padding: '1.5rem' }}>
          <p style={{ color: 'var(--brand-lime)', fontSize: '0.7rem', letterSpacing: '0.12em', textTransform: 'uppercase', fontWeight: 600, marginBottom: '0.5rem' }}>Mode 3</p>
          <h3 style={{ color: 'var(--text-primary)', fontSize: '1.05rem', fontWeight: 600, marginBottom: '0.5rem' }}>Attach your contract</h3>
          <p style={{ color: 'var(--text-secondary)', fontSize: '0.9rem', lineHeight: 1.6 }}>
            Upload your signed contract once. Pushback reads it and uses your actual clauses — payment terms, revision limits, kill fees — in every response it drafts for that project. Your replies reference your real agreement, not generic advice.
          </p>
        </div>
      </section>

      {/* TOOL DIRECTORY MARKER — Task 2 inserts the tool directory section here */}
      {/* CONTRACT ANALYSIS MARKER — Task 2 inserts the contract analysis section here */}
      {/* FAQ MARKER — Task 2 inserts the FAQ section here */}

    </div>

    <Footer />
  </div>
)
```

Replace each MARKER comment with the corresponding content in Task 2 of this plan — leave the markers as comments for now so Task 2 can locate insertion points.

The page MUST NOT have a `'use client'` directive at the top. The page MUST be the default export.

Do not use any `var(--brand-amber)`. Use `var(--brand-lime)` for the eyebrow accents.

Do not use Tailwind layout classes (no `flex`, `grid`, `mx-auto`, `max-w-*`). All layout is inline styles. Tailwind is permitted only for `hover:` and `transition-` utilities — none needed in this task.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "app/how-it-works/page.tsx" | grep -v '^#' | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - File exists at exact path: `test -f app/how-it-works/page.tsx && echo OK` outputs `OK`
    - File NOT in (dashboard) route group: `test ! -f "app/(dashboard)/how-it-works/page.tsx" && echo OK` outputs `OK`
    - No 'use client' directive: `grep -c "^'use client'" app/how-it-works/page.tsx` returns `0`
    - Default export present: `grep -c "^export default function HowItWorksPage" app/how-it-works/page.tsx` returns `1`
    - Imports DEFENSE_TOOLS: `grep -c "import { DEFENSE_TOOLS } from '@/lib/defenseTools'" app/how-it-works/page.tsx` returns `1`
    - Imports Footer: `grep -c "import Footer from '@/components/shared/Footer'" app/how-it-works/page.tsx` returns `1`
    - Imports Link: `grep -c "import Link from 'next/link'" app/how-it-works/page.tsx` returns `1`
    - TOOL_GROUPS constant present with all 4 group keys: `grep -c "heading: 'Payment Issues'" app/how-it-works/page.tsx` returns `1` AND `grep -c "heading: 'Scope & Delivery'" app/how-it-works/page.tsx` returns `1` AND `grep -c "heading: 'Client Behavior'" app/how-it-works/page.tsx` returns `1` AND `grep -c "heading: 'Pricing & Rates'" app/how-it-works/page.tsx` returns `1`
    - All 21 tool types referenced in TOOL_GROUPS: `grep -oE "'(payment_first|payment_second|payment_final|retroactive_discount|disputed_hours|scope_change|revision_limit|moving_goalposts|post_handoff_request|delivery_signoff|ghost_client|feedback_stall|chargeback_threat|review_threat|dispute_response|spec_work_pressure|discount_pressure|rate_increase_pushback|rush_fee_demand|kill_fee|ip_dispute)'" app/how-it-works/page.tsx | sort -u | wc -l | tr -d ' '` returns `21`
    - H1 page title exact: `grep -F "Pushback — How It Works" app/how-it-works/page.tsx | wc -l | tr -d ' '` returns at least `1`
    - "Three ways to use Pushback" heading present: `grep -c "Three ways to use Pushback" app/how-it-works/page.tsx` returns `1`
    - Section markers for Task 2 present: `grep -c "TOOL DIRECTORY MARKER" app/how-it-works/page.tsx` returns `1` AND `grep -c "CONTRACT ANALYSIS MARKER" app/how-it-works/page.tsx` returns `1` AND `grep -c "FAQ MARKER" app/how-it-works/page.tsx` returns `1`
    - No `var(--brand-amber)`: `grep -c "var(--brand-amber)" app/how-it-works/page.tsx` returns `0`
    - No emoji characters: `LC_ALL=C grep -P "[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]" app/how-it-works/page.tsx | wc -l | tr -d ' '` returns `0`
    - tsc clean for this file: `npx tsc --noEmit 2>&1 | grep "app/how-it-works/page.tsx" | grep -v '^#' | wc -l | tr -d ' '` returns `0`
  </acceptance_criteria>
  <done>
    `app/how-it-works/page.tsx` exists outside `(dashboard)`, is a Server Component (no 'use client'), default-exports `HowItWorksPage`, imports DEFENSE_TOOLS + Footer + Link, declares the 4-group TOOL_GROUPS constant covering all 21 tool types, renders the marketing shell with H1 + 3-use-modes section, and contains the three insertion markers for Task 2.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Render the tool directory, contract analysis section, and FAQ in /how-it-works page</name>
  <files>app/how-it-works/page.tsx</files>
  <read_first>
    - app/how-it-works/page.tsx (just created in Task 1) — read the file to find the exact location of TOOL DIRECTORY MARKER, CONTRACT ANALYSIS MARKER, and FAQ MARKER comments
    - lib/defenseTools.ts — re-confirm DEFENSE_TOOLS shape; specifically that `t.label` and `t.description` are the only fields needed
    - .planning/phases/13-how-to/13-PATTERNS.md — `app/how-it-works/page.tsx` Tool directory render pattern (DEFENSE_TOOLS.find by type, list item with strong label + description)
    - .planning/phases/13-how-to/13-UI-SPEC.md — Tool directory copy + FAQ + page-level focal point
  </read_first>
  <behavior>
    - Replace each of the three marker comments with the corresponding section JSX (tool directory, contract analysis explanation, FAQ).
    - Tool directory: 4 H3 headings (one per group), each followed by a `<ul>` of `<li>` entries. Each `<li>` shows `<strong>{tool.label}</strong>` followed by `{tool.description}`. The tool data is looked up via `DEFENSE_TOOLS.find(t => t.type === type)` — never hardcode.
    - Contract analysis section: short H2 + 2 paragraphs explaining how contract upload feeds into responses (free tier: 1 contract analysis, Pro tier: unlimited).
    - FAQ section: H2 + 4-6 Q&A pairs answering the most common new-user questions (free vs Pro limits, where my data goes, can I delete my account, what if Pushback gets a situation wrong).
  </behavior>
  <action>
Modify `app/how-it-works/page.tsx`. Locate each marker comment and REPLACE it with the corresponding JSX block below.

**Replace `{/* TOOL DIRECTORY MARKER ... */}` with:**
```tsx
      {/* Tool Directory */}
      <section style={{ marginBottom: '3.5rem' }}>
        <h2 style={{ color: 'var(--text-primary)', fontSize: '1.5rem', fontWeight: 600, marginBottom: '0.75rem' }}>
          The full tool directory
        </h2>
        <p style={{ color: 'var(--text-secondary)', fontSize: '0.95rem', lineHeight: 1.6, marginBottom: '2rem' }}>
          21 defense tools, grouped by what&apos;s happening with your client. Pick one when you know exactly what you&apos;re dealing with.
        </p>

        {TOOL_GROUPS.map(group => (
          <div key={group.heading} style={{ marginBottom: '2rem' }}>
            <h3 style={{
              color: 'var(--text-primary)',
              fontSize: '0.85rem',
              fontWeight: 600,
              letterSpacing: '0.08em',
              textTransform: 'uppercase',
              marginBottom: '1rem',
            }}>
              {group.heading}
            </h3>
            <ul style={{ listStyle: 'none', padding: 0, margin: 0, display: 'flex', flexDirection: 'column', gap: '0.85rem' }}>
              {group.types.map(type => {
                const tool = DEFENSE_TOOLS.find(t => t.type === type)
                if (!tool) return null
                return (
                  <li key={type} style={{ fontSize: '0.9rem', lineHeight: 1.55 }}>
                    <strong style={{ color: 'var(--text-primary)', fontWeight: 600 }}>{tool.label}</strong>
                    <span style={{ color: 'var(--text-secondary)' }}> — {tool.description}</span>
                  </li>
                )
              })}
            </ul>
          </div>
        ))}
      </section>
```

**Replace `{/* CONTRACT ANALYSIS MARKER ... */}` with:**
```tsx
      {/* Contract analysis */}
      <section style={{ marginBottom: '3.5rem' }}>
        <h2 style={{ color: 'var(--text-primary)', fontSize: '1.5rem', fontWeight: 600, marginBottom: '0.75rem' }}>
          How contract analysis works
        </h2>
        <p style={{ color: 'var(--text-secondary)', fontSize: '0.95rem', lineHeight: 1.65, marginBottom: '1rem' }}>
          Upload your signed contract once. Pushback reads it, scores its risk, and identifies which clauses cover payment terms, revisions, IP, kill fees, and scope. From that point on, every response Pushback drafts for that project quotes your actual contract — not generic advice.
        </p>
        <p style={{ color: 'var(--text-secondary)', fontSize: '0.95rem', lineHeight: 1.65 }}>
          Free accounts get one contract analysis. Pro accounts can analyze unlimited contracts and link them to multiple projects. Contract text is processed by Anthropic on our behalf and is never stored as raw text — only structured analysis is kept on your account.
        </p>
      </section>
```

**Replace `{/* FAQ MARKER ... */}` with:**
```tsx
      {/* FAQ */}
      <section style={{ marginBottom: '3.5rem' }}>
        <h2 style={{ color: 'var(--text-primary)', fontSize: '1.5rem', fontWeight: 600, marginBottom: '1.5rem' }}>
          Frequently asked
        </h2>

        {[
          {
            q: 'How many responses can I generate on the free plan?',
            a: '3 AI-generated responses and 1 contract analysis, total — not per month. Pro accounts get unlimited responses and contract analyses.',
          },
          {
            q: 'Where do my client messages and contracts go?',
            a: 'Pushback sends them to Anthropic (Claude) as a data processor to generate responses. Anthropic does not retain your content for training. Raw contract text is read, analyzed, and discarded — only the structured analysis is stored on your account.',
          },
          {
            q: 'Can I delete my data?',
            a: 'Yes. You can delete any individual project, contract, or response from the dashboard. To delete your entire account and all associated data, email us from the address linked to your account — see the data deletion link in the footer.',
          },
          {
            q: 'What if Pushback identifies the wrong situation?',
            a: 'You can ignore the suggestion and pick a different tool from the sidebar. Every response is also editable before you copy or send — Pushback drafts the message, you decide what goes out.',
          },
          {
            q: 'Is the AI output legal advice?',
            a: 'No. Pushback drafts professional, ready-to-send responses based on your situation and contract. It is not a substitute for a lawyer. For high-stakes disputes, escalate to a qualified professional.',
          },
          {
            q: 'Can I cancel my Pro subscription?',
            a: 'Yes — anytime, from the billing portal linked in your account. You keep Pro access until the end of the current billing period.',
          },
        ].map(({ q, a }) => (
          <div key={q} style={{
            backgroundColor: 'var(--bg-surface)',
            border: '1px solid var(--bg-border)',
            borderRadius: '0.75rem',
            padding: '1.25rem 1.5rem',
            marginBottom: '0.75rem',
          }}>
            <p style={{ color: 'var(--text-primary)', fontSize: '0.95rem', fontWeight: 600, marginBottom: '0.5rem' }}>
              {q}
            </p>
            <p style={{ color: 'var(--text-secondary)', fontSize: '0.9rem', lineHeight: 1.6 }}>
              {a}
            </p>
          </div>
        ))}
      </section>
```

After the replacements, the three marker comments must be entirely gone from the file. Do not leave behind partial comments or duplicate sections.

Do not use Tailwind layout classes (no `flex`, `grid`, `mx-auto`). Inline styles only. Do not use `var(--brand-amber)`. Do not include emoji.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "app/how-it-works/page.tsx" | grep -v '^#' | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - All three markers removed: `grep -c "TOOL DIRECTORY MARKER" app/how-it-works/page.tsx` returns `0` AND `grep -c "CONTRACT ANALYSIS MARKER" app/how-it-works/page.tsx` returns `0` AND `grep -c "FAQ MARKER" app/how-it-works/page.tsx` returns `0`
    - Tool directory section heading present: `grep -c "The full tool directory" app/how-it-works/page.tsx` returns `1`
    - Tool directory uses DEFENSE_TOOLS lookup: `grep -c "DEFENSE_TOOLS.find(t => t.type === type)" app/how-it-works/page.tsx` returns `1`
    - Tool directory renders both label and description: `grep -c "tool.label" app/how-it-works/page.tsx` returns at least `1` AND `grep -c "tool.description" app/how-it-works/page.tsx` returns at least `1`
    - No tool labels hardcoded outside the constant array: `grep -cE "'Scope Change'|'Payment Reminder'|'Kill Fee'|'Ghost Client'|'Rate Negotiation'" app/how-it-works/page.tsx` returns `0` (labels come from DEFENSE_TOOLS at runtime)
    - Contract analysis section present: `grep -c "How contract analysis works" app/how-it-works/page.tsx` returns `1`
    - FAQ section present: `grep -c "Frequently asked" app/how-it-works/page.tsx` returns `1`
    - At least 4 FAQ items: `grep -cE "    q: '" app/how-it-works/page.tsx` returns at least `4`
    - Anthropic mentioned in FAQ (data processing transparency): `grep -c "Anthropic" app/how-it-works/page.tsx` returns at least `1`
    - Free vs Pro limits stated: `grep -cE "(free|Free).*(3|three).*responses|Pro.*unlimited" app/how-it-works/page.tsx` returns at least `1`
    - No emoji: `LC_ALL=C grep -P "[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]" app/how-it-works/page.tsx | wc -l | tr -d ' '` returns `0`
    - No `var(--brand-amber)`: `grep -c "var(--brand-amber)" app/how-it-works/page.tsx` returns `0`
    - tsc clean: `npx tsc --noEmit 2>&1 | grep "app/how-it-works/page.tsx" | grep -v '^#' | wc -l | tr -d ' '` returns `0`
    - Page builds and serves a 200 in dev: `(timeout 30 npm run build 2>&1 | tail -30 | grep -E "Compiled successfully|/how-it-works" | head -2) || true` produces output containing either `Compiled successfully` or `/how-it-works`
  </acceptance_criteria>
  <done>
    All three markers replaced with content sections. Tool directory dynamically renders all 21 tools from DEFENSE_TOOLS via `find(t => t.type === type)`. Contract analysis section explains free vs Pro limits and Anthropic processing. FAQ contains at least 4 question/answer pairs covering plan limits, data privacy, deletion, AI accuracy, legal disclaimer. tsc and build pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser request -> Next.js SSR | Public unauthenticated page; renders only hardcoded copy and DEFENSE_TOOLS data — no user-supplied input crosses any boundary |
| DEFENSE_TOOLS module -> JSX render | Tool labels/descriptions come from a trusted module-scope constant; React escapes by default |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.02-01 | Spoofing | n/a | accept | Public page — no auth, no identity assertion possible |
| T-13.02-02 | Tampering / XSS | DEFENSE_TOOLS data render | accept | All `tool.label` and `tool.description` values are module-scope literals in `lib/defenseTools.ts` (compiled into the bundle); no user input. JSX escapes content by default. No `dangerouslySetInnerHTML` introduced — verified by `grep -c "dangerouslySetInnerHTML" app/how-it-works/page.tsx` returning 0. |
| T-13.02-03 | Repudiation | n/a | accept | Read-only page — no actions to repudiate |
| T-13.02-04 | Information Disclosure | Public page contents | accept | All content is intentionally public marketing copy. No PII, no env vars, no secrets surfaced. |
| T-13.02-05 | Denial of Service | Server Component render cost | accept | Static prose + ~21-item array map. Trivial render cost; no DB calls; no fetch. |
| T-13.02-06 | Open redirect | Logo Link href | mitigate | Only `<Link href="/">` is used (root). `Footer` component owns its own internal hrefs. No URL params accepted on this page — `grep -c "useSearchParams\|searchParams" app/how-it-works/page.tsx` returns 0. |
| T-13.02-07 | Elevation of privilege | Public page | accept | Page exposes no privileged actions. The `(dashboard)` route group is intentionally NOT wrapping this file (D-05). |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with zero errors mentioning `app/how-it-works/page.tsx`
- `npx eslint app/how-it-works/page.tsx --quiet` returns 0 errors
- `npm run build` compiles `/how-it-works` route successfully
- Manual: visit `/how-it-works` in dev — page loads, all 21 tools listed under their group headings, footer present at bottom, logo links back to `/`
- Manual: log out completely, visit `/how-it-works` — page renders identically (no auth redirect)
</verification>

<success_criteria>
- `/how-it-works` is reachable for both authenticated and unauthenticated visitors
- All 21 tools from DEFENSE_TOOLS render under exactly 4 group headings
- Tool labels and descriptions update automatically if `lib/defenseTools.ts` is edited (no hardcoded copy)
- Page contains: H1 title, 3 use modes, full tool directory, contract analysis explainer, FAQ
- Page is a Server Component (no `'use client'`)
- File is at `app/how-it-works/page.tsx` — NOT inside `app/(dashboard)/`
</success_criteria>

<output>
After completion, write `.planning/phases/13-how-to/13-02-SUMMARY.md` covering: file path, group keys used, total tool count rendered, FAQ topics, deviations from spec (none expected). Note any tool labels that surprised you (the source of truth is `lib/defenseTools.ts`).
</output>
