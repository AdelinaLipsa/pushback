---
phase: 13
slug: how-to
status: draft
shadcn_initialized: true
preset: base-nova
created: 2026-04-26
revised: 2026-04-26
revision_reason: "checker v1 — fixed: 3 font weights → 2 (dropped 800, kept 400+600); added /how-it-works focal point declaration; added 80px to spacing exceptions table"
---

# Phase 13 — UI Design Contract

> Visual and interaction contract for Phase 13: How-To & In-App Guidance.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (base-nova style) |
| Preset | base-nova |
| Component library | Radix UI (via shadcn) |
| Icon library | Lucide React |
| Font | Inter (var(--font-inter) → var(--font-sans)); monospace via var(--font-mono) |

Source: `npx shadcn info` + `components.json`

Installed shadcn components: `button`, `dialog`, `sonner`
No new shadcn components required for this phase — zero new dependencies.

---

## Spacing Scale

Declared values (multiples of 4 only):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, badge internal padding |
| sm | 8px | Inline element spacing, hint text margins |
| md | 16px | Default element spacing, section internal padding |
| lg | 24px | Card/section padding |
| xl | 32px | Layout gaps between major sections |
| 2xl | 48px | Section vertical spacing (py-12 equivalent) |
| 3xl | 64px | Page-level top/bottom spacing |
| 4xl | 80px | Landing page section vertical padding (py-20); matches existing section pattern in `app/page.tsx` |

Exceptions:
- Animation vignette overlay: `inset: 0` (full bleed)
- Cinematic entrance touch target: wrapper `min-height` unset — purely visual component
- Hint text margin: `marginTop: 0.5rem` (8px) — matches existing DefenseDashboard spacing

Note: The 80px (4xl) value is the only token outside the standard 4–64px scale. It is explicitly declared here because `py-20` is used for the landing page "See it in action" section wrapper and is consistent with adjacent sections in `app/page.tsx`. No other spacing exceptions exist in this phase.

Source: codebase scan of `app/page.tsx`, `components/defense/DefenseDashboard.tsx`, `components/project/ProjectDetailClient.tsx`

---

## Typography

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Label / eyebrow | 11px (0.7rem) | 600 semibold | 1.2 | Uppercase tracking-widest labels ("How it works", section eyebrows, step labels in animation) |
| Body small | 12px (0.75rem) | 400 regular | 1.5 | Hint copy, secondary descriptive text, tool directory list items |
| Body | 14px (0.875rem) | 400 regular | 1.65 | Page prose, FAQ answers, tool descriptions on /how-it-works |
| Heading | clamp(2.2rem, 5vw, 3.5rem) | 600 semibold | 1.05 | Marketing page section headings (matches `font-semibold` in `app/page.tsx` landing section wrapper) |

**Declared weights: 400 (regular) and 600 (semibold) only.**

Notes:
- All font sizes expressed as `rem` values in inline `style` objects — no Tailwind font-size classes.
- Monospace font (`var(--font-mono)`) reserved for: animation badge values (e.g. `8/10`), code/clause display in animation pasting step.
- Eyebrow labels always `uppercase`, `letterSpacing: '0.15em'` (page sections) or `0.08em` (dashboard labels).
- Weight 600 (semibold) applies to: marketing page section headings (`app/how-it-works/page.tsx`, landing section `app/page.tsx`), sub-headings in dashboard components, and eyebrow labels. This unifies headings at a single weight across both marketing and dashboard surfaces.
- The `text-3xl font-semibold` class used in the landing section wrapper JSX (RESEARCH.md Pattern — Landing Page Section Wrapper) confirms 600 is the correct heading weight; 800 extrabold is NOT used in this phase.

Source: codebase scan of `app/page.tsx` (lines 637–701), `components/defense/DefenseDashboard.tsx` (lines 88, 104), `components/project/ProjectDetailClient.tsx` (line 74), RESEARCH.md landing section wrapper code example

---

## Color

| Role | Value | CSS Var | Usage |
|------|-------|---------|-------|
| Dominant (60%) | `#0a0a0a` | `var(--bg-base)` | Page background, animation container background |
| Secondary (30%) | `#111111` | `var(--bg-surface)` | Cards, tool directory group blocks, FAQ items |
| Elevated surface | `#1a1a1a` | `var(--bg-elevated)` | Inner panels within animation (tool card, response area) |
| Border | `#2a2a2a` | `var(--bg-border)` | Dividers, card outlines, section borders |
| Text primary | `#fafafa` | `var(--text-primary)` | All headings, labels, main content |
| Text secondary | `#a1a1aa` | `var(--text-secondary)` | Prose body, descriptions, secondary copy |
| Text muted | `#52525b` | `var(--text-muted)` | Hint text, timestamps, placeholder copy |
| Accent (10%) | `#84cc16` | `var(--brand-lime)` | See reserved list below |
| Destructive | `#ef4444` | `var(--urgency-high)` | No destructive actions in this phase |

**Accent reserved for:**
1. Eyebrow section labels ("See it in action", "How it works") — on both `/how-it-works` and landing page section
2. Active tool card border + glow in `DemoAnimation` (`border: '1px solid #84cc16'`, `boxShadow: '0 0 12px rgba(132,204,22,0.15)'`)
3. Risk Score badge background tint + text in `DemoAnimation` (`rgba(132,204,22,0.1)` bg, `#84cc16` text)
4. Step label text in `DemoAnimation` ("Pick your situation" etc.)
5. "See the tool guide →" link in DefenseDashboard empty-state hint
6. Navbar "Help" link active/hover state (consistent with existing nav accent pattern)

Do NOT use lime for: tool directory list item labels, FAQ body text, page prose, general nav links.

**Animation-specific color values (exact, from 13-PLAN.md):**
- Vignette: `radial-gradient(ellipse at center, transparent 55%, rgba(9,9,11,0.85) 100%)`
- Animation container background: `#09090b` (matches page bg for seamless vignette bleed)

Source: CONTEXT.md specifics section + `app/globals.css` lines 42–106 + project memory `feedback_lime_color.md`

---

## Visuals — Focal Points

| Surface | Primary Visual Anchor | Secondary Anchor |
|---------|----------------------|-----------------|
| `/how-it-works` page | Page heading ("How It Works" or equivalent H1 at top of content) — first element the eye lands on after any logo/nav header; weight 600, clamp size, color `var(--text-primary)` | Tool directory H3 group headings — draw scannable structure down the page |
| Landing page "See it in action" section | `DemoAnimation` component — the only animated element in the viewport; motion draws attention naturally | Section heading "From contract to response in seconds" (above animation) — sets context |
| DefenseDashboard hint | Hint paragraph is subordinate — no visual anchor; it is a supplementary element, not a focal point | — |
| ProjectDetailClient hint | Same as DefenseDashboard hint — subordinate, no focal anchor | — |

Source: CONTEXT.md D-05, D-06, D-08; RESEARCH.md Pattern 3

---

## Component Inventory

### New Components

| Component | File | Type | Notes |
|-----------|------|------|-------|
| `DemoAnimation` | `components/hero/DemoAnimation.tsx` | `'use client'` | 5-state machine: `idle \| pasting \| analyzing \| selecting \| responding`. 4-step loop ~12s. Typewriter via `setInterval`. Cinematic entrance via `requestAnimationFrame` + CSS transition. Always-on vignette overlay. |

### Modified Components

| Component | File | Change |
|-----------|------|--------|
| `app/page.tsx` | existing | Add "See it in action" `<section>` between Ticker and HowItWorksDemo. Exact JSX from RESEARCH.md. |
| `components/shared/Navbar.tsx` | existing | Add "Help" link after Settings in `NAV_SECTIONS`. Icon: `HelpCircle` from lucide-react. href: `/how-it-works`. |
| `components/shared/Footer.tsx` | existing | Replace `/#how-it-works` anchor with `/how-it-works` in `PRODUCT_LINKS` array (line 8). |
| `components/defense/DefenseDashboard.tsx` | existing | Update empty-state hint: 2-line guidance + lime link. Gate: `showAnalyzePanel && !analysisResult`. |
| `app/(dashboard)/projects/[id]/page.tsx` | existing | Add 1-line hint in `ProjectDetailClient` (or page.tsx depending on component boundary). Gate: `responses.length === 0`. No dismiss. |

### New Pages

| Page | File | Layout |
|------|------|--------|
| `/how-it-works` | `app/how-it-works/page.tsx` | Root layout only (`app/layout.tsx` provides fonts + Toaster). Server Component. Public — no auth. |

---

## Animation Contract (DemoAnimation)

### State Machine

| Step | State | Duration | Visual |
|------|-------|----------|--------|
| 0 | `idle` | 1500ms | Empty panels |
| 1 | `pasting` | ~3500ms | Typewriter reveals contract clause |
| 2 | `analyzing` | 1500ms | Risk Score badge `8/10` appears with lime styling |
| 3 | `selecting` | 1500ms | "Rate Negotiation" card activates with lime border + glow |
| 4 | `responding` | 2500ms | Response text appears; loop restarts |

Total cycle: ~12 seconds. Restarts at `idle` after `responding`.

### Exact Content Strings (from 13-PLAN.md — do not paraphrase)

- Contract clause: `"...Contractor waives all rights to claim additional compensation..."`
- Risk Score badge: `8/10`
- Active tool card label: `Rate Negotiation`
- Response text: rate negotiation response message from 13-PLAN.md (full text)

### Entrance Transition

```
scale(0.92) opacity(0) → scale(1) opacity(1)
duration: 600ms
easing: cubic-bezier(0.16, 1, 0.3, 1) (spring)
trigger: requestAnimationFrame after mount
```

### Vignette Overlay

```css
radial-gradient(ellipse at center, transparent 55%, rgba(9,9,11,0.85) 100%)
position: absolute; inset: 0; pointer-events: none; z-index: 10
```

Source: CONTEXT.md D-01 through D-04, RESEARCH.md Pattern 1, 13-PLAN.md exact strings

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Landing page section eyebrow | See it in action |
| Landing page section heading | From contract to response in seconds |
| Landing page section subheading | Paste a clause, get a risk score, pick your situation, and copy a professional response — no legal knowledge required. |
| Navbar Help link label | How it works |
| Footer link label | How it works (replaces "How it works" → `/#how-it-works` anchor) |
| /how-it-works page title | Pushback — How It Works |
| DefenseDashboard empty-state hint line 1 | Not sure which tool? |
| DefenseDashboard empty-state hint link | See the tool guide → (href: `/how-it-works`, color: `var(--brand-lime)`) |
| ProjectDetailClient new-project hint | New to Pushback? Paste a message from your client above — we'll figure out what you're dealing with. |
| Tool directory group heading — Payments | Payment Issues |
| Tool directory group heading — Scope | Scope & Delivery |
| Tool directory group heading — Behavior | Client Behavior |
| Tool directory group heading — Pricing | Pricing & Rates |

**Empty states (this phase adds/modifies):**
- DefenseDashboard: existing empty state gets the 2-line hint appended (auto-hides when tool selected or analysis present)
- ProjectDetailClient: existing empty state gets 1-line hint (auto-hides when `responses.length > 0`)

**Destructive actions in this phase:** None. No delete dialogs, no confirmation flows.

**Error states in this phase:** None. This phase is read-only UI — no API calls, no mutations.

Source: CONTEXT.md D-11 through D-13, RESEARCH.md Pattern 4, 13-PLAN.md section copy

---

## Interaction Contract

### Hint Auto-Hide Rules

| Surface | Show Condition | Hide Trigger | Persistence |
|---------|---------------|--------------|-------------|
| DefenseDashboard hint | `showAnalyzePanel && !analysisResult` | Tool selected OR analysis result set | None — pure conditional render |
| ProjectDetailClient hint | `responses.length === 0` | First response generated | None — pure conditional render |

No dismiss button. No localStorage. No DB flag. Source: CONTEXT.md D-12.

### DemoAnimation Loop

- Starts: on component mount (`useEffect` with empty deps)
- Loop: automatic, indefinite (`idle` state restarts sequence after `responding`)
- No IntersectionObserver — plays regardless of scroll position
- No user controls (no pause, no skip)

Source: CONTEXT.md D-02.

### /how-it-works Tool Directory

- Tool data sourced from `DEFENSE_TOOLS` (`lib/defenseTools.ts`) at module scope — never hardcode labels/descriptions
- Groups defined as `TOOL_GROUPS: { heading: string; types: DefenseTool[] }[]` constant in page file
- Each tool renders as `<li>`: `<strong>{tool.label}</strong> — {tool.description}`
- Groups render as `<h3>{group.heading}</h3>` + `<ul>` list
- No interactive elements in tool directory (no click, no hover tooltip, no expansion)

Source: CONTEXT.md D-08 through D-10, RESEARCH.md Pattern 3.

---

## Styling Rules

| Rule | Detail |
|------|--------|
| Inline style objects | All layout uses inline `style={{ ... }}` with CSS vars. No Tailwind layout classes. |
| Tailwind utility classes | Acceptable for transitions, color helpers, hover states (e.g. `hover:text-white`, `transition-colors`) |
| CSS variables | Always `var(--bg-base)`, `var(--brand-lime)`, etc. Never raw hex values except in animation (per plan spec) |
| Accent color | Always `var(--brand-lime)`. Never `var(--brand-amber)` (aliased to same value but use lime directly) |
| Emojis | Forbidden. Use Lucide icons only. |
| Border radius | `var(--radius)` (0.625rem) for cards; `4px` for badges and monospace elements |

Source: CONTEXT.md code_context, RESEARCH.md anti-patterns, project memory `feedback_lime_color.md`

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | button, dialog, sonner (already installed) | not required |

No third-party registries. No new shadcn components added in this phase. Registry vetting gate: not applicable.

Source: `components.json` registries: `{}` (empty), `npx shadcn info` Installed Components.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
