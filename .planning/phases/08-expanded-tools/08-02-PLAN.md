---
phase: 08-expanded-tools
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - types/index.ts
  - lib/defenseTools.ts
  - components/defense/DefenseToolCard.tsx
  - lib/anthropic.ts
  - app/api/projects/[id]/defend/route.ts
  - app/api/projects/[id]/analyze-message/route.ts
autonomous: true
requirements: [TOOLS-01, TOOLS-02, TOOLS-03, TOOLS-04]

must_haves:
  truths:
    - "All 5 modified Phase 8 files are committed to git"
    - "Working tree is clean for the Phase 8 file set after the commit"
    - "Commit message references Phase 8 and the 12 new tool types"
    - "The commit includes ALL six target files (types, defenseTools, DefenseToolCard, anthropic, defend route, analyze-message route)"
  artifacts:
    - path: ".git/HEAD"
      provides: "Git HEAD pointing to the new Phase 8 commit"
      contains: "ref:"
  key_links:
    - from: "git commit"
      to: "Phase 8 file set"
      via: "explicit file list passed to git add"
      pattern: "feat\\(08\\)|feat\\(phase-08\\)"
---

<objective>
Commit the Phase 8 implementation that Plan 01 just verified.

Purpose: Phase 8 code already lives in the working tree (modified, not staged).
Plan 01 proved it is correct. This plan stages the 6 Phase 8 files explicitly
(NOT `git add -A` — other unrelated files are also dirty per `git status`)
and creates a single descriptive commit.

Output: One commit on `main` containing the Phase 8 changes, with a message
that names the 12 new tool types and the requirement IDs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-expanded-tools/08-CONTEXT.md
@.planning/phases/08-expanded-tools/08-01-PLAN.md

<interfaces>
The 6 files to commit (and ONLY these — git status shows many other dirty
files from unrelated UI tweaks; do NOT include them):

- types/index.ts
- lib/defenseTools.ts
- components/defense/DefenseToolCard.tsx
- lib/anthropic.ts
- app/api/projects/[id]/defend/route.ts
- app/api/projects/[id]/analyze-message/route.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage and commit Phase 8 files only</name>
  <files>
    types/index.ts,
    lib/defenseTools.ts,
    components/defense/DefenseToolCard.tsx,
    lib/anthropic.ts,
    app/api/projects/[id]/defend/route.ts,
    app/api/projects/[id]/analyze-message/route.ts
  </files>
  <read_first>
    - .planning/phases/08-expanded-tools/08-01-SUMMARY.md (if it exists — confirms verification passed before committing)
    - Run `git status --short` to see ALL dirty files; confirm the 6 target files are listed as modified (M)
    - Run `git diff --stat types/index.ts lib/defenseTools.ts components/defense/DefenseToolCard.tsx lib/anthropic.ts "app/api/projects/[id]/defend/route.ts" "app/api/projects/[id]/analyze-message/route.ts"` to confirm only Phase 8 changes are present in those files
  </read_first>
  <action>
    Pre-flight (read-only, MUST pass before staging):

    1. Confirm Plan 08-01 verification passed. Either:
       a) `.planning/phases/08-expanded-tools/08-01-SUMMARY.md` exists and reports success, OR
       b) Re-run `npx tsc --noEmit` and confirm exit 0.
       If neither holds, STOP — do not commit.

    2. Run `git status --short` and visually confirm these exact 6 files appear with `M` status:
       - types/index.ts
       - lib/defenseTools.ts
       - components/defense/DefenseToolCard.tsx
       - lib/anthropic.ts
       - app/api/projects/[id]/defend/route.ts
       - app/api/projects/[id]/analyze-message/route.ts

    Stage and commit:

    3. Stage ONLY the 6 Phase 8 files by name (do NOT use `git add .` or `git add -A` —
       the working tree contains many unrelated dirty files per git status):

       ```
       git add \
         types/index.ts \
         lib/defenseTools.ts \
         components/defense/DefenseToolCard.tsx \
         lib/anthropic.ts \
         "app/api/projects/[id]/defend/route.ts" \
         "app/api/projects/[id]/analyze-message/route.ts"
       ```

    4. Confirm staged set is exactly 6 files:
       `git diff --cached --name-only | wc -l` MUST return `6`.
       `git diff --cached --name-only | sort` MUST list exactly the 6 paths above.

    5. Create the commit using a HEREDOC for proper formatting:

       ```
       git commit -m "$(cat <<'EOF'
       feat(08): add 12 new defense tool types

       Adds ghost_client, feedback_stall, moving_goalposts, discount_pressure,
       retroactive_discount, rate_increase_pushback, rush_fee_demand, ip_dispute,
       chargeback_threat, spec_work_pressure, post_handoff_request, and
       review_threat to the DefenseTool union, the DEFENSE_TOOLS registry, the
       DefenseToolCard ICON_MAP, the DEFENSE_SYSTEM_PROMPT and
       CLASSIFY_SYSTEM_PROMPT in lib/anthropic.ts, the TOOL_LABELS map in the
       defend route, and DEFENSE_TOOL_VALUES in the analyze-message route.

       Brings the total defense tool count from 8 to 20. Free-tier credit pool
       is shared across all 20 tools (no new RPC needed — D-10).

       Note: lucide-react does not export ReceiptX; retroactive_discount uses
       Receipt instead (D-05).

       Closes TOOLS-01, TOOLS-02, TOOLS-03, TOOLS-04.

       Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
       EOF
       )"
       ```

    6. Confirm commit succeeded:
       - `git log -1 --pretty=format:%s` MUST contain `feat(08)` and `12 new defense tool types`.
       - `git log -1 --name-only --pretty=format:""` MUST list exactly the 6 Phase 8 files.

    7. Run `git status --short` afterward — the 6 Phase 8 files MUST no longer appear.
       (Other unrelated dirty files from the working tree are expected to remain — that's correct.)

    DO NOT push. The user pushes manually.
  </action>
  <acceptance_criteria>
    - Pre-flight: `npx tsc --noEmit` exits 0 OR 08-01-SUMMARY.md confirms verification
    - `git diff --cached --name-only | wc -l` returns `6` immediately before commit
    - The staged file list is exactly: types/index.ts, lib/defenseTools.ts, components/defense/DefenseToolCard.tsx, lib/anthropic.ts, app/api/projects/[id]/defend/route.ts, app/api/projects/[id]/analyze-message/route.ts
    - `git log -1 --pretty=format:%s` matches the regex `^feat\(08\): add 12 new defense tool types`
    - `git log -1 --pretty=format:%B` contains the strings `TOOLS-01`, `TOOLS-02`, `TOOLS-03`, `TOOLS-04`
    - `git log -1 --name-only --pretty=format:""` lists exactly 6 file paths and includes all six Phase 8 files
    - After the commit, `git status --short types/index.ts lib/defenseTools.ts components/defense/DefenseToolCard.tsx lib/anthropic.ts "app/api/projects/[id]/defend/route.ts" "app/api/projects/[id]/analyze-message/route.ts"` returns no output (all clean)
    - No other files were accidentally staged or committed
  </acceptance_criteria>
  <verify>
    <automated>bash -c "git log -1 --pretty=format:%s | grep -qE '^feat\(08\): add 12 new defense tool types' && git log -1 --name-only --pretty=format:'' | grep -c '\\S' | grep -q '^6$'"</automated>
  </verify>
  <done>One new commit exists on main, subject is "feat(08): add 12 new defense tool types", commit touches exactly the 6 Phase 8 files, and no Phase 8 files remain in the working tree as modified.</done>
</task>

</tasks>

<verification>
After Task 1, the Phase 8 code is committed:
- `git log -1` shows the new commit with the correct subject and body
- The 6 modified Phase 8 files are no longer in the working tree dirty list
- Other unrelated dirty files (UI tweaks, etc.) remain untouched
- The commit message names all 4 requirement IDs (TOOLS-01..04)
</verification>

<success_criteria>
- Phase 8 implementation is committed to git in a single, descriptive commit
- Commit subject follows project convention: `feat(08): add 12 new defense tool types`
- Commit body lists the 12 new tool types and references requirement IDs
- Exactly 6 files were committed (no accidental inclusion of unrelated dirty files)
- TOOLS-01, TOOLS-02, TOOLS-03, TOOLS-04 marked closed via the commit message
</success_criteria>

<output>
After completion, create `.planning/phases/08-expanded-tools/08-02-SUMMARY.md`.

The SUMMARY MUST include:
- The commit SHA (`git rev-parse HEAD`)
- The exact commit subject and body
- The list of files in the commit (`git log -1 --name-only --pretty=format:""`)
- Any unrelated dirty files that remained in the working tree (for transparency)
</output>
