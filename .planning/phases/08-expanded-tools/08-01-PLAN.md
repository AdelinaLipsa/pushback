---
phase: 08-expanded-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - types/index.ts
  - lib/defenseTools.ts
  - components/defense/DefenseToolCard.tsx
  - lib/anthropic.ts
  - app/api/projects/[id]/defend/route.ts
  - app/api/projects/[id]/analyze-message/route.ts
autonomous: true
requirements: [TOOLS-01, TOOLS-02, TOOLS-03, TOOLS-04]

must_haves:
  truths:
    - "All 20 defense tool types (8 original + 12 new) appear in the DefenseTool union in types/index.ts"
    - "All 12 new tool entries exist in DEFENSE_TOOLS in lib/defenseTools.ts (ghost_client through review_threat)"
    - "Every icon name referenced by DEFENSE_TOOLS resolves in ICON_MAP in DefenseToolCard.tsx"
    - "DEFENSE_SYSTEM_PROMPT contains a tone block for each of the 12 new tool types"
    - "CLASSIFY_SYSTEM_PROMPT contains a description line for each of the 12 new tool types"
    - "TOOL_LABELS in defend/route.ts contains an ALL-CAPS label for each of the 12 new tool types"
    - "DEFENSE_TOOL_VALUES in analyze-message/route.ts contains all 20 tool type strings"
    - "tsc --noEmit succeeds with zero type errors"
  artifacts:
    - path: "types/index.ts"
      provides: "DefenseTool union with all 20 string literal members"
      contains: "review_threat"
    - path: "lib/defenseTools.ts"
      provides: "DEFENSE_TOOLS array with 20 entries"
      contains: "type: 'review_threat'"
    - path: "components/defense/DefenseToolCard.tsx"
      provides: "ICON_MAP with all icons used by DEFENSE_TOOLS"
      contains: "Receipt"
    - path: "lib/anthropic.ts"
      provides: "DEFENSE_SYSTEM_PROMPT tone blocks + CLASSIFY_SYSTEM_PROMPT descriptions for 20 tools"
      contains: "review_threat:"
    - path: "app/api/projects/[id]/defend/route.ts"
      provides: "TOOL_LABELS map with 20 entries"
      contains: "REVIEW / REPUTATION THREAT"
    - path: "app/api/projects/[id]/analyze-message/route.ts"
      provides: "DEFENSE_TOOL_VALUES const with all 20 tool type strings"
      contains: "review_threat"
  key_links:
    - from: "lib/defenseTools.ts"
      to: "components/defense/DefenseToolCard.tsx"
      via: "icon name lookup in ICON_MAP"
      pattern: "ICON_MAP\\["
    - from: "components/defense/DefenseDashboard.tsx"
      to: "lib/defenseTools.ts"
      via: "DEFENSE_TOOLS.map() at line ~268"
      pattern: "DEFENSE_TOOLS\\.map"
    - from: "app/api/projects/[id]/analyze-message/route.ts"
      to: "z.enum(DEFENSE_TOOL_VALUES)"
      via: "Zod validates classifier output against all 20 types"
      pattern: "z\\.enum\\(DEFENSE_TOOL_VALUES\\)"
---

<objective>
Verify the already-implemented Phase 8 changes are correct and complete.

Purpose: Phase 8 implementation lives in the working tree across 5 files. Before
committing, confirm the DefenseTool union, tool registry, icon map, AI prompts,
and route validators all agree on the same 20 tool types. Catch missing icons,
typos in tool names, or out-of-sync constants before they ship.

Output: A clean `tsc --noEmit`, exact tool-count proofs, and a receipt that every
icon used resolves in lucide-react.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-expanded-tools/08-CONTEXT.md
@.planning/phases/08-expanded-tools/08-PLAN.md

<interfaces>
The 12 new tool type literals (must appear in every consumer):
ghost_client, feedback_stall, moving_goalposts, discount_pressure,
retroactive_discount, rate_increase_pushback, rush_fee_demand, ip_dispute,
chargeback_threat, spec_work_pressure, post_handoff_request, review_threat

Icons added to ICON_MAP (per D-05/D-06):
EyeOff, Hourglass, Shuffle, TrendingDown, Receipt (NOT ReceiptX),
TrendingUp, Zap, Copyright, CreditCard, Eye, PackageOpen, Star
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript and tool-count verification</name>
  <files>types/index.ts, lib/defenseTools.ts, app/api/projects/[id]/analyze-message/route.ts</files>
  <read_first>
    - types/index.ts (find the `DefenseTool` union — confirm 20 string literals)
    - lib/defenseTools.ts (count entries in DEFENSE_TOOLS — must be 20)
    - app/api/projects/[id]/analyze-message/route.ts (find DEFENSE_TOOL_VALUES — must contain 20 strings)
  </read_first>
  <action>
    Run the following grep + counting commands and verify each acceptance criterion. If any check fails, STOP and report the discrepancy — do not proceed to Task 2.

    1. Type union completeness:
       `grep -oE "'(ghost_client|feedback_stall|moving_goalposts|discount_pressure|retroactive_discount|rate_increase_pushback|rush_fee_demand|ip_dispute|chargeback_threat|spec_work_pressure|post_handoff_request|review_threat)'" types/index.ts | sort -u | wc -l`
       MUST equal 12.

    2. DEFENSE_TOOLS entry count:
       `grep -cE "^\s*type: '" lib/defenseTools.ts`
       MUST equal 20.

    3. DEFENSE_TOOL_VALUES count:
       `grep -oE "'(scope_change|payment_first|payment_second|payment_final|revision_limit|kill_fee|delivery_signoff|dispute_response|ghost_client|feedback_stall|moving_goalposts|discount_pressure|retroactive_discount|rate_increase_pushback|rush_fee_demand|ip_dispute|chargeback_threat|spec_work_pressure|post_handoff_request|review_threat)'" app/api/projects/\[id\]/analyze-message/route.ts | sort -u | wc -l`
       MUST equal 20.

    4. TypeScript compile check:
       `npx tsc --noEmit`
       MUST exit 0 with zero errors. (Pre-existing ESLint warnings noted in STATE.md "Deferred Items" are NOT TypeScript errors and are acceptable.)

    Record the numeric output of each grep so the SUMMARY can quote actual proof.
  </action>
  <acceptance_criteria>
    - `grep -oE "'(ghost_client|feedback_stall|moving_goalposts|discount_pressure|retroactive_discount|rate_increase_pushback|rush_fee_demand|ip_dispute|chargeback_threat|spec_work_pressure|post_handoff_request|review_threat)'" types/index.ts | sort -u | wc -l` returns `12`
    - `grep -cE "^\s*type: '" lib/defenseTools.ts` returns `20`
    - `grep -c "ghost_client\|feedback_stall\|moving_goalposts\|discount_pressure\|retroactive_discount\|rate_increase_pushback\|rush_fee_demand\|ip_dispute\|chargeback_threat\|spec_work_pressure\|post_handoff_request\|review_threat" app/api/projects/\[id\]/analyze-message/route.ts` is at least `12`
    - `npx tsc --noEmit` exits with status 0 and prints no errors
    - The string `review_threat` appears in all three files (types/index.ts, lib/defenseTools.ts, analyze-message/route.ts)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>All four checks pass. Tool count is exactly 20 in DEFENSE_TOOLS and DEFENSE_TOOL_VALUES; the 12 new literals appear in DefenseTool union; tsc reports zero errors.</done>
</task>

<task type="auto">
  <name>Task 2: Icon resolution + AI prompt completeness verification</name>
  <files>components/defense/DefenseToolCard.tsx, lib/anthropic.ts, app/api/projects/[id]/defend/route.ts</files>
  <read_first>
    - components/defense/DefenseToolCard.tsx (find ICON_MAP — confirm every icon name used by DEFENSE_TOOLS is a key)
    - lib/anthropic.ts (find DEFENSE_SYSTEM_PROMPT and CLASSIFY_SYSTEM_PROMPT — confirm 12 new entries in each)
    - app/api/projects/[id]/defend/route.ts (find TOOL_LABELS — confirm 12 new entries)
    - lib/defenseTools.ts (re-confirm icon names referenced)
  </read_first>
  <action>
    Run the following checks. If any check fails, STOP and report — do not proceed to commit (Plan 02).

    1. Confirm `Receipt` (NOT `ReceiptX`) is used in DefenseToolCard ICON_MAP per D-05:
       `grep "ReceiptX" components/defense/DefenseToolCard.tsx`
       MUST return zero matches.
       `grep "Receipt[^X]" components/defense/DefenseToolCard.tsx`
       MUST return at least one match.

    2. Every icon name referenced by DEFENSE_TOOLS exists in ICON_MAP. Extract icon names used:
       `grep -oE "icon: '[A-Z][a-zA-Z]+'" lib/defenseTools.ts | sort -u`
       Then for each name N, verify:
       `grep "${N}" components/defense/DefenseToolCard.tsx`
       returns at least one line. (The lucide-react import line and the ICON_MAP key both reference the name.)

    3. Confirm Lucide imports include the 12 new icon names. Run:
       `grep -E "from 'lucide-react'" -A 3 components/defense/DefenseToolCard.tsx | grep -E "EyeOff|Hourglass|Shuffle|TrendingDown|TrendingUp|Zap|Copyright|CreditCard|PackageOpen|Star|Receipt"`
       MUST find imports for the new icon names.

    4. Confirm DEFENSE_SYSTEM_PROMPT has 12 new tone blocks (each begins with `<tool_type>:`):
       `grep -cE "^(ghost_client|feedback_stall|moving_goalposts|discount_pressure|retroactive_discount|rate_increase_pushback|rush_fee_demand|ip_dispute|chargeback_threat|spec_work_pressure|post_handoff_request|review_threat):" lib/anthropic.ts`
       MUST be at least `24` (each appears once in DEFENSE_SYSTEM_PROMPT and once in CLASSIFY_SYSTEM_PROMPT — total 24). If counted only with leading-of-line anchor and the prompt blocks are inside template strings, alternatively run:
       `grep -cE "(ghost_client|feedback_stall|moving_goalposts|discount_pressure|retroactive_discount|rate_increase_pushback|rush_fee_demand|ip_dispute|chargeback_threat|spec_work_pressure|post_handoff_request|review_threat)" lib/anthropic.ts`
       MUST return at least 24.

    5. Confirm TOOL_LABELS has 12 new ALL-CAPS labels in defend/route.ts:
       `grep -cE "^\s*(ghost_client|feedback_stall|moving_goalposts|discount_pressure|retroactive_discount|rate_increase_pushback|rush_fee_demand|ip_dispute|chargeback_threat|spec_work_pressure|post_handoff_request|review_threat):" app/api/projects/\[id\]/defend/route.ts`
       MUST equal `12`.

    6. Spot-check a couple of label strings exist verbatim per D-08:
       `grep "GHOST CLIENT — RADIO SILENCE" app/api/projects/\[id\]/defend/route.ts`
       `grep "REVIEW / REPUTATION THREAT" app/api/projects/\[id\]/defend/route.ts`
       Both MUST find a match.

    Record the actual numeric outputs.
  </action>
  <acceptance_criteria>
    - `grep "ReceiptX" components/defense/DefenseToolCard.tsx` returns zero matches (D-05)
    - `grep -E "icon: 'Receipt'" lib/defenseTools.ts` returns one match (D-05: retroactive_discount uses `Receipt`)
    - All 12 new icon names (EyeOff, Hourglass, Shuffle, TrendingDown, TrendingUp, Zap, Copyright, CreditCard, Eye, PackageOpen, Star, Receipt) appear in components/defense/DefenseToolCard.tsx
    - `grep -cE "^\s*(ghost_client|feedback_stall|moving_goalposts|discount_pressure|retroactive_discount|rate_increase_pushback|rush_fee_demand|ip_dispute|chargeback_threat|spec_work_pressure|post_handoff_request|review_threat):" app/api/projects/\[id\]/defend/route.ts` returns `12`
    - `grep "GHOST CLIENT — RADIO SILENCE" app/api/projects/\[id\]/defend/route.ts` returns one match
    - `grep "REVIEW / REPUTATION THREAT" app/api/projects/\[id\]/defend/route.ts` returns one match
    - The 12 new tool type literals appear at least 24 times across lib/anthropic.ts (≥12 in DEFENSE_SYSTEM_PROMPT + ≥12 in CLASSIFY_SYSTEM_PROMPT)
  </acceptance_criteria>
  <verify>
    <automated>bash -c "test $(grep -cE \"^\s*(ghost_client|feedback_stall|moving_goalposts|discount_pressure|retroactive_discount|rate_increase_pushback|rush_fee_demand|ip_dispute|chargeback_threat|spec_work_pressure|post_handoff_request|review_threat):\" 'app/api/projects/[id]/defend/route.ts') -eq 12"</automated>
  </verify>
  <done>All icon names resolve, all AI prompt blocks present (24+ literal occurrences across the two prompts), all 12 new TOOL_LABELS present with correct ALL-CAPS formatting, ReceiptX is absent.</done>
</task>

</tasks>

<verification>
After both tasks complete, the working tree is proven correct:
- TypeScript compiles cleanly (`tsc --noEmit` exit 0)
- 20 tools registered everywhere they need to be (DEFENSE_TOOLS, DEFENSE_TOOL_VALUES, DefenseTool union)
- 12 new icons resolve in ICON_MAP and lucide-react imports
- 12 new tone blocks + 12 new classifier descriptions in lib/anthropic.ts
- 12 new ALL-CAPS labels in TOOL_LABELS

If any check fails, the executor STOPS and reports the discrepancy. Do not commit broken state.
</verification>

<success_criteria>
- `npx tsc --noEmit` exits 0 with no errors (TOOLS-04)
- DEFENSE_TOOLS contains 20 entries with required `type` and `icon` keys (TOOLS-01)
- DEFENSE_TOOL_VALUES enumerates all 20 tool type strings (TOOLS-03)
- All 12 new tool types have tone blocks in DEFENSE_SYSTEM_PROMPT (TOOLS-02)
- All 12 new icon names resolve via ICON_MAP and lucide-react import (TOOLS-01)
- All 12 new tool labels appear in TOOL_LABELS in defend/route.ts (TOOLS-02)
</success_criteria>

<output>
After completion, create `.planning/phases/08-expanded-tools/08-01-SUMMARY.md`.

The SUMMARY MUST quote the actual numeric output of each grep / wc -l command
so reviewers can confirm the implementation is correct without re-running the
checks. Note any remaining concerns or follow-ups required by Plan 02.
</output>
